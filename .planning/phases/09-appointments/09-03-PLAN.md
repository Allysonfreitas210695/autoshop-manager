---
phase: 09-appointments
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - src/app/(dashboard)/appointments/_components/AppointmentCard.tsx
  - src/app/(dashboard)/appointments/appointments-client.tsx
autonomous: false
requirements: [APPT-01, APPT-02]
must_haves:
  truths:
    - "AppointmentCard exibe serviceType (label formatada) e duration quando nao-null"
    - "Botao Editar em AppointmentCard abre EditAppointmentDrawer com o agendamento"
    - "EditAppointmentDrawer esta integrado em appointments-client.tsx via estado"
  artifacts:
    - path: "src/app/(dashboard)/appointments/_components/AppointmentCard.tsx"
      provides: "display serviceType/duration + botao Editar + prop onEdit (D-05, D-04)"
      contains: "SERVICE_TYPE_LABELS"
    - path: "src/app/(dashboard)/appointments/appointments-client.tsx"
      provides: "integracao EditAppointmentDrawer + handleEdit"
      contains: "EditAppointmentDrawer"
  key_links:
    - from: "src/app/(dashboard)/appointments/_components/AppointmentCard.tsx"
      to: "onEdit prop"
      via: "botao Editar onClick"
      pattern: "onEdit"
    - from: "src/app/(dashboard)/appointments/appointments-client.tsx"
      to: "EditAppointmentDrawer"
      via: "editingAppt state + render condicional"
      pattern: "EditAppointmentDrawer"
---

<objective>
Atualizar AppointmentCard para exibir serviceType (label formatada) e duration (D-05) e adicionar botao Editar (D-04); integrar EditAppointmentDrawer em appointments-client.tsx com estado editingAppt/editDrawerOpen e handleEdit, threading onEdit em todos os render sites de AppointmentCard.

Purpose: Fecha o loop visual e de interacao da edicao. Depende dos Planos 01 (AppointmentRow FKs) e 02 (EditAppointmentDrawer existe). Inclui checkpoint humano de verificacao visual (UI hint: yes).
Output: card com metadata + botao Editar; drawer de edicao aberto a partir do card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-appointments/09-CONTEXT.md
@.planning/phases/09-appointments/09-RESEARCH.md
@.planning/phases/09-appointments/09-PATTERNS.md
@.planning/phases/09-appointments/09-01-SUMMARY.md
@.planning/phases/09-appointments/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppointmentCard - display serviceType/duration + botao Editar</name>
  <files>src/app/(dashboard)/appointments/_components/AppointmentCard.tsx</files>
  <read_first>
    - src/app/(dashboard)/appointments/_components/AppointmentCard.tsx (Props type linhas 24-27; bloco notes ~linha 70; linha de botoes de acao ~linha 72 dentro do div border-t)
    - src/_data-access/appointments.ts (AppointmentRow.serviceType e duration ja existentes)
  </read_first>
  <action>
    Adicionar prop onEdit?: (id: string) => void ao type Props. Adicionar constante module-level SERVICE_TYPE_LABELS: Record<string, string> = { preventiva: "Preventiva", corretiva: "Corretiva", garantia: "Garantia", estetica: "Estetica", revisao: "Revisao", eletrica: "Eletrica", funilaria: "Funilaria" } (usar acentos corretos: Estetica->Estética, Revisao->Revisão, Eletrica->Elétrica conforme labels do CONTEXT D-05). Apos o bloco de notes e antes dos botoes de acao, inserir bloco condicional que renderiza quando appt.serviceType || appt.duration: um div com span font-mono para SERVICE_TYPE_LABELS[appt.serviceType] ?? appt.serviceType (quando serviceType) e span font-mono "{appt.duration} min" (quando duration). Dentro da linha de botoes existente (div border-t), adicionar botao Editar condicional a onEdit que chama onEdit(appt.id), com classe font-mono ml-auto e estilo secondary consistente com os botoes existentes.
  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && grep -q "SERVICE_TYPE_LABELS" "src/app/(dashboard)/appointments/_components/AppointmentCard.tsx" && grep -q "onEdit" "src/app/(dashboard)/appointments/_components/AppointmentCard.tsx" && grep -q "min" "src/app/(dashboard)/appointments/_components/AppointmentCard.tsx" && npx tsc --noEmit 2>&1 | grep -i "AppointmentCard" | head -5 || echo "NO_TS_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - Props inclui onEdit?: (id: string) => void
    - SERVICE_TYPE_LABELS mapeia as 7 chaves com labels acentuadas (Estética, Revisão, Elétrica)
    - serviceType e duration exibidos condicionalmente (apenas quando nao-null)
    - duration renderiza com sufixo " min"
    - botao Editar chama onEdit(appt.id) quando onEdit presente
    - tsc sem erro novo em AppointmentCard.tsx
  </acceptance_criteria>
  <done>Card exibe metadata serviceType/duration e expoe botao Editar via onEdit.</done>
</task>

<task type="auto">
  <name>Task 2: appointments-client - integrar EditAppointmentDrawer</name>
  <files>src/app/(dashboard)/appointments/appointments-client.tsx</files>
  <read_first>
    - src/app/(dashboard)/appointments/appointments-client.tsx (imports ~linha 42; drawerOpen state ~linha 75; handleStatusChange; render sites de AppointmentCard ~linhas 342 e 516; render de NewAppointmentDrawer ~linha 530)
    - src/app/(dashboard)/appointments/_components/EditAppointmentDrawer.tsx (Props: open, onClose, mechanics, customers, appt)
    - src/app/(dashboard)/appointments/_components/AppointmentCard.tsx (prop onEdit adicionada na Task 1)
  </read_first>
  <action>
    Importar EditAppointmentDrawer de ./_components/EditAppointmentDrawer. Adicionar estado: const [editDrawerOpen, setEditDrawerOpen] = useState(false) e const [editingAppt, setEditingAppt] = useState<AppointmentRow | null>(null). Adicionar funcao handleEdit(id: string) que faz const appt = appointments.find((a) => a.id === id); if (!appt) return; setEditingAppt(appt); setEditDrawerOpen(true). Em AMBOS os render sites de AppointmentCard (~linhas 342 e 516) adicionar prop onEdit={handleEdit} (week view usa AppointmentBadge, nao alterar — per Pitfall 4). Apos o NewAppointmentDrawer existente, renderizar condicionalmente {editingAppt && <EditAppointmentDrawer open={editDrawerOpen} onClose={() => { setEditDrawerOpen(false); setEditingAppt(null); }} mechanics={mechanics} customers={customers} appt={editingAppt} />}. Confirmar que mechanics e customers ja estao disponiveis no escopo do componente; se nao, derivar das props existentes.
  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && grep -q "EditAppointmentDrawer" "src/app/(dashboard)/appointments/appointments-client.tsx" && grep -q "handleEdit" "src/app/(dashboard)/appointments/appointments-client.tsx" && grep -c "onEdit={handleEdit}" "src/app/(dashboard)/appointments/appointments-client.tsx" && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - import de EditAppointmentDrawer presente
    - estado editDrawerOpen e editingAppt declarado
    - handleEdit localiza o appt e abre o drawer
    - onEdit={handleEdit} aparece em 2 render sites de AppointmentCard (grep -c == 2)
    - EditAppointmentDrawer renderizado condicionalmente com editingAppt
    - npm test -- --run suite completa verde
  </acceptance_criteria>
  <done>EditAppointmentDrawer abre a partir do botao Editar do card; suite completa verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacao visual do fluxo de agendamentos</name>
  <what-built>Migration 0004 aplicada, serviceType/duration exibidos no card, botao Editar abre EditAppointmentDrawer pre-populado, cancelamento mantem status, duration persiste como inteiro.</what-built>
  <how-to-verify>
    1. Rodar `npm run db:seed` (ou comando de seed do projeto) para popular agendamentos com serviceType/duration; depois `npm run dev`.
    2. Abrir /appointments. Verificar que os cards exibem a label de servico (ex. "Preventiva") e a duracao (ex. "60 min").
    3. Clicar Editar em um card -> o drawer abre com TODOS os campos pre-populados (cliente, veiculo, mecanico, data, hora, tipo de servico, duracao, notas).
    4. Alterar a duracao e salvar -> recarregar a pagina -> a nova duracao persiste.
    5. Clicar Cancelar em um agendamento -> o chip de status muda para Cancelado e PERMANECE (sem reverter) -> recarregar -> status cancelado persiste no DB.
    6. Criar um novo agendamento com duracao 60 -> recarregar -> a duracao aparece (nao foi descartada como NaN).
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                         | Description                                                   |
| ------------------------------------------------ | ------------------------------------------------------------- |
| AppointmentCard onEdit -> client state           | id do agendamento cruza para estado do client component       |
| EditAppointmentDrawer -> updateAppointmentAction | input do form cruza para action server (validado no Plano 02) |

## STRIDE Threat Register

| Threat ID | Category  | Component                   | Disposition | Mitigation Plan                                                             |
| --------- | --------- | --------------------------- | ----------- | --------------------------------------------------------------------------- |
| T-09-02   | Tampering | render de serviceType label | mitigate    | SERVICE_TYPE_LABELS[key] ?? key — fallback seguro, sem injecao (texto puro) |
| T-09-01   | Elevation | edicao via drawer           | mitigate    | submissao usa updateAppointmentAction (authActionClient) do Plano 02        |
| T-09-SC   | Tampering | npm installs                | accept      | nenhum pacote novo nesta fase                                               |

</threat_model>

<verification>
- npm test -- --run suite completa verde
- npx tsc --noEmit sem erros novos
- Checkpoint humano: fluxo visual aprovado (display, edit pre-populado, cancel persiste, duration persiste)
</verification>

<success_criteria>

- AppointmentCard exibe serviceType label e duration quando presentes
- Botao Editar abre EditAppointmentDrawer pre-populado
- Cancelamento persiste status correto
- duration persiste como inteiro
- Verificacao humana aprovada
  </success_criteria>

<output>
Create `.planning/phases/09-appointments/09-03-SUMMARY.md` when done
</output>
