---
phase: 09-appointments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/_db/migrations/0004_appointments_service_type_duration.sql
  - src/_data-access/appointments.ts
  - src/_actions/appointments.ts
  - src/_actions/_audit.test.ts
  - src/_actions/appointments.test.ts
  - src/app/(dashboard)/appointments/_components/NewAppointmentDrawer.tsx
  - src/app/(dashboard)/appointments/appointments-client.tsx
  - scripts/seed.ts
autonomous: true
requirements: [APPT-01, APPT-02]
must_haves:
  truths:
    - "Colunas service_type e duration existem na tabela appointments do Neon DB"
    - "Cancelar um agendamento mantem o status correto na UI (sem reversao por closure)"
    - "duration submetido no formulario de criacao persiste como inteiro (nao NaN)"
    - "AppointmentRow expoe customerId, vehicleId, mechanicId como UUIDs brutos"
    - "Seed popula serviceType e duration em todas as 8 linhas de agendamento"
  artifacts:
    - path: "src/_db/migrations/0004_appointments_service_type_duration.sql"
      provides: "DDL aplicado: service_type (text) + duration (integer)"
      contains: "ADD COLUMN"
    - path: "src/_data-access/appointments.ts"
      provides: "AppointmentRow com FKs brutos + select dos FKs"
      contains: "customerId"
    - path: "src/_actions/appointments.ts"
      provides: "updateAppointmentStatusAction retornando { id, status }"
      contains: "status: parsedInput.status"
  key_links:
    - from: "src/_data-access/appointments.ts"
      to: "appointments.customerId/vehicleId/mechanicId"
      via: "select + map"
      pattern: "customerId: appointments.customerId"
    - from: "src/app/(dashboard)/appointments/appointments-client.tsx"
      to: "data.status"
      via: "onSuccess callback"
      pattern: "status: data.status"
---

<objective>
Aplicar a migration 0004 ao Neon DB e corrigir a camada de dados/acoes: estender AppointmentRow com FKs brutos, corrigir o closure bug do cancelamento (D-02), corrigir o bug do duration valueAsNumber, atualizar o seed (D-01) e ajustar a contagem do audit test de 18 para 19.

Purpose: Estes sao os bloqueadores de fundacao (sem dependencia de UI nova). Sem a migration aplicada, nenhum dado com serviceType/duration persiste; sem os FKs brutos, o EditAppointmentDrawer (Wave 2) nao pode pre-popular selects; sem corrigir o audit count antes, adicionar updateAppointmentAction quebra o teste.
Output: DB migrado, AppointmentRow estendido, dois bugs corrigidos, seed enriquecido, audit count em 19.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-appointments/09-CONTEXT.md
@.planning/phases/09-appointments/09-RESEARCH.md
@.planning/phases/09-appointments/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Aplicar migration 0004 ao Neon DB</name>
  <files>src/_db/migrations/0004_appointments_service_type_duration.sql</files>
  <read_first>
    - src/_db/migrations/0004_appointments_service_type_duration.sql (DDL: ADD COLUMN service_type text + duration integer)
    - src/_db/migrations/meta/_journal.json (confirma idx=4 registrado)
    - package.json (script db:migrate = drizzle-kit migrate, nao-interativo)
  </read_first>
  <action>
    Rodar npm run db:migrate (drizzle-kit migrate, ja nao-interativo) para aplicar as duas instrucoes ALTER TABLE da migration 0004 ao Neon DB. Requer DATABASE_URL no .env. Se a saida reportar "No migrations to run" a migration ja foi aplicada via push em fase anterior — isso e aceitavel (no-op), registrar na SUMMARY. Se aplicar, as colunas service_type (text) e duration (integer) passam a existir na tabela appointments. NAO editar o arquivo .sql nem o _journal.json — apenas executar o comando de aplicacao.
  </action>
  <verify>
    <automated>npm run db:migrate</automated>
  </verify>
  <acceptance_criteria>
    - npm run db:migrate sai com codigo 0
    - Saida indica migration aplicada OU "No migrations to run" (ja aplicada via push)
    - Arquivo 0004_*.sql e _journal.json permanecem inalterados (sem edicao manual)
  </acceptance_criteria>
  <done>Migration 0004 aplicada (ou confirmada ja aplicada); colunas service_type e duration disponiveis no Neon DB.</done>
</task>

<task type="auto">
  <name>Task 2: Estender AppointmentRow com FKs brutos + atualizar audit test count</name>
  <files>src/_data-access/appointments.ts, src/_actions/_audit.test.ts</files>
  <read_first>
    - src/_data-access/appointments.ts (AppointmentRow type linhas 9-21; select linhas 44-57; map linhas 65-80 — FKs NAO presentes atualmente)
    - src/_actions/_audit.test.ts (assertion de contagem total de actions na linha ~57)
  </read_first>
  <action>
    Em src/_data-access/appointments.ts: adicionar tres campos ao type AppointmentRow — customerId: string | null, vehicleId: string | null, mechanicId: string | null. No bloco .select({...}) de listAppointments adicionar customerId: appointments.customerId, vehicleId: appointments.vehicleId, mechanicId: appointments.mechanicId. No rows.map((row) => ({...})) adicionar customerId: row.customerId, vehicleId: row.vehicleId, mechanicId: row.mechanicId. Zero round-trips extras — os FKs ja estao na tabela appointments.

    Em src/_actions/_audit.test.ts: localizar a assertion expect(total).toBe(18) e alterar o literal de 18 para 19. Isto prepara o teste para a adicao de updateAppointmentAction no Plano 02 (per Pitfall 1). Confirmar primeiro o valor atual lendo o arquivo — se nao for exatamente 18, registrar a discrepancia e ajustar para (valor_atual + 1).

  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && grep -q "customerId: appointments.customerId" src/_data-access/appointments.ts && grep -q "vehicleId: row.vehicleId" src/_data-access/appointments.ts && grep -q "toBe(19)" src/_actions/_audit.test.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - AppointmentRow contem customerId, vehicleId, mechanicId tipados como string | null
    - .select de listAppointments inclui appointments.customerId/vehicleId/mechanicId
    - rows.map retorna os tres FKs mapeados de row
    - _audit.test.ts assertion alterada para toBe(19)
    - npx tsc --noEmit nao reporta erro novo em appointments.ts
  </acceptance_criteria>
  <done>AppointmentRow expoe os tres FKs brutos via query existente; audit count preparado em 19.</done>
</task>

<task type="auto">
  <name>Task 3: Corrigir closure bug (D-02), valueAsNumber e seed (D-01)</name>
  <files>src/_actions/appointments.ts, src/app/(dashboard)/appointments/appointments-client.tsx, src/app/(dashboard)/appointments/_components/NewAppointmentDrawer.tsx, scripts/seed.ts, src/_actions/appointments.test.ts</files>
  <read_first>
    - src/_actions/appointments.ts (updateAppointmentStatusAction linhas 41-56 — atualmente retorna { id })
    - src/app/(dashboard)/appointments/appointments-client.tsx (onSuccess linhas 82-96 com bug de closure stale)
    - src/app/(dashboard)/appointments/_components/NewAppointmentDrawer.tsx (register("duration") linha ~202 sem valueAsNumber)
    - scripts/seed.ts (loop de appointments linhas ~541-571, .values sem serviceType/duration)
    - src/_actions/appointments.test.ts (bloco describe de wiring estatico)
  </read_first>
  <action>
    1. D-02 em src/_actions/appointments.ts: alterar o return de updateAppointmentStatusAction de { id: parsedInput.id } para { id: parsedInput.id, status: parsedInput.status }.
    2. D-02 em appointments-client.tsx: substituir o onSuccess callback que usa appointments.find((x) => x.id === data.id)?.status (closure stale) por: setAppointments((prev) => prev.map((a) => (a.id === data.id ? { ...a, status: data.status } : a))). Manter o guard if (!data) return.
    3. valueAsNumber em NewAppointmentDrawer.tsx: alterar register("duration") para register("duration", { valueAsNumber: true }) — sem isto duration vira NaN e e descartado no insert.
    4. D-01 em scripts/seed.ts: antes do loop de appointments declarar serviceTypes = ["preventiva","corretiva","revisao","garantia","eletrica","estetica","funilaria","corretiva"] e durations = [60,90,45,120,60,90,60,45]; dentro do db.insert(appointments).values({...}) existente adicionar serviceType: serviceTypes[i], duration: durations[i] usando o indice i do loop. Garantir que o indice usado corresponde ao do loop existente.
    5. appointments.test.ts: adicionar assertion verificando que updateAppointmentStatusAction retorna status — checar que o source contem "status: parsedInput.status".
  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && grep -q "status: parsedInput.status" src/_actions/appointments.ts && grep -q "status: data.status" "src/app/(dashboard)/appointments/appointments-client.tsx" && grep -q 'valueAsNumber: true' "src/app/(dashboard)/appointments/_components/NewAppointmentDrawer.tsx" && grep -q "serviceType: serviceTypes" scripts/seed.ts && npm test -- --run src/_actions/appointments.test.ts src/_actions/_audit.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - updateAppointmentStatusAction retorna { id, status } (grep "status: parsedInput.status")
    - onSuccess em appointments-client.tsx usa data.status (sem appointments.find closure)
    - register("duration") inclui { valueAsNumber: true }
    - seed.ts insere serviceType e duration via arrays indexados pelo loop
    - npm test -- --run em appointments.test.ts e _audit.test.ts sai verde (audit count 19)
  </acceptance_criteria>
  <done>Closure bug eliminado, duration persiste como inteiro, seed enriquecido, testes estaticos verdes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                              |
| -------------------------------------------- | ---------------------------------------- |
| client form -> updateAppointmentStatusAction | input de status cruza para action server |
| script seed -> DB                            | dados de seed escrevem direto no Neon    |

## STRIDE Threat Register

| Threat ID | Category  | Component                     | Disposition | Mitigation Plan                                                                    |
| --------- | --------- | ----------------------------- | ----------- | ---------------------------------------------------------------------------------- |
| T-09-01   | Elevation | updateAppointmentStatusAction | mitigate    | action mantem wrapper authActionClient (sem alteracao de guarda)                   |
| T-09-02   | Tampering | status enum input             | mitigate    | z.enum(["scheduled","confirmed","completed","cancelled"]) valida antes do update   |
| T-09-SC   | Tampering | npm installs                  | accept      | nenhum pacote novo instalado nesta fase (RESEARCH: Package Legitimacy Audit vazio) |

</threat_model>

<verification>
- npm run db:migrate aplica/confirma migration 0004
- npm test -- --run src/_actions/appointments.test.ts src/_actions/_audit.test.ts verde
- npx tsc --noEmit sem erros novos
</verification>

<success_criteria>

- Colunas service_type e duration existem no Neon DB
- AppointmentRow expoe customerId/vehicleId/mechanicId
- Cancelamento mantem status correto (closure bug corrigido)
- duration persiste como inteiro no create
- Seed popula serviceType + duration nas 8 linhas
- audit count em 19
  </success_criteria>

<output>
Create `.planning/phases/09-appointments/09-01-SUMMARY.md` when done
</output>
