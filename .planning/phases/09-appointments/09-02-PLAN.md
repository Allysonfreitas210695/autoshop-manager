---
phase: 09-appointments
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/_actions/appointments.ts
  - src/_actions/appointments.test.ts
  - src/_hooks/use-appointment-form.ts
  - src/app/(dashboard)/appointments/_components/EditAppointmentDrawer.tsx
autonomous: true
requirements: [APPT-01, APPT-02]
must_haves:
  truths:
    - "updateAppointmentAction aceita e persiste todos os campos do agendamento"
    - "useAppointmentForm suporta mode edit com initialValues e appointmentId"
    - "EditAppointmentDrawer pre-popula campos a partir de AppointmentRow sem query extra"
  artifacts:
    - path: "src/_actions/appointments.ts"
      provides: "updateAppointmentAction (D-03)"
      contains: "updateAppointmentAction"
    - path: "src/_hooks/use-appointment-form.ts"
      provides: "form hook com mode/initialValues/appointmentId"
      contains: "mode"
    - path: "src/app/(dashboard)/appointments/_components/EditAppointmentDrawer.tsx"
      provides: "drawer de edicao (D-04)"
      exports: ["EditAppointmentDrawer"]
  key_links:
    - from: "src/app/(dashboard)/appointments/_components/EditAppointmentDrawer.tsx"
      to: "useAppointmentForm"
      via: "mode edit + initialValues"
      pattern: 'mode: "edit"'
    - from: "src/_hooks/use-appointment-form.ts"
      to: "updateAppointmentAction"
      via: "useAction edit branch"
      pattern: "updateAppointmentAction"
---

<objective>
Adicionar updateAppointmentAction (D-03, todos os campos), estender useAppointmentForm para modo edit (mode/initialValues/appointmentId) e criar EditAppointmentDrawer espelhando NewAppointmentDrawer (D-04).

Purpose: Habilita a capacidade de edicao completa de agendamentos. Depende do Plano 01 (FKs brutos em AppointmentRow para pre-popular selects; audit count em 19).
Output: action de update funcional, hook bimodal, drawer de edicao.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-appointments/09-CONTEXT.md
@.planning/phases/09-appointments/09-RESEARCH.md
@.planning/phases/09-appointments/09-PATTERNS.md
@.planning/phases/09-appointments/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar updateAppointmentAction (D-03)</name>
  <files>src/_actions/appointments.ts, src/_actions/appointments.test.ts</files>
  <read_first>
    - src/_actions/appointments.ts (imports linhas 1-9; createAppointmentAction linhas 11-39 como analogo; padrao authActionClient.schema().action())
    - src/_lib/safe-action.ts (authActionClient wrapper — guard de sessao + ActionError)
    - src/_actions/appointments.test.ts (estrutura describe de wiring estatico via blocks)
  </read_first>
  <behavior>
    - updateAppointmentAction usa authActionClient.schema().action()
    - schema aceita: id (z.uuid), customerId/mechanicId (z.string optional), vehicleId (z.uuid optional), scheduledAt (z.string datetime), serviceType (z.string optional), duration (z.number int min 1 optional), notes (z.string optional), status (z.enum optional)
    - action faz db.update(appointments).set({...rest, scheduledAt: new Date(rest.scheduledAt), updatedAt: new Date()}).where(eq(appointments.id, id))
    - chama revalidatePath("/appointments") antes do return
    - retorna { id }
  </behavior>
  <action>
    Adicionar updateAppointmentAction em src/_actions/appointments.ts seguindo o analogo createAppointmentAction. Schema zod com os campos: id z.uuid(); customerId z.string().optional(); vehicleId z.uuid().optional(); mechanicId z.string().optional(); scheduledAt z.string().datetime(); serviceType z.string().optional(); duration z.number().int().min(1).optional(); notes z.string().optional(); status z.enum(["scheduled","confirmed","completed","cancelled"]).optional(). No corpo: const { id, ...rest } = parsedInput; db.update(appointments).set({ ...rest, scheduledAt: new Date(rest.scheduledAt), updatedAt: new Date() }).where(eq(appointments.id, id)); revalidatePath("/appointments"); return { id }. Mitiga T-09-03: scheduledAt/duration validados por zod; FKs sao UUIDs validados — violacao de FK retorna erro do PG capturado pelo authActionClient.

    Em appointments.test.ts adicionar bloco describe verificando que updateAppointmentAction usa update(appointments) e revalidatePath("/appointments") via inspecao do block.body (mesmo padrao dos testes existentes).

  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && grep -q "export const updateAppointmentAction" src/_actions/appointments.ts && grep -q "update(appointments)" src/_actions/appointments.ts && npm test -- --run src/_actions/appointments.test.ts src/_actions/_audit.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - updateAppointmentAction exportada usando authActionClient.schema().action()
    - schema contem id, customerId, vehicleId, mechanicId, scheduledAt, serviceType, duration, notes, status
    - corpo chama db.update(appointments) e revalidatePath("/appointments")
    - npm test -- --run appointments.test.ts e _audit.test.ts verde (count 19 confere)
  </acceptance_criteria>
  <done>updateAppointmentAction persiste todos os campos; audit test verde com count 19.</done>
</task>

<task type="auto">
  <name>Task 2: Estender useAppointmentForm para modo edit</name>
  <files>src/_hooks/use-appointment-form.ts</files>
  <read_first>
    - src/_hooks/use-appointment-form.ts (Params type linhas 28-32; useForm linhas 41-45; useAction linhas 50-59; onSubmit linhas 61-72)
    - src/_actions/appointments.ts (updateAppointmentAction recem-criada na Task 1)
  </read_first>
  <action>
    Estender o type Params com: mode?: "create" | "edit"; initialValues?: Partial<AppointmentFormData>; appointmentId?: string. Confirmar que mechanics ja esta declarado em Params (per Pitfall 5 — se ausente, adicionar mechanics: MechanicOption[]). Em useForm, usar defaultValues: initialValues ?? { customerId: "", vehicleId: "", mechanicId: "" }. Criar dois useAction: executeCreate (createAppointmentAction, onSuccess reset+onClose+toast "criado") e executeUpdate (updateAppointmentAction, onSuccess onClose+toast "atualizado"). Derivar const execute = mode === "edit" ? executeUpdate : executeCreate e status analogamente. Em onSubmit, montar payload comum (customerId, vehicleId trim||undefined, mechanicId||undefined, scheduledAt via new Date(`${date}T${time}:00`).toISOString(), serviceType||undefined, duration||undefined, notes||undefined); se mode==="edit" && appointmentId chamar executeUpdate({ id: appointmentId, ...payload }), senao executeCreate(payload). Importar updateAppointmentAction.
  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && grep -q "updateAppointmentAction" src/_hooks/use-appointment-form.ts && grep -q 'mode' src/_hooks/use-appointment-form.ts && grep -q "appointmentId" src/_hooks/use-appointment-form.ts && npx tsc --noEmit 2>&1 | grep -i "use-appointment-form" | head -5 || echo "NO_TS_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - Params declara mode, initialValues, appointmentId e mechanics
    - useForm usa defaultValues a partir de initialValues quando presente
    - onSubmit ramifica em executeUpdate({ id, ...payload }) no modo edit
    - tsc nao reporta erro novo em use-appointment-form.ts
  </acceptance_criteria>
  <done>Hook bimodal: cria ou atualiza conforme mode, pre-popula via initialValues.</done>
</task>

<task type="auto">
  <name>Task 3: Criar EditAppointmentDrawer (D-04)</name>
  <files>src/app/(dashboard)/appointments/_components/EditAppointmentDrawer.tsx</files>
  <read_first>
    - src/app/(dashboard)/appointments/_components/NewAppointmentDrawer.tsx (linhas 1-271 — estrutura Sheet, imports, secoes de campos, footer; copiar verbatim e ajustar)
    - src/_hooks/use-appointment-form.ts (assinatura estendida da Task 2)
    - src/_data-access/appointments.ts (AppointmentRow com FKs brutos do Plano 01)
  </read_first>
  <action>
    Criar EditAppointmentDrawer espelhando NewAppointmentDrawer. Props: { open: boolean; onClose: () => void; mechanics: MechanicOption[]; customers: CustomerOption[]; appt: AppointmentRow }. Importar format de date-fns. Construir initialValues usando format(new Date(appt.scheduledAt), "yyyy-MM-dd") para date e format(new Date(appt.scheduledAt), "HH:mm") para time — NAO usar toISOString().split('T') (corrompe por timezone, per Pitfall 3); demais campos: customerId/vehicleId/mechanicId ?? "", serviceType ?? "", duration ?? undefined, notes ?? "". Chamar useAppointmentForm({ customers, mechanics, onClose, mode: "edit", initialValues, appointmentId: appt.id }). Copiar o corpo do form (secoes de campos) verbatim de NewAppointmentDrawer linhas 68-254, aplicando register("duration", { valueAsNumber: true }). Trocar titulo para "Editar Agendamento", descricao "Atualize os dados do agendamento" e label do botao para status === "executing" ? "Salvando..." : "Salvar Alteracoes". Exportar named EditAppointmentDrawer.
  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && grep -q "export function EditAppointmentDrawer\|export const EditAppointmentDrawer" "src/app/(dashboard)/appointments/_components/EditAppointmentDrawer.tsx" && grep -q 'format(new Date(appt.scheduledAt), "yyyy-MM-dd")' "src/app/(dashboard)/appointments/_components/EditAppointmentDrawer.tsx" && grep -q 'mode: "edit"' "src/app/(dashboard)/appointments/_components/EditAppointmentDrawer.tsx" && grep -q 'valueAsNumber: true' "src/app/(dashboard)/appointments/_components/EditAppointmentDrawer.tsx" && npx tsc --noEmit 2>&1 | grep -i "EditAppointmentDrawer" | head -5 || echo "NO_TS_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - EditAppointmentDrawer exportado (named export)
    - initialValues usa date-fns format para date (yyyy-MM-dd) e time (HH:mm)
    - chama useAppointmentForm com mode "edit", initialValues e appointmentId: appt.id
    - register("duration") inclui { valueAsNumber: true }
    - titulo "Editar Agendamento" e botao "Salvar Alteracoes"
    - tsc sem erro novo em EditAppointmentDrawer.tsx
  </acceptance_criteria>
  <done>Drawer de edicao pre-popula todos os campos via AppointmentRow e submete updateAppointmentAction.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                              | Description                                                 |
| ----------------------------------------------------- | ----------------------------------------------------------- |
| EditAppointmentDrawer form -> updateAppointmentAction | input do usuario (todos os campos) cruza para action server |
| updateAppointmentAction -> appointments table         | FKs customerId/vehicleId/mechanicId escrevem na tabela      |

## STRIDE Threat Register

| Threat ID | Category  | Component                          | Disposition | Mitigation Plan                                                                             |
| --------- | --------- | ---------------------------------- | ----------- | ------------------------------------------------------------------------------------------- |
| T-09-01   | Elevation | updateAppointmentAction            | mitigate    | authActionClient envolve a action (guard de sessao)                                         |
| T-09-02   | Tampering | input de campos do agendamento     | mitigate    | schema zod valida tipos/enum antes do db.update                                             |
| T-09-03   | Tampering | FK customerId/vehicleId/mechanicId | mitigate    | vehicleId via z.uuid(); FK invalida gera erro PG capturado por authActionClient/ActionError |
| T-09-SC   | Tampering | npm installs                       | accept      | nenhum pacote novo (date-fns ja instalado)                                                  |

</threat_model>

<verification>
- npm test -- --run src/_actions/appointments.test.ts src/_actions/_audit.test.ts verde
- npx tsc --noEmit sem erros novos
- EditAppointmentDrawer compila e exporta named
</verification>

<success_criteria>

- updateAppointmentAction persiste todos os campos com revalidatePath
- useAppointmentForm opera em modo create e edit
- EditAppointmentDrawer pre-popula via AppointmentRow sem query extra
  </success_criteria>

<output>
Create `.planning/phases/09-appointments/09-02-SUMMARY.md` when done
</output>
