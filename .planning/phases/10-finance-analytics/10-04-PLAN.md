---
phase: 10-finance-analytics
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/app/(dashboard)/analytics/page.tsx
  - src/app/(dashboard)/analytics/_components/AnalyticsClient.tsx
  - src/app/(dashboard)/finance/reports/page.tsx
autonomous: true
requirements: [FIN-01, FIN-02, FIN-03, FIN-04]

must_haves:
  truths:
    - "AnalyticsClient renderiza 'N/D' quando avgTicket, netMargin, nps ou returnRate são null"
    - "Nenhum KPI no dashboard exibe 'null%', 'nullR$', 'NaN' ou '-1' para o operador"
    - "reports/page.tsx usa getCategoryReport(days) em vez de listTransactions(500) + buildCategoryRows()"
    - "reports/page.tsx tem filtro ?periodo=mensal|trimestral|anual com padrão mensal"
    - "Days derivado de getPeriodDays() antes de entrar em getCategoryReport — nunca de searchParam direto"
    - "D-09: getCategoryReport(days) SQL GROUP BY substitui listTransactions(500) + buildCategoryRows() em reports/page.tsx"
    - "D-10: filtro ?periodo=mensal|trimestral|anual em reports/page.tsx com padrão mensal"
  artifacts:
    - path: "src/app/(dashboard)/analytics/_components/AnalyticsClient.tsx"
      provides: "KPI grid com null-guards para N/D"
      contains: "N/D"
    - path: "src/app/(dashboard)/finance/reports/page.tsx"
      provides: "Página de relatórios com período filter e SQL GROUP BY"
      contains: "getCategoryReport"
  key_links:
    - from: "src/app/(dashboard)/analytics/page.tsx"
      to: "AnalyticsClient"
      via: "prop kpis: AnalyticsKpis (com campos null)"
      pattern: "AnalyticsKpis"
    - from: "src/app/(dashboard)/finance/reports/page.tsx"
      to: "src/_data-access/finance.ts"
      via: "getCategoryReport(days)"
      pattern: "getCategoryReport"
    - from: "src/app/(dashboard)/finance/reports/page.tsx"
      to: "searchParams.periodo"
      via: "getPeriodDays(activePeriod)"
      pattern: "getPeriodDays"
---

<objective>
Corrigir o dashboard analytics para não exibir valores sentinel (null%, NaN, -1) e otimizar a página de relatórios financeiros: substituir GROUP BY em JS por SQL, adicionar filtro de período.

Purpose: FIN-02 e FIN-03 estão bloqueados até que AnalyticsClient trate null corretamente. FIN-01 precisa do getCategoryReport wired na reports page. Esses fixes são independentes do CRUD de transações (10-03) e podem rodar em paralelo na Wave 2.
Output: analytics/page.tsx + AnalyticsClient.tsx corrigidos; reports/page.tsx com getCategoryReport e ?periodo filter
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-finance-analytics/10-CONTEXT.md
@.planning/phases/10-finance-analytics/10-RESEARCH.md
@.planning/phases/10-finance-analytics/10-PATTERNS.md
@.planning/phases/10-finance-analytics/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Null-guards em AnalyticsClient e atualizar tipos em analytics/page.tsx</name>
  <files>
    src/app/(dashboard)/analytics/_components/AnalyticsClient.tsx,
    src/app/(dashboard)/analytics/page.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/analytics/_components/AnalyticsClient.tsx — ler completo para localizar todas as ocorrências onde avgTicket, netMargin, nps, returnRate são usados em template strings ou como valores de KPI cards (linhas ~48–78 conforme RESEARCH.md); verificar o tipo Props.kpis atual
    - src/app/(dashboard)/analytics/page.tsx — verificar como kpis é desestruturado ou passado para AnalyticsClient; verificar se há uso direto dos campos null fora do AnalyticsClient
    - src/_data-access/analytics.ts (modificado em 10-01) — confirmar o tipo atualizado AnalyticsKpis com os 4 campos number | null
  </read_first>
  <behavior>
    - Props.kpis em AnalyticsClient deve usar o tipo AnalyticsKpis importado de @/_data-access/analytics (o TypeScript apontará os locais de erro após a mudança de tipo)
    - avgTicket !== null ? formatCurrency(avgTicket) : "N/D" nos locais de value e sub text
    - netMargin !== null ? `${netMargin}%` : "N/D"
    - nps !== null ? nps.toString() : "N/D"
    - returnRate !== null ? `${returnRate}% taxa de retorno` : "Taxa de retorno N/D"
    - KPIs de contagem (totalOrders12m, activeCustomers, newCustomers) mantêm comportamento atual — não são null
    - analytics/page.tsx: nenhuma mudança de lógica — apenas verificar que a tipagem está correta após 10-01
  </behavior>
  <action>
    Em src/app/(dashboard)/analytics/_components/AnalyticsClient.tsx:
    - Importar AnalyticsKpis de @/_data-access/analytics (se não importado já) e usar como tipo do campo kpis em Props
    - Localizar todas as 4 ocorrências onde avgTicket, netMargin, nps, returnRate são usados como valores de KPI cards
    - Aplicar null-guards conforme D-07 e D-08:
      * avgTicket: null → value ou sub text usa "N/D"; não-null → formatCurrency(avgTicket)
      * netMargin: null → "N/D"; não-null → `${netMargin}%`
      * nps: null → "N/D"; não-null → nps.toString()
      * returnRate: null → "Taxa de retorno N/D" (no sub text); não-null → `${returnRate}% taxa de retorno`
    - Não remover nenhum KPI card — apenas mudar os valores exibidos para null

    Em src/app/(dashboard)/analytics/page.tsx:
    - Verificar se o TypeScript indica erros após a mudança de tipo em analytics.ts (10-01)
    - Se kpis é desestruturado com tipos específicos, atualizar a tipagem para aceitar os campos null
    - Não mudar a lógica de fetch — apenas corrigir tipos se necessário

  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && npx tsc --noEmit 2>&1 | grep -E "AnalyticsClient|analytics/page" | head -20</automated>
  </verify>
  <done>
    - grep -c "N/D" src/app/(dashboard)/analytics/_components/AnalyticsClient.tsx retorna >= 4 (uma por KPI null)
    - tsc --noEmit sem erros em AnalyticsClient.tsx e analytics/page.tsx
    - Nenhuma ocorrência de `${analyticsKpis.avgTicket}` ou `.nps}` sem guard (grep verifica ausência de padrão direto sem ternário)
  </done>
</task>

<task type="auto">
  <name>Task 2: Wiring getCategoryReport e filtro ?periodo em reports/page.tsx</name>
  <files>src/app/(dashboard)/finance/reports/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/finance/reports/page.tsx — ler completo: localizar listTransactions(500) e buildCategoryRows() para substituição; localizar Promise.all e estrutura atual de queries; verificar tipo CategoryRow atual e DataTable de categorias; verificar se há filtro de período existente ou se está hardcoded
    - src/app/(dashboard)/finance/page.tsx — padrão de getPeriodDays(), Period type, PERIODS array e links de filtro (copiar verbatim ajustando href para /finance/reports)
    - src/_data-access/finance.ts (modificado em 10-01) — confirmar assinatura getCategoryReport(days): Promise&lt;CategoryReport[]&gt; e tipo CategoryReport com campos category, grossRevenue, totalExpenses, netProfit, status
  </read_first>
  <action>
    Em src/app/(dashboard)/finance/reports/page.tsx:

    1. Adicionar type Period, getPeriodDays(), PERIODS array — copiar de finance/page.tsx verbatim (ajustar href dos links para "/finance/reports?periodo=...")

    2. Atualizar Props para incluir searchParams: Promise&lt;{ periodo?: string }&gt;; fazer await searchParams; derivar activePeriod e days via getPeriodDays()

    3. Atualizar o import de @/_data-access/finance: adicionar getCategoryReport; remover listTransactions se não usado em outro lugar da página (verificar outros usos antes de remover)

    4. Substituir no Promise.all: remover listTransactions(500) da lista; adicionar getCategoryReport(days); o resultado passa a ser categoryRows diretamente (já tipado como CategoryReport[])

    5. Remover a chamada buildCategoryRows(allTransactions) após o Promise.all; remover a variável allTransactions

    6. Adicionar id sintético para DataTable: const categoryRowsWithId = categoryRows.map((r, i) => ({ ...r, id: String(i) })) — somente se DataTable exige getRowId e CategoryReport não tem id

    7. Adicionar os links de período antes da tabela de categorias, seguindo o padrão visual de finance/page.tsx (PERIODS.map com Link href para /finance/reports?periodo=)

    8. Garantir que days é passado também para getFinanceMetrics(days) e outros queries que aceitam o parâmetro — verificar assinaturas

    Manter todos os outros blocos (gráficos, métricas, outras seções) intactos. Apenas a seção de categorias e o filtro de período são alterados.

  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && npx tsc --noEmit 2>&1 | grep -E "reports/page" | head -20</automated>
  </verify>
  <done>
    - grep -c "getCategoryReport" src/app/(dashboard)/finance/reports/page.tsx retorna >= 2 (import + chamada)
    - grep -c "buildCategoryRows\|listTransactions(500)" src/app/(dashboard)/finance/reports/page.tsx retorna 0 (removido)
    - grep -c "periodo" src/app/(dashboard)/finance/reports/page.tsx retorna >= 3 (searchParams + Period type + links)
    - grep -c "getPeriodDays" src/app/(dashboard)/finance/reports/page.tsx retorna >= 1
    - tsc --noEmit sem erros em reports/page.tsx
    - npm run build sem erros de SSR em finance/reports/*
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                               |
| ---------------------------------------- | --------------------------------------------------------- |
| searchParams.periodo → getCategoryReport | searchParam de URL não confiável entra como `days` no SQL |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                       | Disposition | Mitigation Plan                                                                                                                                   |
| ---------- | ---------------------- | --------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-10-04-01 | Tampering              | sql.raw(String(days)) em getCategoryReport via reports/page.tsx | mitigate    | reports/page.tsx usa getPeriodDays(activePeriod) onde activePeriod é enum Period com 3 valores fixos; searchParam arbitrário não chega ao sql.raw |
| T-10-04-02 | Information Disclosure | AnalyticsClient exibe null como "N/D"                           | accept      | "N/D" é info-neutral — não expõe dados internos nem confunde o operador                                                                           |

</threat_model>

<verification>
- tsc --noEmit limpo para os 3 arquivos
- grep confirma: getCategoryReport em reports/page.tsx, N/D em AnalyticsClient, getPeriodDays em reports/page.tsx
- grep confirma ausência: buildCategoryRows, listTransactions(500) em reports/page.tsx
- npm run build sem erros em analytics/* e finance/reports/*
</verification>

<success_criteria>

- Dashboard analytics em /analytics não exibe "-1%", "null%", "NaN" ou "R$ 0,00 ticket" — exibe "N/D" quando sem dados
- /finance/reports tem filtro mensal/trimestral/anual funcional
- Categorias em reports vêm do SQL GROUP BY, não de listTransactions(500) + buildCategoryRows()
- tsc --noEmit e npm run build limpos
  </success_criteria>

<output>
Criar `.planning/phases/10-finance-analytics/10-04-SUMMARY.md` quando concluído
</output>
