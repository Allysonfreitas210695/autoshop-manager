---
phase: 10-finance-analytics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed.ts
autonomous: true
requirements: [FIN-01]

must_haves:
  truths:
    - "scripts/seed.ts cria transações suficientes para preencher 6 meses de dados nos gráficos getMonthlyCashFlow"
    - "O seed pode ser executado duas vezes sem duplicação (wipe() deleta transactions antes de re-inserir)"
    - "Transações de receita têm serviceOrderId vinculado a O.S. existentes; despesas têm serviceOrderId null"
    - "Valores estão dentro do range R$200–R$2500 receita e R$100–R$800 despesas (per D-12)"
  artifacts:
    - path: "scripts/seed.ts"
      provides: "seed com ~30+ transações cobrindo 6 meses"
      contains: "transactions"
  key_links:
    - from: "scripts/seed.ts"
      to: "transactions table"
      via: "db.insert(transactions) após loop de service orders"
      pattern: "db\\.insert.*transactions"
---

<objective>
Verificar e ajustar o seed de transações para garantir que getMonthlyCashFlow retorne dados não-zerados para os últimos 6 meses. O seed já tem implementação parcial — esta task audita o volume e as categorias, corrigindo gaps sem reescrever do zero.

Purpose: Analytics e finance charts dependem de dados de seed para serem testáveis em desenvolvimento. Sem seed adequado, FIN-01 não pode ser verificado (os gráficos ficam vazios mesmo com queries corretas).
Output: scripts/seed.ts com seção de transações verificada e ajustada conforme D-11/D-12
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-finance-analytics/10-CONTEXT.md
@.planning/phases/10-finance-analytics/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar e ajustar seed de transações</name>
  <files>scripts/seed.ts</files>
  <read_first>
    - scripts/seed.ts — ler a seção completa de transações (linhas ~484–538 conforme RESEARCH.md) para entender o volume atual, as categorias usadas, e como os IDs de service orders são referenciados; verificar também a função wipe() para confirmar que transactions está incluída
    - src/_db/schema/transactions.ts — confirmar tipos exatos dos campos type, status, category (text livre), serviceOrderId para garantir que o seed insere valores compatíveis
  </read_first>
  <action>
    Ler scripts/seed.ts completo para auditar:
    1. Volume de transações criadas (alvo: ≥30 registros abrangendo os últimos 6 meses conforme D-11)
    2. Distribuição temporal: confirmar que as datas cobrem os 6 meses anteriores à data atual (usar new Date() relativo, não datas hardcoded)
    3. Mix de categorias: receitas vinculadas a O.S. com serviceOrderId preenchido; despesas com categorias "Despesa Fixa", "Fornecedor", "Mão de Obra", "Peças", etc. e serviceOrderId null
    4. Valores: receitas R$200–R$2500, despesas R$100–R$800 (per D-12)
    5. wipe(): confirmar que await db.delete(transactions) existe e está em posição correta (antes do re-insert)

    Se o volume já estiver adequado (≥30 transações, 6 meses cobridos, mix de categorias): apenas documentar no SUMMARY que o seed está OK.

    Se houver gaps (datas hardcoded, volume insuficiente, categorias uniformes demais, wipe() ausente): fazer ajustes cirúrgicos:
    - Substituir datas hardcoded por expressões relativas: new Date(Date.now() - N * 30 * 24 * 60 * 60 * 1000)
    - Adicionar mais entradas se volume < 30
    - Garantir mix mínimo: pelo menos 3 categorias distintas em despesas
    - Garantir que wipe() inclui transactions na ordem correta de deleção (sem FK violations)

    Não reescrever partes do seed não relacionadas a transactions.

  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && npx tsc --noEmit scripts/seed.ts 2>&1 | head -20 || npx tsc --noEmit 2>&1 | grep "seed" | head -10</automated>
  </verify>
  <done>
    - scripts/seed.ts compila sem erros de tipo
    - Seção de transactions cria ≥30 registros (grep -c "type:.*income\|type:.*expense" scripts/seed.ts retorna >= 10 como proxy)
    - wipe() contém db.delete(transactions) (grep -c "delete.*transactions\|transactions.*delete" scripts/seed.ts retorna >= 1)
    - Datas usam expressões relativas a Date.now() ou new Date() — não hardcoded em 2024/2025
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary         | Description                                                           |
| ---------------- | --------------------------------------------------------------------- |
| seed script → DB | Script de desenvolvimento executa mutations diretas sem validação Zod |

## STRIDE Threat Register

| Threat ID | Category  | Component                  | Disposition | Mitigation Plan                                                                                    |
| --------- | --------- | -------------------------- | ----------- | -------------------------------------------------------------------------------------------------- |
| T-10-S1   | Tampering | scripts/seed.ts wipe()     | accept      | Script de desenvolvimento apenas — nunca exposto em produção; wipe() intencional para idempotência |
| T-10-S2   | Tampering | amount como string no seed | accept      | Seed usa valores literais numéricos controlados pelo desenvolvedor; não há input externo           |

</threat_model>

<verification>
- npm run db:seed executa sem erros (requer DB conectado — verificação manual em ambiente de dev)
- tsc --noEmit limpo para scripts/seed.ts
- Seção de transactions presente após wipe() e após loop de service orders
</verification>

<success_criteria>

- scripts/seed.ts audited: volume adequado (≥30 transações), datas cobrindo últimos 6 meses, mix de categorias (income/expense, categorias variadas), wipe() idempotente
- Qualquer gap corrigido cirurgicamente sem reescrever seções não relacionadas
  </success_criteria>

<output>
Criar `.planning/phases/10-finance-analytics/10-02-SUMMARY.md` quando concluído
</output>
