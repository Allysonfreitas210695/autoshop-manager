---
phase: 10-finance-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/_actions/finance.ts
  - src/_data-access/finance.ts
  - src/_data-access/analytics.ts
autonomous: true
requirements: [FIN-01, FIN-02, FIN-03, FIN-04]

must_haves:
  truths:
    - "createTransactionAction, updateTransactionAction, deleteTransactionAction existem em src/_actions/finance.ts e passam pela validação Zod"
    - "getCategoryReport(days) existe em src/_data-access/finance.ts e retorna CategoryReport[] com campos number (não string)"
    - "AnalyticsKpis tem avgTicket/netMargin/nps/returnRate tipados como number | null"
    - "getAnalyticsKpis retorna null (não 0) para os 4 KPIs derivados quando o denominador é zero"
    - "amount é persistido como String(parsedInput.amount) em create e update actions"
    - "D-04: categoria é z.string().min(1) sem enum — campo texto livre na transactionSchema"
    - "D-06: getAnalyticsKpis retorna null para avgTicket/netMargin/nps/returnRate quando denominador é zero"
    - "getAnalyticsKpis usa queries batch — ausência de N+1 confirmada por grep: nenhuma ocorrência de db.select/db.query dentro de for/map em analytics.ts"
  artifacts:
    - path: "src/_actions/finance.ts"
      provides: "CRUD server actions para transações"
      exports:
        [
          "createTransactionAction",
          "updateTransactionAction",
          "deleteTransactionAction",
        ]
    - path: "src/_data-access/finance.ts"
      provides: "getCategoryReport SQL GROUP BY"
      exports: ["getCategoryReport", "CategoryReport"]
    - path: "src/_data-access/analytics.ts"
      provides: "AnalyticsKpis com campos null para KPIs derivados"
      exports: ["AnalyticsKpis", "getAnalyticsKpis"]
  key_links:
    - from: "src/_actions/finance.ts"
      to: "transactions table"
      via: "db.insert/update/delete com authActionClient"
      pattern: "authActionClient\\.schema"
    - from: "src/_data-access/finance.ts"
      to: "transactions table"
      via: "getCategoryReport SQL GROUP BY category"
      pattern: "groupBy.*category"
    - from: "src/_data-access/analytics.ts"
      to: "AnalyticsKpis return"
      via: "null coalesce para denominador zero"
      pattern: "avgTicket.*null"
---

<objective>
Criar a camada de dados e server actions para o módulo Finance & Analytics: (1) três server actions CRUD para transações, (2) nova função getCategoryReport com SQL GROUP BY substituindo processamento JS in-memory, (3) atualizar AnalyticsKpis para retornar null em KPIs derivados sem dados.

Purpose: Estabelece o contrato de tipos e as mutations que os planos de UI (10-02 e 10-03) dependem. Sem esses artefatos, os drawers e o dashboard analytics não têm actions para chamar nem tipos corretos para consumir.
Output: src/\_actions/finance.ts (novo), src/\_data-access/finance.ts (getCategoryReport adicionada), src/\_data-access/analytics.ts (tipos null + retorno corrigido)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-finance-analytics/10-CONTEXT.md
@.planning/phases/10-finance-analytics/10-RESEARCH.md
@.planning/phases/10-finance-analytics/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar src/_actions/finance.ts com três server actions CRUD</name>
  <files>src/_actions/finance.ts</files>
  <read_first>
    - src/_actions/orders.ts — padrão canônico de authActionClient + Zod schema + db.insert/update/delete + revalidatePath
    - src/_lib/safe-action.ts — exportação de authActionClient
    - src/_db/schema/transactions.ts — colunas disponíveis: id, date, description, category, type (enum income/expense), amount (numeric 12,2), status (enum paid/pending/overdue), serviceOrderId (FK nullable), createdAt, updatedAt
  </read_first>
  <action>
    Criar src/_actions/finance.ts (arquivo novo — não existe ainda).

    Imports obrigatórios: "use server"; eq from "drizzle-orm"; revalidatePath from "next/cache"; z from "zod"; db from "@/_db"; transactions from "@/_db/schema"; authActionClient from "@/_lib/safe-action".

    Zod schema (inline em cada action, sem .default()): type: z.enum(["income", "expense"]), amount: z.coerce.number().positive("Valor deve ser maior que zero"), date: z.string().min(1, "Informe a data"), description: z.string().min(1), category: z.string().min(1), status: z.enum(["paid", "pending", "overdue"]).

    createTransactionAction: db.insert(transactions).values({ type, amount: String(parsedInput.amount), date: new Date(parsedInput.date), description, category, status }). Revalidar "/finance" e "/analytics".

    updateTransactionAction: adicionar id: z.uuid() ao schema. db.update(transactions).set({ ...campos, updatedAt: new Date() }).where(eq(transactions.id, parsedInput.id)). Mesmas revalidações.

    deleteTransactionAction: schema só com id: z.uuid(). db.delete(transactions).where(eq(transactions.id, parsedInput.id)). Mesmas revalidações.

    Verificar padrão exato de authActionClient em orders.ts (especialmente se usa .use() chain ou .schema() diretamente) antes de escrever o arquivo.

  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && npx tsc --noEmit 2>&1 | grep -E "finance\.ts|_actions" | head -20</automated>
  </verify>
  <done>
    - src/_actions/finance.ts existe com exports: createTransactionAction, updateTransactionAction, deleteTransactionAction
    - tsc --noEmit sem erros nos arquivos de actions
    - amount usa String(parsedInput.amount) em create e update (grep confirma: grep -c "String(parsedInput.amount)" src/_actions/finance.ts retorna >= 2)
    - Todos usam authActionClient (grep confirma: grep -c "authActionClient" src/_actions/finance.ts retorna 3)
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar getCategoryReport em finance.ts e corrigir null-guards em analytics.ts</name>
  <files>src/_data-access/finance.ts, src/_data-access/analytics.ts</files>
  <read_first>
    - src/_data-access/finance.ts — padrão sql.raw(String(days)) (linha ~71), padrão Number(r.amount) na camada de retorno, estrutura atual de exports e tipos
    - src/_data-access/analytics.ts — tipo atual AnalyticsKpis (linhas ~45–54), lógica atual de getAnalyticsKpis (buscar linhas com avgTicket/netMargin/nps/returnRate para editar)
    - src/_db/schema/transactions.ts — confirmar nomes exatos das colunas usadas no sql template
  </read_first>
  <action>
    Em src/_data-access/finance.ts: adicionar export type CategoryReport = { category: string; grossRevenue: number; totalExpenses: number; netProfit: number; status: "positive" | "neutral" | "negative" } e export async function getCategoryReport(days = 30): Promise&lt;CategoryReport[]&gt;.

    A query usa db.select com sql&lt;number&gt; para grossRevenue (CASE WHEN type='income' AND status='paid') e totalExpenses (CASE WHEN type='expense' AND status='paid'), .from(transactions), .where(sql`date >= current_date - interval '${sql.raw(String(days))} days'`), .groupBy(transactions.category), .orderBy por sum de income desc.

    No map de retorno: grossRevenue = Number(r.grossRevenue), totalExpenses = Number(r.totalExpenses), netProfit = grossRevenue - totalExpenses, status derivado do netProfit.

    Em src/_data-access/analytics.ts: (1) atualizar export type AnalyticsKpis para avgTicket: number | null, netMargin: number | null, nps: number | null, returnRate: number | null. (2) No retorno de getAnalyticsKpis, substituir as 4 expressões ternárias que retornam 0 para retornar null: avgTicket: totalOrders > 0 ? revenue / totalOrders : null, netMargin: revenue > 0 ? Math.round(...) : null, nps: totalFb > 0 ? Math.round(...) : null, returnRate: totalReturningBase > 0 ? Math.round(...) : null.

    Manter todos os outros campos e comportamentos de analytics.ts inalterados.

    FIN-03 audit (per D-06 / N+1 elimination): após editar analytics.ts, verificar que getAnalyticsKpis não contém nenhuma chamada db.select, db.query, db.execute, ou equivalente dentro de um loop for, forEach, map, ou reduce. Usar grep para confirmar: grep -n "for\|forEach\|\.map\|\.reduce" src/_data-access/analytics.ts — inspecionar cada ocorrência e garantir que não há query aninhada.

  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && npx tsc --noEmit 2>&1 | grep -E "finance\.ts|analytics\.ts" | head -30</automated>
  </verify>
  <done>
    - getCategoryReport exportada em finance.ts (grep -c "export.*getCategoryReport" src/_data-access/finance.ts retorna 1)
    - CategoryReport exportada com campo status string literal (grep -c "export type CategoryReport" src/_data-access/finance.ts retorna 1)
    - Number() aplicado a grossRevenue e totalExpenses no map de retorno (grep -c "Number(r\." src/_data-access/finance.ts retorna >= 3 incluindo os campos existentes)
    - AnalyticsKpis tem 4 campos null (grep -c "number | null" src/_data-access/analytics.ts retorna 4)
    - FIN-03 audit passou: grep -v "^\s*//" src/_data-access/analytics.ts | grep -c "db\." retorna 0 dentro de qualquer bloco de loop — confirmar ausência de N+1 por inspeção das ocorrências de for/map
    - tsc --noEmit limpo para ambos os arquivos
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                                                 |
| ----------------------- | ----------------------------------------------------------------------------------------------------------- |
| Browser → Server Action | Input de formulário não-confiável atravessa a fronteira via createTransactionAction/updateTransactionAction |
| searchParams → SQL      | `days` derivado de `?periodo=` searchParam antes de entrar em sql.raw                                       |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                   | Disposition | Mitigation Plan                                                                                                 |
| --------- | ---------------------- | --------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------- |
| T-10-01   | Elevation of Privilege | createTransactionAction / updateTransactionAction / deleteTransactionAction | mitigate    | authActionClient valida sessão antes de qualquer mutation — sem sessão → ActionError lançado                    |
| T-10-02   | Tampering              | Zod schema em finance.ts actions                                            | mitigate    | amount: z.coerce.number().positive() rejeita negativos e zero; type e status com z.enum limitam valores aceitos |
| T-10-03   | Tampering              | sql.raw(String(days)) em getCategoryReport                                  | mitigate    | days sempre derivado de getPeriodDays() que retorna exclusivamente 30, 90 ou 365 — nunca de user input direto   |
| T-10-04   | Information Disclosure | getAnalyticsKpis retornando dados financeiros                               | accept      | qualquer usuário autenticado pode ver métricas agregadas — sem PII, sem dado individual exposto                 |

</threat_model>

<verification>
- tsc --noEmit limpo no projeto completo após as duas tasks
- grep -c "authActionClient" src/_actions/finance.ts == 3
- grep "String(parsedInput.amount)" src/_actions/finance.ts retorna 2 linhas (create + update)
- grep "number | null" src/_data-access/analytics.ts retorna 4 ocorrências
- grep "getCategoryReport" src/_data-access/finance.ts retorna export + function declaration
- FIN-03: grep -n "for\|forEach\|\.map\|\.reduce" src/_data-access/analytics.ts — inspecionar manualmente que nenhuma ocorrência contém db.select aninhado
</verification>

<success_criteria>

- src/\_actions/finance.ts existe com createTransactionAction, updateTransactionAction, deleteTransactionAction usando authActionClient
- src/\_data-access/finance.ts exporta getCategoryReport(days) retornando CategoryReport[] com number (não string)
- src/\_data-access/analytics.ts: AnalyticsKpis tem 4 campos number | null; getAnalyticsKpis retorna null (não 0) quando denominador é zero
- tsc --noEmit sem erros nos três arquivos
  </success_criteria>

<output>
Criar `.planning/phases/10-finance-analytics/10-01-SUMMARY.md` quando concluído
</output>
