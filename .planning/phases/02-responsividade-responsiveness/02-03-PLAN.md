---
phase: 02-responsividade-responsiveness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/orders/new/step-indicator.tsx
  - src/app/(dashboard)/finance/page.tsx
  - src/app/(dashboard)/finance/reports/page.tsx
autonomous: true
requirements: [RESP-03]
must_haves:
  truths:
    - "On a 320-375px viewport all 5 wizard steps stay visible and the step row scrolls horizontally instead of compressing"
    - "The finance dashboard cash-flow bar chart scrolls horizontally on narrow viewports instead of collapsing"
    - "The finance reports monthly line chart and cost donut chart scroll horizontally on narrow viewports instead of collapsing"
  artifacts:
    - path: "src/app/(dashboard)/orders/new/step-indicator.tsx"
      provides: "Horizontal-scroll step row with min-width step items"
      contains: "overflow-x-auto"
    - path: "src/app/(dashboard)/finance/page.tsx"
      provides: "Scroll wrapper around CashFlowBarChart"
      contains: "min-w-[300px]"
    - path: "src/app/(dashboard)/finance/reports/page.tsx"
      provides: "Scroll wrappers around MonthlyLineChart and CostDonutChart"
      contains: "min-w-[360px]"
  key_links:
    - from: "src/app/(dashboard)/orders/new/step-indicator.tsx"
      to: "step item divs"
      via: "min-w + shrink-0 prevents compression inside overflow-x-auto"
      pattern: "min-w-\\[56px\\] shrink-0"
---

<objective>
Make the order-wizard step indicator and the finance charts reflow on mobile via horizontal scroll (RESP-03). Per D-07, the 5-step indicator scrolls horizontally so every step stays visible on 320px screens; per D-09, recharts charts (which use `width="100%"`) get an `overflow-x-auto` wrapper plus a `min-w-*` inner div so they scroll rather than compress.

Purpose: On narrow viewports the step circles compress (connector lines collapse) and the recharts `ResponsiveContainer width="100%"` charts shrink below readability. Both are fixed with pure Tailwind wrapper classes — no chart API or component logic changes.
Output: Three modified files; scrollable step indicator and scrollable finance charts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-responsividade-responsiveness/02-CONTEXT.md
@.planning/phases/02-responsividade-responsiveness/02-RESEARCH.md
@.planning/phases/02-responsividade-responsiveness/02-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert step indicator to horizontal scroll</name>
  <files>src/app/(dashboard)/orders/new/step-indicator.tsx</files>
  <read_first>
    - src/app/(dashboard)/orders/new/step-indicator.tsx (read the full file — outer flex row at line 25, step item div at line 31, connector div at lines 57-65, progress bar at lines 71-78)
    - src/app/(dashboard)/finance/page.tsx (lines 139-152 — the established hidden-scrollbar horizontal scroll row pattern to mirror)
    - .planning/phases/02-responsividade-responsiveness/02-PATTERNS.md (step-indicator.tsx section, lines 173-212)
  </read_first>
  <action>
    Two edits, per D-07:

    1. Outer step row (line 25): change `<div className="flex items-center justify-between">` to `<div className="flex items-center overflow-x-auto pb-1 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden">`. This removes `justify-between` (which forces compression) and adds horizontal scroll with a hidden scrollbar, mirroring the established filter-row pattern in finance/page.tsx.

    2. Step item wrapper (line 31): change `<div key={step.number} className="flex flex-1 items-center">` to `<div key={step.number} className="flex min-w-[56px] shrink-0 items-center">`. Replacing `flex-1` with `min-w-[56px] shrink-0` gives each step a fixed minimum width and prevents flex from shrinking it; 5 x 56px = 280px fits within a 320px viewport so auto-scroll-into-view is not required.

    CRITICAL — do NOT change the connector line div (lines 57-65): the `mx-2 h-px flex-1 ...` connector MUST keep its `flex-1` so it stretches between the fixed-width step items (per the PATTERNS.md note). Do NOT change the progress bar (lines 71-78) or the "Preenchimento" percentage text — they sit below the scroll row and are unaffected.

  </action>
  <verify>
    <automated>grep -q 'overflow-x-auto pb-1 \[scrollbar-width:none\]' "src/app/(dashboard)/orders/new/step-indicator.tsx" && grep -q 'min-w-\[56px\] shrink-0 items-center' "src/app/(dashboard)/orders/new/step-indicator.tsx" && grep -q 'h-px flex-1' "src/app/(dashboard)/orders/new/step-indicator.tsx" && ! grep -q 'justify-between' "src/app/(dashboard)/orders/new/step-indicator.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the outer step row className contains `overflow-x-auto pb-1 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden` and no longer contains `justify-between`.
    - Source assertion: the step item div className contains `min-w-[56px] shrink-0 items-center` and no longer contains `flex-1 items-center` on that div.
    - Source assertion: the connector div STILL contains `h-px flex-1` (unchanged).
    - Behavior assertion: at 320-375px all 5 steps remain visible and the row scrolls horizontally (verified in plan 04 RESP-05 pass).
  </acceptance_criteria>
  <done>The step row scrolls horizontally with fixed-min-width steps; connector lines and progress bar are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap CashFlowBarChart in a horizontal-scroll container</name>
  <files>src/app/(dashboard)/finance/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/finance/page.tsx (read around lines 237-239 — the `<div className="p-4"><CashFlowBarChart data={cashFlow} /></div>` wrapper)
    - .planning/phases/02-responsividade-responsiveness/02-PATTERNS.md (finance/page.tsx section, lines 322-344)
  </read_first>
  <action>
    Locate the `<CashFlowBarChart data={cashFlow} />` usage (currently inside `<div className="p-4">` at lines 237-239). Replace that wrapper structure with a two-level wrapper per D-09:
    - Outer div: `className="overflow-x-auto p-4"` (keeps the existing `p-4` padding, adds horizontal scroll).
    - Inner div: `className="min-w-[300px]"` (the bar chart has ~6 bars, so 300px is the readability floor per RESEARCH.md Open Question 2).
    - `<CashFlowBarChart data={cashFlow} />` stays inside the inner div, unchanged.

    Do NOT change the recharts `ResponsiveContainer width="100%"` inside the chart component — the wrapper handles scroll while the chart keeps filling its (now min-width-bounded) container. Do NOT touch the filter-tabs row at lines 139-152; it already uses the correct horizontal-scroll pattern.

  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/(dashboard)/finance/page.tsx','utf8'); const i=s.indexOf('CashFlowBarChart data='); const seg=s.slice(Math.max(0,i-160),i); process.exit((seg.includes('overflow-x-auto p-4')&&seg.includes('min-w-[300px]'))?0:1)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: within ~160 chars before `<CashFlowBarChart data=` the source contains both `overflow-x-auto p-4` and `min-w-[300px]`.
    - Source assertion: the finance/page.tsx filter row at lines ~139-152 still contains `overflow-x-auto` with `[scrollbar-width:none]` (untouched).
    - Behavior assertion: at 375px the cash-flow bar chart scrolls horizontally rather than collapsing (verified in plan 04 RESP-05 pass).
  </acceptance_criteria>
  <done>CashFlowBarChart is wrapped in `overflow-x-auto p-4` outer + `min-w-[300px]` inner div.</done>
</task>

<task type="auto">
  <name>Task 3: Wrap reports charts in horizontal-scroll containers</name>
  <files>src/app/(dashboard)/finance/reports/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/finance/reports/page.tsx (read around lines 215-217 for `MonthlyLineChart` and line 224 for `CostDonutChart`)
    - src/app/(dashboard)/finance/page.tsx (the Task 2 wrapper, as the sister-file analog)
    - .planning/phases/02-responsividade-responsiveness/02-PATTERNS.md (finance/reports/page.tsx section, lines 348-384)
  </read_first>
  <action>
    Two chart wrappers, per D-09:

    1. MonthlyLineChart (currently `<div className="p-4"><MonthlyLineChart data={monthlyCashFlow} /></div>` at lines 215-217): replace with outer `<div className="overflow-x-auto p-4">` + inner `<div className="min-w-[360px]">` containing `<MonthlyLineChart data={monthlyCashFlow} />`. The line chart has 6 months x 3 series (denser), so `min-w-[360px]` is the floor.

    2. CostDonutChart (currently `<CostDonutChart data={costBreakdown} />` at line 224, inside a `Card className="p-4"`): wrap it in outer `<div className="overflow-x-auto">` + inner `<div className="min-w-[280px]">`. The donut is radial and scales down gracefully, so `min-w-[280px]` is a conservative floor and no extra `p-4` is added (the parent Card already pads).

    Do NOT change the recharts `ResponsiveContainer width="100%"` inside either chart component. Do NOT alter the category table (already has correct `hidden sm:table-cell` / `hidden md:table-cell` column hiding) or the KPI grid.

  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/(dashboard)/finance/reports/page.tsx','utf8'); const l=s.indexOf('MonthlyLineChart data='); const ls=s.slice(Math.max(0,l-160),l); const d=s.indexOf('CostDonutChart data='); const ds=s.slice(Math.max(0,d-160),d); process.exit((ls.includes('overflow-x-auto p-4')&&ls.includes('min-w-[360px]')&&ds.includes('overflow-x-auto')&&ds.includes('min-w-[280px]'))?0:1)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: before `<MonthlyLineChart data=` the source contains `overflow-x-auto p-4` and `min-w-[360px]`.
    - Source assertion: before `<CostDonutChart data=` the source contains `overflow-x-auto` and `min-w-[280px]`.
    - Behavior assertion: at 375px both reports charts scroll horizontally rather than collapsing (verified in plan 04 RESP-05 pass).
  </acceptance_criteria>
  <done>MonthlyLineChart wrapped with `min-w-[360px]` and CostDonutChart wrapped with `min-w-[280px]`, both inside `overflow-x-auto`.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                                                                               |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none)   | This plan adds Tailwind wrapper classes around existing chart/step components. No new input surfaces, no auth/session changes, no server actions touched. |

## STRIDE Threat Register

| Threat ID | Category | Component                    | Disposition | Mitigation Plan                                                                                                       |
| --------- | -------- | ---------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------- |
| T-02-03   | (n/a)    | Chart + step scroll wrappers | accept      | ASVS L1 not applicable — no security-relevant change. No new dependencies, so no supply-chain (T-{phase}-SC) surface. |

ASVS L1 is not applicable to this CSS-only responsive polish plan (per phase Security Domain).
</threat_model>

<verification>
- `grep`/`node` source assertions in each task pass.
- Final gate runs in plan 04 (RESP-05): `npx tsc --noEmit && npm run lint && npm run build`.
</verification>

<success_criteria>

- Step indicator scrolls horizontally with fixed-min-width steps; connector lines unchanged.
- CashFlowBarChart, MonthlyLineChart, and CostDonutChart each sit inside an `overflow-x-auto` + `min-w-*` wrapper.
- No recharts `ResponsiveContainer` API changed; no table/grid markup altered.
  </success_criteria>

<output>
Create `.planning/phases/02-responsividade-responsiveness/02-03-SUMMARY.md` when done.
</output>
