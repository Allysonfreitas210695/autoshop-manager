---
phase: 02-responsividade-responsiveness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/orders/_components/OrdersClient.tsx
  - src/app/(dashboard)/page.tsx
autonomous: true
requirements: [RESP-02, RESP-03]
must_haves:
  truths:
    - "On the Orders table at <640px the Total (preço) column is hidden, leaving O.S.#, Cliente/Veículo, and Status always visible"
    - "On the dashboard recent-orders table at <640px the Total column is hidden, matching the Orders table rule"
    - "DataTable horizontal scroll continues to work — no parent change clips it"
  artifacts:
    - path: "src/app/(dashboard)/orders/_components/OrdersClient.tsx"
      provides: "totalAmount column hidden below sm"
      contains: "hidden sm:table-cell"
    - path: "src/app/(dashboard)/page.tsx"
      provides: "totalAmount column hidden below sm in orderColumns"
      contains: "hidden sm:table-cell"
  key_links:
    - from: "src/app/(dashboard)/orders/_components/OrdersClient.tsx"
      to: "src/_components/ui/data-table.tsx"
      via: "DataTableColumn.className propagated to TableHead/TableCell"
      pattern: 'className: "hidden sm:table-cell"'
---

<objective>
Complete responsive column hiding on the two order tables (RESP-02, RESP-03). Per D-05, the price/Total column is secondary detail and must be hidden below the `sm` breakpoint (640px), keeping only the essential columns (O.S.# + Cliente/Veículo + Status) visible on phones.

Purpose: The Orders table and the dashboard recent-orders table already hide `plate` at sm and `mechanic` at md, but their `totalAmount` columns are still always-visible — the one gap versus D-05. The canonical pattern is `className: "hidden sm:table-cell"` on the column definition (as used in customers-client.tsx).
Output: Two modified column definitions; phone-width order tables show only essential columns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-responsividade-responsiveness/02-CONTEXT.md
@.planning/phases/02-responsividade-responsiveness/02-RESEARCH.md
@.planning/phases/02-responsividade-responsiveness/02-PATTERNS.md
@src/app/(dashboard)/customers/customers-client.tsx
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hide Total column below sm in the Orders table</name>
  <files>src/app/(dashboard)/orders/_components/OrdersClient.tsx</files>
  <read_first>
    - src/app/(dashboard)/orders/_components/OrdersClient.tsx (read the full `columns` array — the `totalAmount` column definition is around lines 69-79; confirm current state)
    - src/app/(dashboard)/customers/customers-client.tsx (canonical reference — see how `className: "hidden sm:table-cell"` sits on a column definition)
    - .planning/phases/02-responsividade-responsiveness/02-PATTERNS.md (OrdersClient.tsx section, lines 216-274)
  </read_first>
  <action>
    Locate the column definition object with `id: "totalAmount"` (header `"Total"`, `align: "right"`). Add a new property `className: "hidden sm:table-cell"` to that object (place it alongside `id`/`header`/`align`, before `cell`). This hides the Total/preço column below 640px per D-05.

    Do NOT touch any other column. Verify the already-correct columns remain unchanged: `orderNumber` (no className), `plate` (`hidden sm:table-cell`), `customer` (no className), `mechanic` (`hidden md:table-cell`), `status` (no className), `updatedAt` (`hidden md:table-cell`). Do not modify the `cell` render function of `totalAmount`. Do not change any wrapper/Card markup — the DataTable's internal `overflow-x-auto` already handles horizontal scroll (D-03), and the wrapper's `overflow-hidden` is intentional for border-radius clipping.

  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/(dashboard)/orders/_components/OrdersClient.tsx','utf8'); const i=s.indexOf('id: \"totalAmount\"'); const seg=s.slice(i,i+260); process.exit(seg.includes('hidden sm:table-cell')?0:1)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the object containing `id: "totalAmount"` also contains `className: "hidden sm:table-cell"` within the same definition block.
    - Source assertion: the `plate` column still has `hidden sm:table-cell` and `mechanic` still has `hidden md:table-cell` (untouched).
    - Behavior assertion: at viewport width 375px the Orders table shows O.S.#, Cliente/Veículo, and Status but not Total (verified visually in plan 04 RESP-05 pass).
  </acceptance_criteria>
  <done>The Orders table `totalAmount` column hides below `sm`; all other columns unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Hide Total column below sm in the dashboard recent-orders table</name>
  <files>src/app/(dashboard)/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/page.tsx (read the `orderColumns` array — the `totalAmount` column is around lines 65-73; confirm current state)
    - .planning/phases/02-responsividade-responsiveness/02-PATTERNS.md (page.tsx section, lines 278-318)
  </read_first>
  <action>
    In the `orderColumns` array, locate the column object with `id: "totalAmount"` (header `"Total"`, `align: "right"`). Add `className: "hidden sm:table-cell"` to that object (before the `cell` property), mirroring the Orders table change in Task 1 per D-05.

    Do NOT alter the dashboard metric grid (already `sm:grid-cols-2 lg:grid-cols-4`, verified correct) or any other column. The already-correct columns are: `id`/O.S. (no className), `plate` (`hidden sm:table-cell`), `customer` (no className), `mechanic` (`hidden md:table-cell`), `status` (no className). Do not modify the `cell` render function.

  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/(dashboard)/page.tsx','utf8'); const i=s.indexOf('id: \"totalAmount\"'); const seg=s.slice(i,i+260); process.exit(seg.includes('hidden sm:table-cell')?0:1)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the `orderColumns` object with `id: "totalAmount"` contains `className: "hidden sm:table-cell"`.
    - Source assertion: the dashboard metric grid still contains `sm:grid-cols-2 lg:grid-cols-4` (untouched).
    - Behavior assertion: at 375px the dashboard recent-orders table hides the Total column (verified in plan 04 RESP-05 pass).
  </acceptance_criteria>
  <done>The dashboard `orderColumns` `totalAmount` column hides below `sm`; grid and other columns unchanged.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                                                             |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| (none)   | This plan adds one Tailwind class to two column definitions. No new input surfaces, no auth/session changes, no server actions touched. |

## STRIDE Threat Register

| Threat ID | Category | Component                  | Disposition | Mitigation Plan                                                                                                       |
| --------- | -------- | -------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------- |
| T-02-02   | (n/a)    | DataTable column className | accept      | ASVS L1 not applicable — no security-relevant change. No new dependencies, so no supply-chain (T-{phase}-SC) surface. |

ASVS L1 is not applicable to this CSS-only responsive polish plan (per phase Security Domain).
</threat_model>

<verification>
- `node` source assertions in each task pass.
- Final gate runs in plan 04 (RESP-05): `npx tsc --noEmit && npm run lint && npm run build`.
</verification>

<success_criteria>

- Orders table and dashboard recent-orders table both hide the `totalAmount` column below `sm`.
- Essential columns (O.S.#, Cliente/Veículo, Status) remain visible at all breakpoints.
- No grid or wrapper markup altered; DataTable horizontal scroll preserved.
  </success_criteria>

<output>
Create `.planning/phases/02-responsividade-responsiveness/02-02-SUMMARY.md` when done.
</output>
