---
phase: 02-responsividade-responsiveness
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03"]
files_modified: []
autonomous: false
requirements: [RESP-05]
must_haves:
  truths:
    - "npx tsc --noEmit reports zero errors"
    - "npm run lint reports zero errors"
    - "npm run build succeeds"
    - "At 375px, 768px, and 1024px no existing route shows horizontal page overflow or clipped content"
    - "The mobile sidebar drawer opens and closes, and DataTables scroll horizontally on small screens"
  artifacts:
    - path: ".planning/phases/02-responsividade-responsiveness/02-04-SUMMARY.md"
      provides: "Breakpoint verification record and gate results"
      contains: "RESP-05"
  key_links:
    - from: "all Wave 1 plan changes"
      to: "rendered routes at 375/768/1024px"
      via: "breakpoint verification pass"
      pattern: "375"
---

<objective>
Verify breakpoints across all existing routes and run the locked completion checklist (RESP-05, D-12). This is the closing verification pass for Phase 2 — it confirms the Wave 1 class changes produced no horizontal overflow or clipped content at 375px (mobile), 768px (tablet), and 1024px (desktop), and that the automated gate (tsc / lint / build) is clean.

Purpose: RESP-05 is a verification requirement, not a code change. It depends on plans 02-01, 02-02, and 02-03 being complete.
Output: An automated gate pass plus a human-verified breakpoint record in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-responsividade-responsiveness/02-CONTEXT.md
@.planning/phases/02-responsividade-responsiveness/02-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the locked completion gate (tsc / lint / build)</name>
  <files></files>
  <read_first>
    - .planning/phases/02-responsividade-responsiveness/02-RESEARCH.md (Validation Architecture section, lines 547-585 — quick-run and full-suite commands)
    - AGENTS.md (this is NOT the Next.js you know — read node_modules/next/dist/docs/ before interpreting any build error)
  </read_first>
  <action>
    Run the locked completion checklist in order and capture each result:
    1. `npx tsc --noEmit` — must report zero TypeScript errors. This guards against accidental prop/type regressions from the `icon-touch` variant and the column className additions.
    2. `npm run lint` — must report zero ESLint errors (import sort, no-unused-vars).
    3. `npm run build` — must complete a successful Next.js 16 / Turbopack build.

    If any command fails, fix the underlying class/prop regression in the relevant Wave 1 file (do not suppress with eslint-disable or ts-ignore), then re-run all three until clean. If a build error references Next.js APIs, consult `node_modules/next/dist/docs/` per AGENTS.md before changing code.

  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: `npx tsc --noEmit` exits 0 with no error lines.
    - CLI output: `npm run lint` exits 0 with no error lines.
    - CLI output: `npm run build` exits 0 and prints a successful build summary.
  </acceptance_criteria>
  <done>All three gate commands pass with zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human breakpoint verification across all routes</name>
  <files></files>
  <action>
    This is a human-verification checkpoint (no source edits). Present the verification steps below to the user, wait for the resume signal, and record the per-route / per-width result in the SUMMARY. See `<what-built>` and `<how-to-verify>` for the exact verification procedure.
  </action>
  <what-built>
    Wave 1 applied the Phase 2 responsive changes: mobile drawer width `w-[85vw] max-w-xs`, `icon-touch` (44px) header buttons, `py-3` sidebar touch targets, `totalAmount` column hidden at sm on both order tables, horizontal-scroll step indicator, and horizontal-scroll finance charts. Task 1 confirmed tsc/lint/build are clean.
  </what-built>
  <how-to-verify>
    1. Start the app: `npm run dev` (or use the production build from Task 1).
    2. Open browser DevTools device toolbar. Test each route at THREE widths: 375px, 768px, 1024px.
    3. Routes to check (per RESEARCH.md target list): `/` (dashboard), `/orders`, `/orders/new`, `/customers`, `/customers/[id]`, `/inventory`, `/inventory/new`, `/finance`, `/finance/reports`, `/appointments`, `/analytics`.
    4. For each route at each width, confirm: NO horizontal page-level scrollbar (no body overflow), NO content clipped off-screen.
    5. Specifically confirm: at 375px the mobile menu button (top-left) opens a drawer that is ~85% of screen width and closes on nav-item tap or backdrop tap; DataTables (orders, customers, inventory, finance) scroll horizontally inside their card without pushing the page wide; the `/orders/new` step indicator shows all 5 steps and scrolls horizontally; finance charts scroll horizontally rather than collapsing.
    6. Confirm tap targets: the Menu/Search/Bell header buttons and sidebar nav links feel >= 44px (inspect box in DevTools if unsure).
  </how-to-verify>
  <verify>
    <human-check>User confirms no horizontal overflow or clipped content on any route at 375/768/1024px; drawer open/close and DataTable scroll work.</human-check>
  </verify>
  <resume-signal>Type "approved" if no horizontal overflow or clipping at any of 375/768/1024px. Otherwise describe the route, width, and the overflow/clipping observed so it can be fixed.</resume-signal>
  <done>User approves the breakpoint pass; any reported overflow/clipping is fixed and re-verified before approval.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                                                                      |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| (none)   | This plan runs verification commands and a manual visual pass. No source behavior changes, no new input surfaces, no auth/server-action changes. |

## STRIDE Threat Register

| Threat ID | Category | Component              | Disposition | Mitigation Plan                                                                                                                 |
| --------- | -------- | ---------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| T-02-04   | (n/a)    | Verification-only plan | accept      | ASVS L1 not applicable — no security-relevant change. No new dependencies installed, so no supply-chain (T-{phase}-SC) surface. |

ASVS L1 is not applicable to this CSS-only responsive verification plan (per phase Security Domain).
</threat_model>

<verification>
- Task 1 automated gate: `npx tsc --noEmit && npm run lint && npm run build` all pass.
- Task 2 human checkpoint: breakpoint pass approved across all routes at 375/768/1024px.
</verification>

<success_criteria>

- tsc, lint, and build are all clean.
- No horizontal overflow or clipped content on any existing route at 375px, 768px, or 1024px.
- Mobile drawer open/close and DataTable horizontal scroll confirmed working.
  </success_criteria>

<output>
Create `.planning/phases/02-responsividade-responsiveness/02-04-SUMMARY.md` when done — record the gate results and a per-route/per-width breakpoint table (RESP-05).
</output>
