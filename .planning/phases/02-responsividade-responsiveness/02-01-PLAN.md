---
phase: 02-responsividade-responsiveness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/_components/ui/button.tsx
  - src/_components/shared/header.tsx
  - src/_components/shared/sidebar.tsx
autonomous: true
requirements: [RESP-01, RESP-04]
must_haves:
  truths:
    - "On a phone-width viewport the mobile sidebar drawer opens to 85vw (capped at max-w-xs), not a fixed 256px"
    - "The three mobile-critical header icon buttons (Menu, Search, Bell) render at >= 44x44px touch target"
    - "Sidebar nav links and the Suporte Técnico button render at >= 44px height"
    - 'The existing size="icon" (size-8) Button variant is unchanged so close/table-action buttons keep their visual size'
  artifacts:
    - path: "src/_components/ui/button.tsx"
      provides: "New icon-touch size variant (size-11 = 44px)"
      contains: "icon-touch"
    - path: "src/_components/shared/header.tsx"
      provides: "Mobile drawer width w-[85vw] max-w-xs and icon-touch buttons"
      contains: "w-[85vw] max-w-xs"
    - path: "src/_components/shared/sidebar.tsx"
      provides: "Nav links and support button at py-3 touch height"
      contains: "py-3"
  key_links:
    - from: "src/_components/shared/header.tsx"
      to: "src/_components/ui/button.tsx"
      via: 'Button size="icon-touch" prop'
      pattern: 'size="icon-touch"'
---

<objective>
Make navigation touch-friendly and the mobile drawer adaptive (RESP-01, RESP-04). Add a new `icon-touch` Button size variant (44x44px) per D-11, apply it to the three mobile-critical header icon buttons, widen the mobile sidebar drawer to `w-[85vw] max-w-xs` per D-02, and bump sidebar nav/support padding from `py-2.5` to `py-3` (>= 44px) per D-10.

Purpose: The Sheet-based mobile sidebar and header icon buttons already exist but fall below the 44px Apple HIG touch minimum and use a fixed `w-64` drawer that does not adapt to 320px screens.
Output: Three modified files; new reusable `icon-touch` variant; touch-compliant navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-responsividade-responsiveness/02-CONTEXT.md
@.planning/phases/02-responsividade-responsiveness/02-RESEARCH.md
@.planning/phases/02-responsividade-responsiveness/02-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add icon-touch size variant to Button</name>
  <files>src/_components/ui/button.tsx</files>
  <read_first>
    - src/_components/ui/button.tsx (read the full `size` variants object on lines 22-34 — see current state, do not assume)
    - .planning/phases/02-responsividade-responsiveness/02-PATTERNS.md (button.tsx section, lines 143-170 — exact target)
  </read_first>
  <action>
    In the `size` object of `buttonVariants` (currently lines 22-34), add a new entry `"icon-touch": "size-11"` immediately after the existing `"icon-lg": "size-9"` entry. `size-11` = 44px x 44px, the Apple HIG / Material touch minimum (D-11).

    CRITICAL — do NOT modify the existing `icon: "size-8"` entry. Per Pitfall 4 in RESEARCH.md, that variant is consumed by close buttons (sheet.tsx), table action buttons, and other small icon buttons site-wide; changing it would cause visual regressions. You are ADDING one new entry only, not editing any existing entry. The `VariantProps<typeof buttonVariants>` type derives the new value automatically — no type annotation changes are needed.

  </action>
  <verify>
    <automated>grep -q '"icon-touch": "size-11"' "src/_components/ui/button.tsx" && grep -q '\bicon: "size-8"' "src/_components/ui/button.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/_components/ui/button.tsx` contains the exact string `"icon-touch": "size-11"`.
    - Source assertion: `src/_components/ui/button.tsx` STILL contains `icon: "size-8"` (existing variant unchanged).
    - Behavior assertion: `<Button size="icon-touch" />` type-checks without a TypeScript error (verified by the phase tsc gate in plan 04).
  </acceptance_criteria>
  <done>The `icon-touch` size variant exists and maps to `size-11`; the original `icon` variant is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Widen mobile drawer and apply icon-touch to header buttons</name>
  <files>src/_components/shared/header.tsx</files>
  <read_first>
    - src/_components/shared/header.tsx (read the full file — SheetContent at lines 94-100, SheetTrigger Button at lines 82-93, Search button at lines 136-144, Bell button at lines 145-153)
    - src/_components/ui/button.tsx (confirm the `icon-touch` variant added in Task 1 exists before consuming it)
    - .planning/phases/02-responsividade-responsiveness/02-PATTERNS.md (header.tsx section, lines 26-101)
  </read_first>
  <action>
    Two edits in header.tsx:

    1. Drawer width (D-02): On the `SheetContent` element (line 96), change the className from `"border-outline-variant bg-surface-container w-64 p-0"` to `"border-outline-variant bg-surface-container w-[85vw] max-w-xs p-0"`. This replaces the fixed `w-64` (256px) with a viewport-relative width that adapts down to 320px screens while capping at `max-w-xs` (320px) on larger phones. Do NOT use `asChild` anywhere — SheetTrigger already uses the Base UI `render={<Button ... />}` prop; keep that pattern.

    2. Touch targets (D-11): Change `size="icon"` to `size="icon-touch"` on the THREE mobile-critical icon buttons only:
       - The SheetTrigger Button (the `Menu` trigger, lines 84-89) — keep its `className="lg:hidden"`.
       - The mobile Search Button (lines 137-142) — keep its `className="sm:hidden"`.
       - The Bell notifications Button (lines 146-149) — keep its existing className `"text-on-surface-variant hover:bg-surface-container relative rounded-full"`.

    Do NOT change the "Fechar busca" X button at lines 68-75 (it is inside the conditional mobile search bar and stays `size="icon"`). Do NOT change `ThemeToggle` (it owns its own internal button). The `lucide-react` icons inside keep `className="size-5"` (icon glyph size is independent of button box size).

  </action>
  <verify>
    <automated>grep -q 'w-\[85vw\] max-w-xs' "src/_components/shared/header.tsx" && [ "$(grep -c 'size="icon-touch"' "src/_components/shared/header.tsx")" = "3" ] && ! grep -q 'w-64' "src/_components/shared/header.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: header.tsx SheetContent className contains `w-[85vw] max-w-xs` and no longer contains `w-64`.
    - Source assertion: header.tsx contains exactly 3 occurrences of `size="icon-touch"` (Menu trigger, mobile Search, Bell).
    - Source assertion: the "Fechar busca" button (aria-label="Fechar busca") still uses `size="icon"`.
    - Source assertion: SheetTrigger still uses `render={` (Base UI render prop), never `asChild`.
  </acceptance_criteria>
  <done>Mobile drawer width is `w-[85vw] max-w-xs`; Menu, Search, and Bell buttons use `icon-touch`; the search-close button and ThemeToggle are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Raise sidebar nav and support button touch height</name>
  <files>src/_components/shared/sidebar.tsx</files>
  <read_first>
    - src/_components/shared/sidebar.tsx (read the full file — nav link cn() at line 61, Suporte Técnico button at line 78)
    - .planning/phases/02-responsividade-responsiveness/02-PATTERNS.md (sidebar.tsx section, lines 105-139)
  </read_first>
  <action>
    Two `py-2.5` -> `py-3` substitutions in sidebar.tsx (D-10). `py-2.5` yields ~40px height (below the 44px minimum); `py-3` yields ~48px.

    1. Nav link (line 61): inside the `cn(...)` call, change the base class string from `"text-label-md flex items-center gap-3 rounded-lg px-4 py-2.5 font-mono transition-colors"` to the same string with `py-2.5` replaced by `py-3`. Leave the conditional active/inactive branches untouched.

    2. Suporte Técnico button (line 78): in its className, change `py-2.5` to `py-3`. Leave every other class in that string untouched.

    Change ONLY the `py-2.5` tokens. Do not alter `px-4`, `gap-3`, `rounded-lg`, or any other class.

  </action>
  <verify>
    <automated>[ "$(grep -c 'py-2.5' "src/_components/shared/sidebar.tsx")" = "0" ] && [ "$(grep -c 'py-3' "src/_components/shared/sidebar.tsx")" -ge "2" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/_components/shared/sidebar.tsx` contains zero occurrences of `py-2.5`.
    - Source assertion: the nav-link base string contains `px-4 py-3 font-mono`.
    - Source assertion: the Suporte Técnico button className contains `px-4 py-3 font-mono`.
  </acceptance_criteria>
  <done>Both the nav links and the support button use `py-3` (>= 44px); no `py-2.5` remains in the file.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                                                                             |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none)   | This plan changes only Tailwind class strings on existing client components. No new input surfaces, no auth/session changes, no server actions touched. |

## STRIDE Threat Register

| Threat ID | Category | Component            | Disposition | Mitigation Plan                                                                                                                 |
| --------- | -------- | -------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | (n/a)    | CSS-only class edits | accept      | ASVS L1 not applicable — no security-relevant change. No new dependencies installed, so no supply-chain (T-{phase}-SC) surface. |

ASVS L1 is not applicable to this CSS-only responsive polish plan (per phase Security Domain).
</threat_model>

<verification>
- `grep` assertions in each task pass (see `<verify>` blocks).
- Final gate runs in plan 04 (RESP-05): `npx tsc --noEmit && npm run lint && npm run build`.
</verification>

<success_criteria>

- New `icon-touch` (size-11) Button variant exists; original `icon` (size-8) untouched.
- Mobile drawer width = `w-[85vw] max-w-xs`; no `w-64` remains in header.tsx.
- Menu, Search, and Bell header buttons use `icon-touch`.
- Sidebar nav links and support button use `py-3`; no `py-2.5` remains.
  </success_criteria>

<output>
Create `.planning/phases/02-responsividade-responsiveness/02-01-SUMMARY.md` when done.
</output>
