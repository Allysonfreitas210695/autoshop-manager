---
phase: 05-db-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/_db/index.ts
  - src/_db/schema/purchase-orders.ts
  - src/_db/migrations/
autonomous: true
requirements: [FOUND-01]
must_haves:
  truths:
    - "DB connection pool is bounded to max 3 connections (serverless-safe for Vercel Lambda)"
    - "purchase_order_status enum includes the 'confirmed' value"
    - "drizzle-kit migrate applies migration 0003 without error"
  artifacts:
    - path: "src/_db/index.ts"
      provides: "Bounded pg.Pool singleton (max:3, idleTimeout, connectionTimeout)"
      contains: "max: 3"
    - path: "src/_db/schema/purchase-orders.ts"
      provides: "purchaseOrderStatus pgEnum with 'confirmed'"
      contains: "confirmed"
    - path: "src/_db/migrations/"
      provides: "Migration 0003 adding 'confirmed' to enum"
      contains: "ADD VALUE 'confirmed'"
  key_links:
    - from: "src/_db/index.ts"
      to: "process.env.DATABASE_URL"
      via: "new Pool({ connectionString })"
      pattern: "new Pool\\("
    - from: "src/_db/migrations/0003"
      to: "purchase_order_status"
      via: "ALTER TYPE ADD VALUE"
      pattern: "ADD VALUE 'confirmed'"
---

<objective>
Harden the database connection pool for Vercel serverless and close the only schema gap (the missing `'confirmed'` purchase-order status enum value), then generate and apply migration 0003.

Purpose: Make the DB live and serverless-safe (FOUND-01) and unblock INV-02 (Phase 8 purchase orders with "confirmed" status) before downstream phases reach the database.
Output: Bounded pool config, updated enum, generated + applied migration 0003.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-db-foundation-auth/05-CONTEXT.md
@.planning/phases/05-db-foundation-auth/05-RESEARCH.md
@.planning/phases/05-db-foundation-auth/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Harden pg.Pool for serverless (D-01, D-02, D-03)</name>
  <files>src/_db/index.ts</files>
  <read_first>
    - src/_db/index.ts (current pool config — surgical edit target)
    - .planning/phases/05-db-foundation-auth/05-PATTERNS.md (src/_db/index.ts analog: target pool pattern, lines 37-47)
  </read_first>
  <action>
    In the existing `new Pool({...})` call (currently only passing `connectionString: process.env.DATABASE_URL`), add three properties per D-01/D-02/D-03: `max: 3`, `idleTimeoutMillis: 30000`, `connectionTimeoutMillis: 2000`. Keep the `globalForDb` singleton guard (`if (process.env.NODE_ENV !== "production")`) and the `drizzle(pool, { schema })` export unchanged. Do NOT introduce new `any`/`as unknown` casts — the pre-existing `globalForDb` cast stays as-is. Do not swap the driver (Neon serverless driver is deferred per CONTEXT.md).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/_db/index.ts contains `max: 3`
    - src/_db/index.ts contains `idleTimeoutMillis: 30000` (or `30_000`)
    - src/_db/index.ts contains `connectionTimeoutMillis: 2000` (or `2_000`)
    - src/_db/index.ts still contains `globalForDb.pool = pool`
    - src/_db/index.ts still contains `export const db = drizzle(pool, { schema })`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Pool is bounded to max 3 with idle/connection timeouts; singleton and exports intact; typecheck green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 'confirmed' to purchase_order_status enum (D-05)</name>
  <files>src/_db/schema/purchase-orders.ts</files>
  <read_first>
    - src/_db/schema/purchase-orders.ts (current pgEnum, lines 14-19)
    - .planning/phases/05-db-foundation-auth/05-PATTERNS.md (purchase-orders.ts analog: append 'confirmed' last; order matters for snapshot diff)
  </read_first>
  <action>
    Append `'confirmed'` as the 5th and last value of the `purchaseOrderStatus` pgEnum so the array becomes `["draft", "sent", "received", "cancelled", "confirmed"]`. Do NOT reorder the existing four values — drizzle-kit snapshot diffing requires append-last so the generated migration is a clean `ALTER TYPE ADD VALUE`. Do not touch the `purchaseOrders` / `purchaseOrderItems` table definitions or relations.
  </action>
  <verify>
    <automated>grep -v '^//' src/_db/schema/purchase-orders.ts | grep -c "confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - src/_db/schema/purchase-orders.ts pgEnum array ends with `"confirmed"`
    - The four prior values remain in order: draft, sent, received, cancelled
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Enum has 5 values ending in "confirmed"; existing order preserved; typecheck green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: [BLOCKING] Generate and apply migration 0003 (D-04, D-06)</name>
  <files>src/_db/migrations/</files>
  <read_first>
    - src/_db/migrations/0002_flat_reavers.sql (ALTER TYPE / statement-breakpoint convention analog)
    - src/_db/schema/purchase-orders.ts (must reflect Task 2's enum change before generating)
  </read_first>
  <action>
    This task MUST run AFTER Task 2 completes. Run `npm run db:generate` (drizzle-kit generate) to produce a new migration `0003_*.sql` containing `ALTER TYPE "public"."purchase_order_status" ADD VALUE 'confirmed';`. Do NOT hand-write the migration SQL — drizzle-kit emits it from the schema diff. Then run `npm run db:migrate` (drizzle-kit migrate) to apply it against the Neon DB (`DATABASE_URL` in .env). drizzle-kit checks the journal and is safe whether 0002 was already applied or not. Do NOT use `drizzle-kit push` — it bypasses the migration journal. If a `prepared statement "..." already exists` error appears (Neon PgBouncer transaction mode), note it in the SUMMARY for follow-up (`?pgbouncer=true` is the documented fix) — do not change DATABASE_URL without operator confirmation.
  </action>
  <verify>
    <automated>npm run db:migrate</automated>
  </verify>
  <acceptance_criteria>
    - A new file matching `src/_db/migrations/0003_*.sql` exists
    - That 0003 file contains `ADD VALUE 'confirmed'`
    - `npm run db:migrate` exits 0
    - `src/_db/migrations/meta/` snapshot updated by drizzle-kit (not hand-edited)
  </acceptance_criteria>
  <done>Migration 0003 generated with ADD VALUE 'confirmed' and applied to the DB without error.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                |
| --------------------- | ---------------------------------------------------------- |
| app runtime → Neon DB | Server-side code opens pooled connections via DATABASE_URL |
| drizzle-kit CLI → DB  | Migration runner applies DDL using local .env credentials  |

## STRIDE Threat Register

| Threat ID | Category               | Component                    | Disposition | Mitigation Plan                                                                                                                        |
| --------- | ---------------------- | ---------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-01   | Denial of Service      | pg.Pool in src/\_db/index.ts | mitigate    | `max: 3` cap (D-01) + idle/connection timeouts (D-02) prevent Vercel Lambda connection exhaustion against Neon's hard connection limit |
| T-05-02   | Information Disclosure | DATABASE_URL / pool errors   | mitigate    | Do not log DATABASE_URL; do not add error handlers that print the connection string; .env stays local-only (not committed)             |
| T-05-03   | Tampering              | drizzle-kit migrate DDL      | accept      | drizzle-kit reads only trusted local schema/config — no user-input path; ALTER TYPE ADD VALUE is additive and non-destructive          |
| T-05-SC   | Tampering              | npm installs                 | accept      | No new packages installed this phase (RESEARCH.md Package Legitimacy Audit: none); existing deps already vetted                        |

</threat_model>

<verification>
- `npm run typecheck` exits 0 after Tasks 1 and 2
- `src/_db/index.ts` has `max: 3` and both timeout properties
- `purchase_order_status` enum ends with `confirmed`
- `npm run db:migrate` exits 0 and `0003_*.sql` exists with `ADD VALUE 'confirmed'`
</verification>

<success_criteria>

- Pool bounded to max 3 with idle/connection timeouts (serverless-safe — FOUND-01)
- Enum gap closed: `confirmed` present and applied to DB via migration 0003
- No new dependencies, no driver swap, no schema reordering
  </success_criteria>

<output>
Create `.planning/phases/05-db-foundation-auth/05-01-SUMMARY.md` when done
</output>
