---
phase: 05-db-foundation-auth
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - scripts/seed.ts
autonomous: true
requirements: [FOUND-02, FOUND-03]
must_haves:
  truths:
    - "Seed script wipes all 12 tables in FK-reverse order, including verification"
    - "Running the seed twice in a row succeeds without FK or duplicate-email errors (idempotent)"
    - "Users are created via Better Auth API so seeded credentials can log in"
  artifacts:
    - path: "scripts/seed.ts"
      provides: "Idempotent seed covering all 12 relationships"
      contains: "verificationTable"
  key_links:
    - from: "scripts/seed.ts wipe()"
      to: "verification table"
      via: "db.delete(verificationTable) before userTable"
      pattern: "db\\.delete\\(verificationTable\\)"
    - from: "scripts/seed.ts createUser"
      to: "Better Auth"
      via: "auth.api.signUpEmail"
      pattern: "auth\\.api\\.signUpEmail"
---

<objective>
Make `scripts/seed.ts` truly idempotent by adding the missing `verification` table to the `wipe()` function, then validate that the seed runs cleanly twice in a row and that seeded users authenticate.

Purpose: FOUND-03 (developer can populate DB with representative data) and FOUND-02 (auth against real Drizzle tables — seed creates the login users via Better Auth so login/session can be verified).
Output: Idempotent seed covering all 12 relationships; auth-ready seeded users.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-db-foundation-auth/05-CONTEXT.md
@.planning/phases/05-db-foundation-auth/05-RESEARCH.md
@.planning/phases/05-db-foundation-auth/05-PATTERNS.md
@.planning/phases/05-db-foundation-auth/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix seed idempotency — add verification to wipe (D-07, D-08, D-09)</name>
  <files>scripts/seed.ts</files>
  <read_first>
    - scripts/seed.ts (current imports lines 16-29 and wipe() lines 75-88)
    - .planning/phases/05-db-foundation-auth/05-PATTERNS.md (seed.ts analog: import + wipe target, lines 122-196)
    - src/_db/schema/auth.ts (verification table export to confirm name)
  </read_first>
  <action>
    Add `verification as verificationTable` to the existing `@/_db/schema` import block (currently imports account, appointments, purchaseOrderItems, purchaseOrders, serviceOrderItems, serviceOrders, services, session, transactions, user, vehicles — but NOT verification). In the `wipe()` function, insert `await db.delete(verificationTable);` immediately BEFORE `await db.delete(userTable);` — verification rows reference `user.id`, so it must be cleared first in FK-reverse order. The full reverse order per CONTEXT.md D-07 specifics is: transactions → serviceOrderItems → serviceOrders → appointments → purchaseOrderItems → purchaseOrders → vehicles → services → session → account → verification → user. Do NOT change `createUser` — it already correctly uses `auth.api.signUpEmail` then `db.update` for custom fields (D-08); keep direct `db.insert(userTable)` out. Do not add try/catch around the deletes (delete on empty table is a no-op). All 12 relationships (D-09) must remain covered: user, session, account, verification, vehicles, services, serviceOrders, serviceOrderItems, transactions, appointments, purchaseOrders, purchaseOrderItems.
  </action>
  <verify>
    <automated>grep -v '^ *//' scripts/seed.ts | grep -c "db.delete(verificationTable)"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seed.ts imports `verification as verificationTable` from `@/_db/schema`
    - wipe() contains `await db.delete(verificationTable)` positioned before `await db.delete(userTable)`
    - createUser still calls `auth.api.signUpEmail` (no direct user insert added)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>verification is imported and deleted before user in wipe(); createUser unchanged; typecheck green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Validate seed idempotency (FOUND-03)</name>
  <files>scripts/seed.ts</files>
  <read_first>
    - scripts/seed.ts (post-edit state from Task 1)
    - .planning/phases/05-db-foundation-auth/05-VALIDATION.md (seed validation: run twice consecutively without FK error)
  </read_first>
  <action>
    Run `npm run db:seed` twice in a row against the Neon DB. The second run exercises the `wipe()` idempotency: it must clear all 12 tables (including verification) and re-insert without `ForeignKeyConstraintViolation` or duplicate-unique-email errors. Confirm seeded admin user `admin@precisionauto.com` exists after the run (the credential used for the FOUND-02 manual login UAT, password `senha123`). If a Neon PgBouncer `prepared statement "..." already exists` error appears, record it in the SUMMARY for follow-up — do not modify DATABASE_URL without operator confirmation. This seed is dev-only data (`senha123`) — must never run against production with real users; note this in the SUMMARY.
  </action>
  <verify>
    <automated>npm run db:seed &amp;&amp; npm run db:seed</automated>
  </verify>
  <acceptance_criteria>
    - First `npm run db:seed` exits 0
    - Second consecutive `npm run db:seed` exits 0 (no FK or duplicate-email error)
    - Seed output shows users, vehicles, service orders, transactions, appointments, and purchase orders created on both runs
  </acceptance_criteria>
  <done>Seed runs cleanly twice consecutively; all 12 tables populated; idempotency proven.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                            |
| ---------------------- | ---------------------------------------------------------------------- |
| seed CLI → Better Auth | seed creates users through auth.api.signUpEmail (server-side, trusted) |
| seed CLI → Neon DB     | bulk db.delete / db.insert using local .env credentials                |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                                                                                                                           |
| --------- | ---------------------- | -------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-04   | Spoofing               | seed default password `senha123` | accept      | Dev-only seed data; SUMMARY documents that seed MUST NOT run against production with real user data; passwords still hashed by Better Auth (not plaintext)                                |
| T-05-05   | Elevation of Privilege | user creation in seed            | mitigate    | Users created via `auth.api.signUpEmail` (D-08) so password hashing uses Better Auth's config; never `db.insert(userTable)` directly which would store un-authenticatable/plaintext users |
| T-05-06   | Information Disclosure | DATABASE_URL in seed logs        | mitigate    | Seed must not log the connection string; .env stays local-only                                                                                                                            |
| T-05-SC   | Tampering              | npm installs                     | accept      | No new packages this phase (RESEARCH.md Package Legitimacy Audit: none)                                                                                                                   |

</threat_model>

<verification>
- `scripts/seed.ts` imports and deletes `verification` before `user`
- `npm run typecheck` exits 0
- `npm run db:seed && npm run db:seed` both exit 0 (idempotent)
- Seeded admin credential available for FOUND-02 manual login check
</verification>

<success_criteria>

- Seed is idempotent: runs cleanly twice with no FK/duplicate errors (FOUND-03)
- All 12 relationships covered including verification (D-09)
- Users created via Better Auth API — seeded credentials can authenticate (supports FOUND-02)
  </success_criteria>

<output>
Create `.planning/phases/05-db-foundation-auth/05-02-SUMMARY.md` when done
</output>
