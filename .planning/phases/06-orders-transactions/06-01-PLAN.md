---
phase: 06-orders-transactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/_actions/orders.ts
  - src/_actions/orders.test.ts
autonomous: true
requirements: [OS-01, OS-02, OS-03]

must_haves:
  truths:
    - "Closing an O.S. (status → completed) inserts exactly one transactions row with the order total and orderNumber in the description (D-01, D-02)"
    - "A transactions row is inserted ONLY when status === completed, never on other status changes (D-01)"
    - "Approving or rejecting a budget item recalculates serviceOrders.totalAmount as the sum of approved items only (D-03, D-04)"
    - "Every db.update on serviceOrders sets updatedAt: new Date() (OS-03)"
    - "updateOrderStatusAction revalidates /orders, /orders/[id], /finance, /analytics (D-05)"
    - "approveOrderItemAction revalidates /orders/[id]/budget and /orders/[id] (D-06)"
  artifacts:
    - path: "src/_actions/orders.ts"
      provides: "updateOrderStatusAction with atomic transaction insert + approveOrderItemAction with totalAmount recalc"
      contains: "db.insert(transactions)"
    - path: "src/_actions/orders.test.ts"
      provides: "Static source-assertion tests proving D-01..D-06 wiring exists"
      exports: ["(vitest describe blocks)"]
  key_links:
    - from: "src/_actions/orders.ts updateOrderStatusAction"
      to: "transactions table"
      via: "db.insert(transactions) guarded by status === completed"
      pattern: "db\\.insert\\(transactions\\)"
    - from: "src/_actions/orders.ts approveOrderItemAction"
      to: "serviceOrders.totalAmount"
      via: "re-query approved items + db.update(serviceOrders)"
      pattern: "totalAmount: String\\("
---

<objective>
Make Phase 6 data behavior true: closing an O.S. atomically creates a `transactions` row, and approving/rejecting budget items recalculates the parent order `totalAmount`. All edits are surgical modifications to the two existing actions in `src/_actions/orders.ts`. No new files in the data layer; one new test file.

Purpose: Without these edits, Finance/Analytics show zero data (no transactions are ever written) and order totals go stale after budget approval. This closes OS-01, OS-02, OS-03.
Output: Modified `src/_actions/orders.ts` (updateOrderStatusAction + approveOrderItemAction) and a new `src/_actions/orders.test.ts` source-assertion test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-orders-transactions/06-CONTEXT.md
@.planning/phases/06-orders-transactions/06-RESEARCH.md
@.planning/phases/06-orders-transactions/06-PATTERNS.md
@.planning/phases/06-orders-transactions/06-VALIDATION.md
@src/_actions/orders.ts
@src/_db/schema/transactions.ts
@src/_actions/_audit.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source-assertion test scaffold for D-01..D-06 (Wave 0)</name>
  <files>src/_actions/orders.test.ts</files>
  <read_first>
    - src/_actions/_audit.test.ts (the established static source-assertion test pattern — readFileSync + regex over the action source; THIS is the harness style to copy, since there is no DB-mock harness in this repo)
    - src/_actions/orders.ts (current state — so the test asserts against the real target file)
    - .planning/phases/06-orders-transactions/06-VALIDATION.md (per-task verification map; tests use `npm run test:run`)
    - vitest.config.ts (include glob is `src/**/*.{test,spec}.{ts,tsx}`, globals enabled)
  </read_first>
  <behavior>
    - Test reads src/_actions/orders.ts via readFileSync (mirror _audit.test.ts), isolates the updateOrderStatusAction and approveOrderItemAction export blocks using the exportBlocks split helper.
    - updateOrderStatusAction block asserts: contains `db.insert(transactions)`; the insert is guarded by a `=== "completed"` check; description uses `O.S. #` with `orderNumber`; sets `category: "Serviço"`, `type: "income"`, `status: "paid"`, `serviceOrderId`; uses `.returning(` capturing `orderNumber` and `totalAmount`; sets `updatedAt: new Date()`; revalidates `/orders`, the `/orders/${...}` detail path, `/finance`, `/analytics`.
    - approveOrderItemAction block asserts: re-queries serviceOrderItems filtered by `approved` true; computes total with `Number(` on unitPrice and `.reduce(`; calls `db.update(serviceOrders)` with `totalAmount: String(` and `updatedAt: new Date()`; revalidates the `/orders/${...}/budget` path and the `/orders/${...}` detail path.
  </behavior>
  <action>Create src/_actions/orders.test.ts following the _audit.test.ts pattern exactly: `import { readFileSync } from "node:fs"`, resolve the path to `src/_actions/orders.ts` via `dirname(fileURLToPath(import.meta.url))` + `join`, and reuse the `exportBlocks(source)` regex-split approach (matchAll on `export const (\w+)\s*=`). Write a `describe("Phase 6 orders actions wiring (D-01..D-06)")` with one `it` per concern listed in `<behavior>`. Each assertion uses `expect(block.body.includes("..."))` or `expect(block.body).toMatch(/.../)` with a descriptive failure message as the second arg, matching the _audit.test.ts style. These tests will FAIL initially (RED) because orders.ts does not yet contain `db.insert(transactions)` or the totalAmount recalc — that is expected before Tasks 2 and 3. Do NOT use a live DB or mock the pg driver; this repo verifies actions by static source assertion (DB persistence is manual-verify per VALIDATION.md). Do not place fenced code inside any action file.</action>
  <verify>
    <automated>npm run test:run -- src/_actions/orders.test.ts 2>&1 | grep -qE "FAIL|failed" && echo "RED-as-expected" || echo "check"; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File src/_actions/orders.test.ts exists and is picked up by vitest (matches include glob).
    - `npx tsc --noEmit` exits 0 (test file type-checks).
    - Running the test before Tasks 2/3 reports failures for the transaction-insert and totalAmount-recalc assertions (RED state confirms the test actually checks the new behavior).
    - Test reuses the readFileSync + exportBlocks source-assertion pattern from _audit.test.ts (no DB connection, no pg mock).
  </acceptance_criteria>
  <done>orders.test.ts exists with one `it` per D-01..D-06 concern, type-checks clean, and currently fails on the not-yet-implemented assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: updateOrderStatusAction — atomic transaction insert on close (D-01, D-02, D-05)</name>
  <files>src/_actions/orders.ts</files>
  <read_first>
    - src/_actions/orders.ts (current updateOrderStatusAction is lines 108-127; createOrderAction lines 68-105 shows the `.returning()` + `db.insert` analog to copy)
    - src/_db/schema/transactions.ts (column names/enums: date, description, category, type=income, amount numeric string, status=paid, serviceOrderId FK)
    - .planning/phases/06-orders-transactions/06-PATTERNS.md (exact import line to add and the guarded-insert shape)
    - .planning/phases/06-orders-transactions/06-CONTEXT.md (D-01, D-02, D-05 verbatim field list)
  </read_first>
  <behavior>
    - When status !== "completed": status/closedAt(null)/updatedAt updated; NO transactions row inserted.
    - When status === "completed": serviceOrders updated with closedAt=new Date() and updatedAt=new Date(); exactly one transactions row inserted with description `O.S. #<orderNumber>`, category "Serviço", type "income", amount = order.totalAmount (string), status "paid", serviceOrderId = order.id, date = new Date().
    - Action revalidates /orders, /orders/${id}, /finance, /analytics after all DB writes.
  </behavior>
  <action>Modify the import on line 8 from `{ serviceOrderItems, serviceOrders, vehicles }` to add `transactions` (alphabetical: `serviceOrderItems, serviceOrders, transactions, vehicles`). In updateOrderStatusAction, change the existing `db.update(serviceOrders)...where(eq(serviceOrders.id, parsedInput.id))` to destructure `const [order] = await db.update(...)` and append `.returning({ id: serviceOrders.id, orderNumber: serviceOrders.orderNumber, totalAmount: serviceOrders.totalAmount })`. After the update, add a guarded block `if (parsedInput.status === "completed")` that calls `db.insert(transactions).values({ date: new Date(), description: \`O.S. #${order.orderNumber}\`, category: "Serviço", type: "income", amount: order.totalAmount, status: "paid", serviceOrderId: order.id })` per D-02. Keep `closedAt: parsedInput.status === "completed" ? new Date() : null` and `updatedAt: new Date()` in the set clause. Expand the trailing revalidatePath calls (currently `/orders` + `/orders/${parsedInput.id}`) to also call `revalidatePath("/finance")` and `revalidatePath("/analytics")` per D-05, placed AFTER the insert. Preserve the `authActionClient` wrapper and the existing Zod `.schema()`. The `=== "completed"` guard is mandatory (Pitfall 5: prevents duplicate transaction rows on status toggling).</action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:run -- src/_actions/orders.test.ts 2>&1 | grep -i "updateOrderStatus"</automated>
  </verify>
  <acceptance_criteria>
    - src/_actions/orders.ts line 8 imports `transactions` from `@/_db/schema`.
    - updateOrderStatusAction contains `db.insert(transactions)` inside a block guarded by `parsedInput.status === "completed"`.
    - The update uses `.returning(` capturing orderNumber and totalAmount; `const [order]` destructure present.
    - Action calls `revalidatePath("/finance")` and `revalidatePath("/analytics")` in addition to the two existing paths.
    - `npx tsc --noEmit` exits 0.
    - The updateOrderStatusAction-related assertions in orders.test.ts now pass.
    - `_audit.test.ts` still passes (action remains authActionClient + .schema, export count unchanged at 9).
  </acceptance_criteria>
  <done>Closing an O.S. atomically inserts a paid income transaction with the order total and number; non-completed status changes insert nothing; finance/analytics paths revalidate; type-check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: approveOrderItemAction — recalc totalAmount from approved items (D-03, D-04, D-06)</name>
  <files>src/_actions/orders.ts</files>
  <read_first>
    - src/_actions/orders.ts (current approveOrderItemAction is lines 146-161; needs `and` import added on line 3)
    - src/_data-access/orders.ts (items select-from-serviceOrderItems pattern and the Number()/String() numeric convention)
    - .planning/phases/06-orders-transactions/06-PATTERNS.md (combined re-query + reduce + update shape)
    - .planning/phases/06-orders-transactions/06-CONTEXT.md (D-03, D-04, D-06 verbatim)
  </read_first>
  <behavior>
    - After updating the item's `approved` flag, re-query all serviceOrderItems for the order where approved = true.
    - newTotal = sum of quantity * Number(unitPrice) across those approved items.
    - serviceOrders.totalAmount set to String(newTotal) with updatedAt: new Date().
    - Approving an item increases the total by that item's line; rejecting (approved=false) removes it from the total.
    - Revalidates both /orders/${orderId}/budget and /orders/${orderId}.
  </behavior>
  <action>Change the import on line 3 from `import { eq } from "drizzle-orm"` to `import { and, eq } from "drizzle-orm"`. In approveOrderItemAction, keep the existing `db.update(serviceOrderItems).set({ approved }).where(eq(serviceOrderItems.id, parsedInput.itemId))`. After it, add a re-query per D-04: `const items = await db.select().from(serviceOrderItems).where(and(eq(serviceOrderItems.serviceOrderId, parsedInput.orderId), eq(serviceOrderItems.approved, true)))`. Compute `const newTotal = items.reduce((s, i) => s + i.quantity * Number(i.unitPrice), 0)` — `Number()` is required because unitPrice is a numeric(12,2) JS string. Then `await db.update(serviceOrders).set({ totalAmount: String(newTotal), updatedAt: new Date() }).where(eq(serviceOrders.id, parsedInput.orderId))` per D-03 (updatedAt mandatory for OS-03). Keep the existing `revalidatePath(\`/orders/${parsedInput.orderId}/budget\`)` and add `revalidatePath(\`/orders/${parsedInput.orderId}\`)` per D-06, both after all DB writes. Preserve the authActionClient wrapper and Zod schema. Discretion (D allows it): the reduce-in-JS strategy is chosen here over a sql SUM expression because it matches createOrderAction's existing reduce convention.</action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:run -- src/_actions/orders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/_actions/orders.ts line 3 imports `and` and `eq` from `drizzle-orm`.
    - approveOrderItemAction re-queries serviceOrderItems with `and(...approved true...)` and computes total via `.reduce(` with `Number(i.unitPrice)`.
    - Action calls `db.update(serviceOrders).set({ totalAmount: String(...), updatedAt: new Date() })`.
    - Action calls `revalidatePath` for both the budget path and the `/orders/${orderId}` detail path.
    - `npx tsc --noEmit` exits 0.
    - Full `npm run test:run -- src/_actions/orders.test.ts` is green (all D-01..D-06 assertions pass).
    - `_audit.test.ts` still passes.
  </acceptance_criteria>
  <done>Approving/rejecting a budget item recalculates and persists totalAmount from approved items only, with updatedAt set; both order paths revalidate; full orders.test.ts green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                             |
| ------------------------ | ----------------------------------------------------------------------- |
| Client → Server Action   | Untrusted itemId/orderId/status/approved values cross into DB mutations |
| Server Action → Postgres | totalAmount/amount written as numeric strings; status enum constrained  |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                              | Disposition | Mitigation Plan                                                                                                                                                                                                                 |
| --------- | ---------------------- | ---------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-01   | Elevation of Privilege | updateOrderStatusAction, approveOrderItemAction                        | mitigate    | `authActionClient` wrapper rejects unauthenticated calls — preserved on every export (enforced by \_audit.test.ts)                                                                                                              |
| T-06-02   | Tampering              | id/orderId/itemId inputs                                               | mitigate    | `z.uuid()` validation on all id fields; status constrained to `z.enum([...])`                                                                                                                                                   |
| T-06-03   | Tampering              | Duplicate transaction on status toggle (completed → other → completed) | accept      | `=== "completed"` guard limits to completed transitions; repeated completed transitions inserting a second row is a documented known limitation (CONTEXT.md deferred / RESEARCH Open Question 1) — low value, low risk for v1.1 |
| T-06-04   | Tampering              | totalAmount integrity                                                  | mitigate    | totalAmount derived server-side from approved items via re-query — never accepted from client input                                                                                                                             |
| T-06-SC   | Tampering              | npm/pip/cargo installs                                                 | mitigate    | No new packages installed this phase (RESEARCH Package Legitimacy Audit: N/A); no install task exists                                                                                                                           |

</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after each task.
- `npm run test:run` full suite green (orders.test.ts + _audit.test.ts + existing suites).
- Manual smoke (per VALIDATION.md, gate before /gsd-verify-work): create O.S. → approve items (total updates on detail) → set status completed → inspect DB: one new `transactions` row with type=income, status=paid, amount=order total, description `O.S. #<n>`, serviceOrderId set; navigate /finance to confirm it appears.
</verification>

<success_criteria>

- OS-01: status updates persist (existing) and revalidate; list reflects change after revalidation.
- OS-02: closing an O.S. inserts exactly one correct `transactions` row.
- OS-03: every serviceOrders update (status change AND budget approval) sets `updatedAt: new Date()`.
- D-01..D-06 all implemented and asserted by orders.test.ts.
  </success_criteria>

<output>
Create `.planning/phases/06-orders-transactions/06-01-SUMMARY.md` when done.
</output>
