---
phase: 03-usabilidade-usability
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/_hooks/use-update-order-status.ts
  - src/app/(dashboard)/orders/_components/OrdersClient.tsx
  - src/app/(dashboard)/orders/[id]/budget/_components/BudgetClient.tsx
  - src/app/(dashboard)/orders/[id]/budget/page.tsx
autonomous: true
requirements: [USAB-05, USAB-06]
must_haves:
  truths:
    - "Mudar o status de uma O.S. na lista chama updateOrderStatusAction e o chip atualiza com toast"
    - "O chip de status reflete a mudança imediatamente (optimistic) antes da confirmação do servidor"
    - "Aprovar/reprovar um item no orçamento chama approveOrderItemAction e confirma com toast"
    - "O total aprovado recalcula após a aprovação (via revalidação da rota)"
  artifacts:
    - path: "src/_hooks/use-update-order-status.ts"
      provides: "Hook useOptimisticAction para mudança de status"
      contains: "useOptimisticAction"
    - path: "src/app/(dashboard)/orders/[id]/budget/_components/BudgetClient.tsx"
      provides: "Botões de aprovação de item wired a approveOrderItemAction"
      contains: "approveOrderItemAction"
      min_lines: 30
    - path: "src/app/(dashboard)/orders/[id]/budget/page.tsx"
      provides: "Server Component delegando interatividade ao BudgetClient"
      contains: "BudgetClient"
  key_links:
    - from: "src/app/(dashboard)/orders/_components/OrdersClient.tsx"
      to: "updateOrderStatusAction"
      via: "useOptimisticAction (hook use-update-order-status)"
      pattern: "use-update-order-status|useOptimisticAction"
    - from: "src/app/(dashboard)/orders/[id]/budget/_components/BudgetClient.tsx"
      to: "approveOrderItemAction"
      via: "useAction execute"
      pattern: "useAction\\(approveOrderItemAction"
---

<objective>
Wire as duas ações existentes que ainda não têm UI: `updateOrderStatusAction` (mudança de status com UI otimista na lista de O.S.) e `approveOrderItemAction` (aprovação de itens no orçamento, via split Server/Client da budget page).

Purpose: USAB-05 (feedback) e USAB-06 (optimistic UI) — únicos requisitos da fase que exigem UI otimista. Depende de 03-01 porque `createOrderAction` no mesmo arquivo `orders.ts` deve estar estável antes de tocarmos consumidores de orders.

Output: `use-update-order-status.ts` (novo hook otimista) + `OrdersClient.tsx` com seletor de status por linha + `BudgetClient.tsx` (novo) + `budget/page.tsx` delegando botões interativos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-usabilidade-usability/03-RESEARCH.md
@.planning/phases/03-usabilidade-usability/03-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Status otimista da O.S. (hook + seletor na lista)</name>
  <files>src/_hooks/use-update-order-status.ts, src/app/(dashboard)/orders/_components/OrdersClient.tsx</files>
  <read_first>
    - src/app/(dashboard)/orders/_components/OrdersClient.tsx (coluna "status" usa StatusChip; OrderRow["status"] = "pending" | "in_progress" | "completed" | "delayed")
    - src/_actions/orders.ts (updateOrderStatusAction: { id: z.uuid(), status: enum["pending","in_progress","completed","delayed"] })
    - src/_components/ui/status-chip.tsx (StatusChip — props de status)
    - .planning/phases/03-usabilidade-usability/03-RESEARCH.md (Pattern 5 — useOptimisticAction signature)
    - src/_hooks/use-appointment-form.ts (analog de useAction; mesma família de hooks de next-safe-action)
  </read_first>
  <behavior>
    - O hook expõe execute + optimisticState a partir de useOptimisticAction(updateOrderStatusAction).
    - currentState inicializa com o status atual da O.S.; updateFn retorna { status: input.status }.
    - Ao selecionar um novo status, optimisticState.status muda imediatamente (antes da resposta do servidor) e o StatusChip reflete isso.
    - onSuccess: toast.success("Status atualizado."); onError: toast.error(fallback) e rollback automático do useOptimisticAction.
  </behavior>
  <action>
    Criar `src/_hooks/use-update-order-status.ts` (`"use client"`):
    1. Imports: `useOptimisticAction` de `next-safe-action/hooks`, `toast` de `sonner`, `updateOrderStatusAction` de `@/_actions/orders`.
    2. Tipar o status como o enum literal de `updateOrderStatusAction` (reusar `OrderRow["status"]` de `@/_data-access/orders` ou definir o union "pending"|"in_progress"|"completed"|"delayed").
    3. `export function useUpdateOrderStatus(orderId: string, initialStatus: Status) { const { execute, optimisticState } = useOptimisticAction(updateOrderStatusAction, { currentState: { status: initialStatus }, updateFn: (_state, input) => ({ status: input.status }), onSuccess: () => toast.success("Status atualizado."), onError: ({ error }) => toast.error(error.serverError ?? "Erro ao atualizar status.") }); return { execute, optimisticStatus: optimisticState.status }; }`. Ajustar a forma exata de `currentState`/`updateFn` conforme a assinatura real verificada no read_first.
    Em `src/app/(dashboard)/orders/_components/OrdersClient.tsx`:
    4. Criar um pequeno componente cliente por linha (ex: `OrderStatusCell`) que usa `useUpdateOrderStatus(row.id, row.status)` e renderiza um `<select>`/menu com as 4 opções de status; ao mudar, chama `execute({ id: row.id, status: novoStatus })` e exibe `<StatusChip status={optimisticStatus} />`. Substituir a célula da coluna "status" (hoje `<StatusChip status={row.status} />`) por esse componente.
    5. Usar Controller apenas se for react-hook-form; aqui é um select simples controlado — usar `value`/`onChange` nativo com tokens do design system.
    Sem `any`/`as unknown`. Não usar `isSubmitting`.
  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/Oficina-Mecanica" && npx tsc --noEmit && grep -q "useOptimisticAction" src/_hooks/use-update-order-status.ts && grep -Eq "use-update-order-status|useUpdateOrderStatus" "src/app/(dashboard)/orders/_components/OrdersClient.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `use-update-order-status.ts` existe e contém `useOptimisticAction(updateOrderStatusAction`.
    - `OrdersClient.tsx` usa `useUpdateOrderStatus` e renderiza StatusChip com o status otimista.
    - O seletor chama `execute({ id, status })` com um valor do enum válido.
    - `npx tsc --noEmit` retorna zero erros.
  </acceptance_criteria>
  <done>Mudar status na lista atualiza o chip imediatamente e confirma via toast; tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split Server/Client da budget page para aprovação de itens</name>
  <files>src/app/(dashboard)/orders/[id]/budget/_components/BudgetClient.tsx, src/app/(dashboard)/orders/[id]/budget/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/orders/[id]/budget/page.tsx (Server Component async; itens renderizados linhas ~118-156; botão "Confirmar Aprovação" linha ~208)
    - src/_actions/orders.ts (approveOrderItemAction: { itemId: z.uuid(), approved: z.boolean(), orderId: z.uuid() })
    - src/app/(dashboard)/inventory/purchase-orders/new/_components/NewPurchaseOrderClient.tsx (analog useAction em componente sem react-hook-form)
    - .planning/phases/03-usabilidade-usability/03-PATTERNS.md (seção "BudgetClient.tsx" — ApproveItemButton e ConfirmApprovalButton, com classes exatas)
  </read_first>
  <behavior>
    - ApproveItemButton recebe itemId, orderId, approved; ao clicar chama execute({ itemId, orderId, approved: !approved }).
    - onSuccess: toast.success(approved ? "Item reprovado." : "Item aprovado."); onError: toast.error(fallback).
    - ConfirmApprovalButton recebe orderId + itemIds[]; ao clicar aprova todos os pendentes (execute por item, approved: true) e toast de sucesso.
    - Ambos os botões desabilitam enquanto status === "executing".
    - Após sucesso, a rota /orders/[id]/budget é revalidada (a action já chama revalidatePath) e os totais recalculam no re-render do Server Component.
  </behavior>
  <action>
    Criar `src/app/(dashboard)/orders/[id]/budget/_components/BudgetClient.tsx` (`"use client"`):
    1. Imports: `useAction` de `next-safe-action/hooks`, `toast` de `sonner`, `approveOrderItemAction` de `@/_actions/orders`, ícones do lucide-react.
    2. Export `ApproveItemButton({ itemId, orderId, approved }: { itemId: string; orderId: string; approved: boolean })` — `useAction(approveOrderItemAction, { onSuccess: () => toast.success(approved ? "Item reprovado." : "Item aprovado."), onError: ({ error }) => toast.error(error.serverError ?? "Erro ao atualizar item.") })`; `<button disabled={status === "executing"} onClick={() => execute({ itemId, orderId, approved: !approved })}>` com as classes do toggle do PATTERNS.md (border-status-completed / hover error etc) e ícone CheckCircle quando aprovado.
    3. Export `ConfirmApprovalButton({ orderId, itemIds }: { orderId: string; itemIds: string[] })` — `useAction(approveOrderItemAction, { onSuccess, onError })`; `onClick={() => itemIds.forEach((itemId) => execute({ itemId, orderId, approved: true }))}`; label `status === "executing" ? "Confirmando..." : "Confirmar Aprovação"` com as classes do botão primário do PATTERNS.md.
    Em `src/app/(dashboard)/orders/[id]/budget/page.tsx` (mantém Server Component):
    4. Importar `{ ApproveItemButton, ConfirmApprovalButton }` de `./_components/BudgetClient`.
    5. Substituir o `<div>` estático de checkbox/toggle de cada item (linhas ~125-135) por `<ApproveItemButton itemId={item.id} orderId={id} approved={item.approved} />`.
    6. Substituir o `<button>...Confirmar Aprovação</button>` estático (linha ~208) por `<ConfirmApprovalButton orderId={id} itemIds={pendingItems.map((i) => i.id)} />`.
    7. NÃO adicionar hooks (useState/useAction) na `page.tsx` — ela permanece Server Component async.
    Sem `any`/`as unknown`.
  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/Oficina-Mecanica" && npx tsc --noEmit && test -f "src/app/(dashboard)/orders/[id]/budget/_components/BudgetClient.tsx" && grep -q "useAction(approveOrderItemAction" "src/app/(dashboard)/orders/[id]/budget/_components/BudgetClient.tsx" && grep -q "ConfirmApprovalButton" "src/app/(dashboard)/orders/[id]/budget/page.tsx" && ! grep -q "useState\|useAction" "src/app/(dashboard)/orders/[id]/budget/page.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `BudgetClient.tsx` existe, é `"use client"`, exporta `ApproveItemButton` e `ConfirmApprovalButton`, contém `useAction(approveOrderItemAction`.
    - `budget/page.tsx` importa e usa ambos os componentes e NÃO contém `useState`/`useAction` (segue Server Component).
    - Cada item da lista renderiza `ApproveItemButton`; o card de ação renderiza `ConfirmApprovalButton`.
    - `npx tsc --noEmit` retorna zero erros.
  </acceptance_criteria>
  <done>Aprovar item no orçamento chama approveOrderItemAction, confirma com toast e recalcula o total aprovado via revalidação; tsc verde.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` e `npm run lint` zero erros.
- `npx vitest run` verde após a wave.
- Fluxo manual: mudar status de O.S. → chip muda na hora + toast; aprovar item → toast + total aprovado recalcula.
</verification>

<success_criteria>

- USAB-05: mudança de status e aprovação de item confirmam com toast.
- USAB-06: o chip de status usa UI otimista (atualiza antes da resposta do servidor).
  </success_criteria>

<output>
Create `.planning/phases/03-usabilidade-usability/03-04-SUMMARY.md` when done
</output>
