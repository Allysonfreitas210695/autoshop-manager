---
phase: 03-usabilidade-usability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/_actions/orders.ts
  - src/app/(dashboard)/orders/new/order-wizard.tsx
autonomous: true
requirements: [USAB-03, USAB-05]
must_haves:
  truths:
    - "Concluir o wizard de O.S. chama createOrderAction (não setTimeout) e cria a O.S. no banco"
    - "createOrderAction aceita plate/customerName/vehicleModel e cria o vehicle inline antes da serviceOrder"
    - "Após sucesso, toast de confirmação aparece e a rota /orders é revalidada"
    - "Botão 'Gerar O.S.' fica desabilitado enquanto status === 'executing'"
  artifacts:
    - path: "src/_actions/orders.ts"
      provides: "createOrderAction com schema plate/customerName/vehicleModel + insert de vehicle inline"
      contains: "vehicleModel"
    - path: "src/app/(dashboard)/orders/new/order-wizard.tsx"
      provides: "handleFinalSubmit chamando execute() de useAction(createOrderAction)"
      contains: "useAction"
  key_links:
    - from: "src/app/(dashboard)/orders/new/order-wizard.tsx"
      to: "createOrderAction"
      via: "useAction execute em handleFinalSubmit"
      pattern: "useAction\\(createOrderAction"
    - from: "src/_actions/orders.ts"
      to: "db.insert(vehicles)"
      via: "criação inline do vehicle antes da serviceOrder"
      pattern: "insert\\(vehicles\\)"
---

<objective>
Wire a criação de Ordem de Serviço de ponta a ponta: corrigir o schema de `createOrderAction` para aceitar os dados que o wizard realmente coleta (placa + dados do veículo, não um UUID), criar o registro de `vehicle` inline na action, e substituir o stub `setTimeout`/`console.log` de `handleFinalSubmit` no wizard por uma chamada real via `useAction`.

Purpose: Hoje concluir o wizard NÃO persiste nada — `handleFinalSubmit` faz `setTimeout(800ms)` + `console.log`. Além disso, `createOrderAction` exige `vehicleId: z.uuid()` que o wizard nunca fornece. Esta é a raiz do problema de usabilidade da Fase 3 para O.S. (USAB-05). A correção do schema é pré-requisito do Plano 03-04.

Output: `createOrderAction` com schema reescrito + insert de vehicle inline; `order-wizard.tsx` chamando a action via `useAction` com toast de sucesso/erro e botão desabilitado durante execução.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-usabilidade-usability/03-RESEARCH.md
@.planning/phases/03-usabilidade-usability/03-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever schema e action de createOrderAction (criar vehicle inline)</name>
  <files>src/_actions/orders.ts</files>
  <read_first>
    - src/_actions/orders.ts (estado atual: schema com vehicleId: z.uuid() nas linhas 13-33; insert nas linhas 41-74)
    - src/_db/schema/vehicles.ts (plate/make/model são NOT NULL; ownerId é nullable; year/color/mileage/vin nulláveis)
    - src/_actions/inventory.ts (createPartAction — analog exato de estrutura de action authActionClient)
    - .planning/phases/03-usabilidade-usability/03-PATTERNS.md (seção "src/_actions/orders.ts — schema fix")
  </read_first>
  <action>
    Em `createOrderAction` (src/_actions/orders.ts):
    1. Substituir o campo `vehicleId: z.uuid()` no `.schema()` por três campos string: `plate: z.string().min(6).max(8)`, `customerName: z.string().min(2)`, `vehicleModel: z.string().min(2)`. Manter `customerId`, `mechanicId`, `clientReport`, `diagnosis`, `serviceType` como opcionais; manter `priority: z.string()` (sem `.default()` — o input vem preenchido); manter `dueAt` opcional; manter o array `items` (sem `.default([])` interno problemático — manter como está).
    2. Adicionar `vehicles` ao import existente de `@/_db/schema` (linha 8 atualmente importa `serviceOrderItems, serviceOrders`).
    3. No corpo do `.action(async ({ parsedInput }) => {...})`, ANTES de inserir a serviceOrder, criar o vehicle inline: `const [vehicle] = await db.insert(vehicles).values({ plate: parsedInput.plate.toUpperCase(), make: "Não informado", model: parsedInput.vehicleModel }).returning({ id: vehicles.id });`. NÃO informar `ownerId` (é nullable). `make` é obrigatório no schema do DB — usar o literal "Não informado".
    4. Trocar `vehicleId: parsedInput.vehicleId` no insert de serviceOrders por `vehicleId: vehicle.id`.
    5. Manter `revalidatePath("/orders")` e o retorno `{ id: order.id, orderNumber: order.orderNumber }`.
    Sem `any`/`as unknown`. Não remover `updateOrderStatusAction` nem `approveOrderItemAction`.
  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/Oficina-Mecanica" && npx tsc --noEmit && grep -q "insert(vehicles)" src/_actions/orders.ts && grep -q "vehicleModel" src/_actions/orders.ts && ! grep -q "vehicleId: z.uuid()" src/_actions/orders.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/_actions/orders.ts` NÃO contém mais a string `vehicleId: z.uuid()`.
    - `src/_actions/orders.ts` contém `insert(vehicles)` e cria o vehicle com `make: "Não informado"`.
    - O `.schema()` de `createOrderAction` contém os campos `plate`, `customerName`, `vehicleModel`.
    - O insert de `serviceOrders` usa `vehicleId: vehicle.id` (id retornado do insert do vehicle).
    - `npx tsc --noEmit` retorna zero erros.
  </acceptance_criteria>
  <done>createOrderAction aceita plate/customerName/vehicleModel, cria vehicle inline e insere a serviceOrder com o id retornado; tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire order-wizard handleFinalSubmit para createOrderAction via useAction</name>
  <files>src/app/(dashboard)/orders/new/order-wizard.tsx</files>
  <read_first>
    - src/app/(dashboard)/orders/new/order-wizard.tsx (estado atual: handleFinalSubmit com setTimeout/console.log; botão "Gerar O.S.")
    - src/_hooks/use-appointment-form.ts (analog canônico de useAction + onSuccess/onError)
    - src/app/(dashboard)/inventory/purchase-orders/new/_components/NewPurchaseOrderClient.tsx (analog de useAction em componente sem react-hook-form)
    - src/_schemas/order-wizard.ts (step1: customerName/plate/vehicleModel; step2: customerReport/initialDiagnosis/serviceType/priority; step3: parts[]{name,quantity,unitPrice}, laborItems[]{description,price})
    - .planning/phases/03-usabilidade-usability/03-PATTERNS.md (seção "order-wizard.tsx")
  </read_first>
  <behavior>
    - Ao concluir o último step, `execute` de useAction(createOrderAction) é chamado com os campos mapeados de wizardData (plate, customerName, vehicleModel, clientReport, diagnosis, serviceType, priority, items[]).
    - parts de step3 viram items com itemType "part" (description=name, quantity, unitPrice); laborItems viram items com itemType "labor" (description, quantity 1, unitPrice=price).
    - onSuccess dispara toast.success com o orderNumber retornado e navega para /orders.
    - onError dispara toast.error com error.serverError (com fallback "Erro ao criar O.S.").
    - Enquanto status === "executing", o botão final fica desabilitado e o label muda para "Gerando O.S....".
  </behavior>
  <action>
    Em `src/app/(dashboard)/orders/new/order-wizard.tsx`:
    1. Adicionar imports: `import { useAction } from "next-safe-action/hooks";` e `import { createOrderAction } from "@/_actions/orders";` (toast de "sonner" já está importado — confirmar; se não, adicionar).
    2. Após o `useState` de estado do wizard, adicionar: `const { execute, status } = useAction(createOrderAction, { onSuccess: ({ data }) => { toast.success(\`O.S. #${data?.orderNumber} criada com sucesso.\`); router.push("/orders"); }, onError: ({ error }) => { toast.error(error.serverError ?? "Erro ao criar O.S."); } });`
    3. Substituir o corpo de `handleFinalSubmit` (que hoje faz setTimeout + console.log): mapear `wizardData.step3.parts` para `{ description: p.name, itemType: "part" as const, quantity: p.quantity, unitPrice: p.unitPrice }` e `wizardData.step3.laborItems` para `{ description: l.description, itemType: "labor" as const, quantity: 1, unitPrice: l.price }`; chamar `execute({ plate: wizardData.step1.plate ?? "", customerName: wizardData.step1.customerName ?? "", vehicleModel: wizardData.step1.vehicleModel ?? "", clientReport: wizardData.step2.customerReport, diagnosis: wizardData.step2.initialDiagnosis, serviceType: wizardData.step2.serviceType, priority: wizardData.step2.priority ?? "normal", items: [...parts, ...labor] })`. Remover qualquer `setTimeout`/`console.log`/estado de "submitted" simulado relacionado a esse submit.
    4. No botão final "Gerar O.S.", adicionar `disabled={isFinalStep && status === "executing"}` e label condicional: `status === "executing" ? "Gerando O.S...." : "Gerar O.S."`. NÃO usar `isSubmitting` do react-hook-form para o estado de loading deste submit.
    Sem `any`/`as unknown`. Tipar os mapeamentos de parts/labor conforme os tipos de step3.
  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/Oficina-Mecanica" && npx tsc --noEmit && grep -q "useAction(createOrderAction" src/app/\(dashboard\)/orders/new/order-wizard.tsx && ! grep -q "setTimeout" src/app/\(dashboard\)/orders/new/order-wizard.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `order-wizard.tsx` contém `useAction(createOrderAction`.
    - `order-wizard.tsx` NÃO contém mais `setTimeout` no fluxo de submit final.
    - `handleFinalSubmit` chama `execute(...)` com `items` derivado de `step3.parts` + `step3.laborItems`.
    - Botão final usa `status === "executing"` para `disabled` e label.
    - `npx tsc --noEmit` retorna zero erros.
  </acceptance_criteria>
  <done>Concluir o wizard chama createOrderAction, mostra toast e navega para /orders; botão desabilita durante execução; tsc verde.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` zero erros.
- `npm run lint` zero erros nos arquivos tocados.
- Fluxo manual: preencher o wizard até o fim → toast "O.S. #... criada" → /orders mostra a nova O.S. sem reload manual.
</verification>

<success_criteria>

- USAB-05: criação de O.S. via wizard chama a server action real e confirma com toast.
- createOrderAction persiste vehicle + serviceOrder + items e revalida /orders.
  </success_criteria>

<output>
Create `.planning/phases/03-usabilidade-usability/03-01-SUMMARY.md` when done
</output>
