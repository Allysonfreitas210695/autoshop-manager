---
phase: 03-usabilidade-usability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/_hooks/use-new-part-form.ts
  - src/app/(dashboard)/inventory/new/_components/NewPartForm.tsx
  - src/app/(dashboard)/inventory/_components/UpdateStockDialog.tsx
  - src/app/(dashboard)/inventory/inventory-client.tsx
autonomous: true
requirements: [USAB-03, USAB-05]
must_haves:
  truths:
    - "Cadastrar peça chama createPartAction (não console.log) e a lista /inventory atualiza"
    - "Campos inválidos no formulário de peça mostram erro inline"
    - "Editar a quantidade de uma peça na tabela chama updateStockAction e confirma com toast"
    - "Botões de submit/salvar desabilitam enquanto status === 'executing'"
  artifacts:
    - path: "src/_hooks/use-new-part-form.ts"
      provides: "Hook chamando createPartAction via useAction (sem setTimeout/console.log)"
      contains: "useAction"
    - path: "src/app/(dashboard)/inventory/_components/UpdateStockDialog.tsx"
      provides: "Edição inline de estoque via updateStockAction"
      contains: "updateStockAction"
      min_lines: 30
    - path: "src/app/(dashboard)/inventory/inventory-client.tsx"
      provides: "Coluna/ação que abre o UpdateStockDialog por linha"
      contains: "UpdateStockDialog"
  key_links:
    - from: "src/_hooks/use-new-part-form.ts"
      to: "createPartAction"
      via: "useAction execute em onSubmit"
      pattern: "useAction\\(createPartAction"
    - from: "src/app/(dashboard)/inventory/_components/UpdateStockDialog.tsx"
      to: "updateStockAction"
      via: "useAction execute"
      pattern: "useAction\\(updateStockAction"
---

<objective>
Wire o módulo de inventário: corrigir `use-new-part-form.ts` (hoje `console.log` + `setTimeout`) para chamar `createPartAction` via `useAction`, ajustando o `NewPartForm.tsx` para usar `status` em vez de `isSubmitting`; e adicionar edição inline de estoque por linha na tabela chamando `updateStockAction`.

Purpose: USAB-05 (feedback de criação/edição) e USAB-03 (validação inline) para inventário. Cadastrar peça hoje não persiste, e `updateStockAction` existe mas nenhuma UI o chama.

Output: hook corrigido + NewPartForm ajustado + `UpdateStockDialog.tsx` novo + `inventory-client.tsx` com gatilho de edição por linha.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-usabilidade-usability/03-RESEARCH.md
@.planning/phases/03-usabilidade-usability/03-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire use-new-part-form a createPartAction e ajustar NewPartForm</name>
  <files>src/_hooks/use-new-part-form.ts, src/app/(dashboard)/inventory/new/_components/NewPartForm.tsx</files>
  <read_first>
    - src/_hooks/use-new-part-form.ts (estado atual: onSubmit com console.log + setTimeout linhas 50-57; retorna isSubmitting/submitted)
    - src/app/(dashboard)/inventory/new/_components/NewPartForm.tsx (consome o hook; usa isSubmitting/submitted nas linhas 19-44, 265, 331)
    - src/_hooks/use-appointment-form.ts (analog canônico useAction)
    - src/_actions/inventory.ts (createPartAction: name, description?, sku?, price, stockQuantity, minStock)
    - .planning/phases/03-usabilidade-usability/03-PATTERNS.md (seção "use-new-part-form.ts" — mapeamento unitPrice→price, stock→stockQuantity)
  </read_first>
  <behavior>
    - onSubmit com dados válidos chama execute() de useAction(createPartAction) mapeando: name, sku, price=data.unitPrice, stockQuantity=data.stock, minStock; category/supplier/location NÃO são enviados (action não os aceita).
    - onSuccess: toast.success("Peça cadastrada com sucesso."), reset() (se aplicável) e router.push("/inventory").
    - onError: toast.error(error.serverError ?? "Erro ao cadastrar peça.").
    - O formulário usa status === "executing" para desabilitar o botão (não isSubmitting).
    - Campos inválidos continuam renderizando errors inline (comportamento já presente — manter).
  </behavior>
  <action>
    Em `src/_hooks/use-new-part-form.ts`:
    1. Adicionar imports: `import { useAction } from "next-safe-action/hooks";`, `import { toast } from "sonner";`, `import { createPartAction } from "@/_actions/inventory";`.
    2. Remover o estado `submitted` (`useState(false)`) e o `setTimeout`. Manter `useRouter`.
    3. Adicionar `const { execute, status } = useAction(createPartAction, { onSuccess: () => { toast.success("Peça cadastrada com sucesso."); router.push("/inventory"); }, onError: ({ error }) => { toast.error(error.serverError ?? "Erro ao cadastrar peça."); } });`.
    4. Reescrever `onSubmit` para `execute({ name: data.name, sku: data.sku, price: data.unitPrice, stockQuantity: data.stock, minStock: data.minStock })`. NÃO enviar `category`, `supplier`, `location`.
    5. No retorno do hook, substituir `isSubmitting` por `status` e remover `submitted`. Manter os demais valores (`register, handleSubmit, control, errors, stockValue, unitPriceValue, nameValue, categoryValue, totalValue, onSubmit, goToInventory`).
    Em `src/app/(dashboard)/inventory/new/_components/NewPartForm.tsx`:
    6. Trocar a desestruturação para usar `status` em vez de `isSubmitting`; remover o uso de `submitted` e o bloco de retorno de tela "Peça cadastrada!" (linhas ~30-44) — o feedback agora é via toast + navegação.
    7. Nos dois botões "Cadastrar Peça" (mobile linha ~265 e desktop linha ~331), trocar `disabled={isSubmitting}` por `disabled={status === "executing"}` e `{isSubmitting ? "Salvando..." : "Cadastrar Peça"}` por `{status === "executing" ? "Salvando..." : "Cadastrar Peça"}`.
    Sem `any`/`as unknown`.
  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/Oficina-Mecanica" && npx tsc --noEmit && grep -q "useAction(createPartAction" src/_hooks/use-new-part-form.ts && ! grep -q "setTimeout" src/_hooks/use-new-part-form.ts && ! grep -q "isSubmitting" "src/app/(dashboard)/inventory/new/_components/NewPartForm.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `use-new-part-form.ts` contém `useAction(createPartAction` e NÃO contém `setTimeout` nem estado `submitted`.
    - `onSubmit` mapeia `unitPrice→price` e `stock→stockQuantity` e não envia category/supplier/location.
    - `NewPartForm.tsx` NÃO referencia mais `isSubmitting` nem `submitted`.
    - `npx tsc --noEmit` retorna zero erros.
  </acceptance_criteria>
  <done>Cadastro de peça chama createPartAction, confirma com toast e navega; botões usam status; tsc verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar UpdateStockDialog e ligar à tabela de inventário</name>
  <files>src/app/(dashboard)/inventory/_components/UpdateStockDialog.tsx, src/app/(dashboard)/inventory/inventory-client.tsx</files>
  <read_first>
    - src/app/(dashboard)/inventory/inventory-client.tsx (tabela com columns DataTableColumn<Part>; Part tem id, name, sku, stock, minStock, unitPrice)
    - src/_actions/inventory.ts (updateStockAction: { id: z.uuid(), stockQuantity: z.number().int().min(0) })
    - src/app/(dashboard)/appointments/_components/NewAppointmentDrawer.tsx (analog Sheet/Dialog + Base UI render prop)
    - src/_components/ui/sheet.tsx ou src/_components/ui/dialog.tsx (componente de overlay disponível — usar o que existir; verificar qual está presente)
    - .planning/phases/03-usabilidade-usability/03-PATTERNS.md (Padrão 1 useAction + toast)
  </read_first>
  <behavior>
    - O dialog recebe id, name e stock atual de uma peça; um input numérico permite editar a quantidade (defaultValue = stock atual).
    - Salvar chama execute() de useAction(updateStockAction) com { id, stockQuantity: novoValor }.
    - onSuccess: toast.success("Estoque atualizado.") e fecha o dialog.
    - onError: toast.error(error.serverError ?? "Erro ao atualizar estoque.").
    - Botão salvar desabilita enquanto status === "executing".
    - Quantidade negativa não é submetida (input min 0; validação simples antes do execute).
  </behavior>
  <action>
    Criar `src/app/(dashboard)/inventory/_components/UpdateStockDialog.tsx` como `"use client"`:
    1. Props: `{ partId: string; partName: string; currentStock: number; open: boolean; onClose: () => void }`.
    2. Estado local `const [qty, setQty] = useState(currentStock);` (input numérico controlado, `min={0}`, `inputMode="numeric"`).
    3. `const { execute, status } = useAction(updateStockAction, { onSuccess: () => { toast.success("Estoque atualizado."); onClose(); }, onError: ({ error }) => { toast.error(error.serverError ?? "Erro ao atualizar estoque."); } });`
    4. Botão salvar: `disabled={status === "executing"}`, onClick `() => execute({ id: partId, stockQuantity: Math.max(0, Number(qty) || 0) })`; label `status === "executing" ? "Salvando..." : "Salvar"`.
    5. Usar o componente de overlay existente no projeto (Sheet) com Base UI `render` prop para botões de fechar — NUNCA `asChild`. Aplicar tokens do design system (font-mono, text-on-surface-variant).
    Em `src/app/(dashboard)/inventory/inventory-client.tsx`:
    6. Adicionar estado `const [editingPart, setEditingPart] = useState<Part | null>(null);`.
    7. Adicionar uma coluna de ação à tabela (`DataTableColumn<Part>`) com um botão (ícone, ex: Pencil do lucide-react) cujo `onClick` faz `setEditingPart(row)`.
    8. Renderizar `<UpdateStockDialog partId={editingPart.id} partName={editingPart.name} currentStock={editingPart.stock} open={editingPart !== null} onClose={() => setEditingPart(null)} />` quando `editingPart` não for null (render condicional).
    Sem `any`/`as unknown`. Import nomeado `UpdateStockDialog`.
  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/Oficina-Mecanica" && npx tsc --noEmit && test -f "src/app/(dashboard)/inventory/_components/UpdateStockDialog.tsx" && grep -q "useAction(updateStockAction" "src/app/(dashboard)/inventory/_components/UpdateStockDialog.tsx" && grep -q "UpdateStockDialog" "src/app/(dashboard)/inventory/inventory-client.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `UpdateStockDialog.tsx` existe, exporta `UpdateStockDialog` e contém `useAction(updateStockAction`.
    - `inventory-client.tsx` importa e renderiza `UpdateStockDialog` controlado por `editingPart`.
    - O dialog não usa `asChild` (Base UI render prop).
    - `npx tsc --noEmit` retorna zero erros.
  </acceptance_criteria>
  <done>Editar estoque por linha chama updateStockAction, confirma com toast e revalida /inventory; tsc verde.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` e `npm run lint` zero erros.
- Fluxo manual: cadastrar peça → toast + /inventory atualiza; editar estoque de uma linha → toast + valor atualiza.
</verification>

<success_criteria>

- USAB-05: criação de peça e atualização de estoque confirmam com toast.
- USAB-03: validação inline preservada no formulário de peça.
  </success_criteria>

<output>
Create `.planning/phases/03-usabilidade-usability/03-03-SUMMARY.md` when done
</output>
