---
phase: 03-usabilidade-usability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/customers/_components/NewCustomerDrawer.tsx
  - src/app/(dashboard)/customers/customers-client.tsx
autonomous: true
requirements: [USAB-03, USAB-05]
must_haves:
  truths:
    - "Clicar 'Cadastrar Novo Cliente' abre um drawer (não navega para /orders/new)"
    - "Submeter o drawer chama createCustomerAction e mostra toast de sucesso"
    - "Campos inválidos mostram erro inline (react-hook-form errors) abaixo do campo"
    - "Após sucesso, a lista /customers é revalidada e o drawer fecha"
  artifacts:
    - path: "src/app/(dashboard)/customers/_components/NewCustomerDrawer.tsx"
      provides: "Drawer de criação de cliente com useAction(createCustomerAction)"
      contains: "createCustomerAction"
      min_lines: 60
    - path: "src/app/(dashboard)/customers/customers-client.tsx"
      provides: "Botão que abre o drawer via estado local"
      contains: "NewCustomerDrawer"
  key_links:
    - from: "src/app/(dashboard)/customers/customers-client.tsx"
      to: "NewCustomerDrawer"
      via: "estado drawerOpen + render do componente"
      pattern: "NewCustomerDrawer"
    - from: "src/app/(dashboard)/customers/_components/NewCustomerDrawer.tsx"
      to: "createCustomerAction"
      via: "useAction execute em onSubmit"
      pattern: "useAction\\(createCustomerAction"
---

<objective>
Corrigir o fluxo de cadastro de cliente: hoje o botão "Cadastrar Novo Cliente" em `customers-client.tsx` navega para `/orders/new` (rota errada) e nenhum formulário de cliente existe. Criar `NewCustomerDrawer.tsx` (padrão Sheet, cópia direta do `NewAppointmentDrawer`) wired a `createCustomerAction`, e trocar o botão para abrir esse drawer.

Purpose: USAB-05 (feedback de criação) e USAB-03 (validação inline) para o módulo de clientes. O bug de navegação impede qualquer cadastro de cliente.

Output: `NewCustomerDrawer.tsx` novo + `customers-client.tsx` corrigido para abrir o drawer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-usabilidade-usability/03-RESEARCH.md
@.planning/phases/03-usabilidade-usability/03-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar NewCustomerDrawer.tsx wired a createCustomerAction</name>
  <files>src/app/(dashboard)/customers/_components/NewCustomerDrawer.tsx</files>
  <read_first>
    - src/app/(dashboard)/appointments/_components/NewAppointmentDrawer.tsx (analog exato — estrutura Sheet/SheetContent/SheetHeader/SheetFooter, Controller, SheetClose com render prop, validação inline)
    - src/_actions/customers.ts (createCustomerAction: name min 2, email válido, phone/cpf/address opcionais)
    - src/_components/ui/sheet.tsx (Sheet, SheetClose, SheetContent, SheetDescription, SheetFooter, SheetHeader, SheetTitle)
    - .planning/phases/03-usabilidade-usability/03-PATTERNS.md (seção "NewCustomerDrawer.tsx" e Padrões 1-4)
  </read_first>
  <behavior>
    - Schema local zod: name (min 2), email (email válido), phone/cpf/address opcionais. SEM .default() em campos zod — usar defaultValues no useForm.
    - Campos obrigatórios inválidos (name vazio, email inválido) populam formState.errors e renderizam <p className="text-label-xs text-error"> abaixo do campo.
    - Submeter com dados válidos chama execute() de useAction(createCustomerAction) com phone/cpf/address mapeados para undefined quando vazios.
    - onSuccess: toast.success("Cliente cadastrado com sucesso."), reset(), onClose().
    - onError: toast.error(error.serverError ?? "Erro ao cadastrar cliente.").
    - Botão de submit fica disabled quando status === "executing" com label "Salvando...".
  </behavior>
  <action>
    Criar `src/app/(dashboard)/customers/_components/NewCustomerDrawer.tsx` como `"use client"`, replicando a estrutura de `NewAppointmentDrawer.tsx`:
    1. Props: `{ open: boolean; onClose: () => void }`.
    2. Schema local `z.object({ name: z.string().min(2, "Nome deve ter ao menos 2 caracteres."), email: z.string().email("E-mail inválido."), phone: z.string().optional(), cpf: z.string().optional(), address: z.string().optional() })`; `type CustomerFormData = z.infer<...>`.
    3. `useForm<CustomerFormData>({ resolver: zodResolver(schema), defaultValues: { name: "", email: "", phone: "", cpf: "", address: "" } })`.
    4. `const { execute, status, result } = useAction(createCustomerAction, { onSuccess: () => { toast.success("Cliente cadastrado com sucesso."); reset(); onClose(); }, onError: ({ error }) => { toast.error(error.serverError ?? "Erro ao cadastrar cliente."); } });`
    5. `onSubmit(data)`: `execute({ name: data.name, email: data.email, phone: data.phone || undefined, cpf: data.cpf || undefined, address: data.address || undefined })`.
    6. JSX: `<Sheet open={open} onOpenChange={(o) => { if (!o) onClose(); }}>` → `<SheetContent side="right" className="bg-surface w-full overflow-y-auto sm:max-w-lg">` com SheetHeader (título "Novo Cliente" + ícone User do lucide-react), form com inputs `register("name")`, `register("email")`, `register("phone")`, `register("cpf")`, `register("address")`, cada um seguido de bloco de erro `{errors.campo && <p className="text-label-xs text-error">{errors.campo.message}</p>}`. Usar `<Label>` para cada campo (tokens do design system: text-on-surface-variant, font-mono).
    7. SheetFooter: `<SheetClose render={<Button variant="outline" onClick={onClose}>Cancelar</Button>} />` (render prop — NUNCA asChild) e `<Button onClick={handleSubmit(onSubmit)} disabled={status === "executing"}>{status === "executing" ? "Salvando..." : "Cadastrar Cliente"}</Button>`.
    Sem `any`/`as unknown`. Export nomeado `NewCustomerDrawer`.
  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/Oficina-Mecanica" && npx tsc --noEmit && test -f "src/app/(dashboard)/customers/_components/NewCustomerDrawer.tsx" && grep -q "useAction(createCustomerAction" "src/app/(dashboard)/customers/_components/NewCustomerDrawer.tsx" && grep -q "render={" "src/app/(dashboard)/customers/_components/NewCustomerDrawer.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `NewCustomerDrawer.tsx` existe e exporta `NewCustomerDrawer`.
    - Contém `useAction(createCustomerAction` e callbacks onSuccess/onError com toast.
    - Usa `render={...}` no SheetClose (não `asChild`).
    - Cada campo obrigatório tem bloco de erro inline com `errors.<campo>.message`.
    - `npx tsc --noEmit` retorna zero erros.
  </acceptance_criteria>
  <done>Drawer de cliente funcional com validação inline, toast e reset/close em sucesso; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Trocar botão de customers-client para abrir o drawer</name>
  <files>src/app/(dashboard)/customers/customers-client.tsx</files>
  <read_first>
    - src/app/(dashboard)/customers/customers-client.tsx (botão atual com router.push("/orders/new") por volta da linha 126)
    - src/app/(dashboard)/appointments/appointments-client.tsx (analog: useState drawerOpen + render do drawer no final do JSX)
    - .planning/phases/03-usabilidade-usability/03-PATTERNS.md (seção "customers-client.tsx — fix rota errada")
  </read_first>
  <action>
    Em `src/app/(dashboard)/customers/customers-client.tsx`:
    1. Importar o novo componente: `import { NewCustomerDrawer } from "./_components/NewCustomerDrawer";`.
    2. Adicionar estado local junto aos outros useState: `const [drawerOpen, setDrawerOpen] = useState(false);`.
    3. Localizar o botão "Cadastrar Novo Cliente" que faz `onClick={() => router.push("/orders/new")}` e trocar por `onClick={() => setDrawerOpen(true)}`. Se `router` ficar sem uso após a troca, remover o import/uso de `useRouter` para não quebrar o lint.
    4. Renderizar `<NewCustomerDrawer open={drawerOpen} onClose={() => setDrawerOpen(false)} />` no final do JSX retornado (antes do fechamento do container raiz).
    Sem `any`/`as unknown`.
  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/Oficina-Mecanica" && npx tsc --noEmit && npm run lint && grep -q "NewCustomerDrawer" "src/app/(dashboard)/customers/customers-client.tsx" && ! grep -q 'push("/orders/new")' "src/app/(dashboard)/customers/customers-client.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `customers-client.tsx` renderiza `<NewCustomerDrawer ... />` e usa `setDrawerOpen(true)` no botão.
    - `customers-client.tsx` NÃO contém mais `push("/orders/new")`.
    - `npx tsc --noEmit` e `npm run lint` retornam zero erros (sem variável `router` não usada).
  </acceptance_criteria>
  <done>Botão abre o drawer de cliente; rota errada removida; tsc + lint verdes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` e `npm run lint` zero erros.
- Fluxo manual: clicar "Cadastrar Novo Cliente" → drawer abre → submeter inválido mostra erros inline → submeter válido → toast + /customers atualiza.
</verification>

<success_criteria>

- USAB-05: cadastro de cliente confirma com toast e atualiza a lista.
- USAB-03: validação inline consistente no drawer de cliente.
  </success_criteria>

<output>
Create `.planning/phases/03-usabilidade-usability/03-02-SUMMARY.md` when done
</output>
