---
phase: 03-usabilidade-usability
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/loading.tsx
  - src/app/(dashboard)/error.tsx
  - src/app/(dashboard)/orders/loading.tsx
  - src/app/(dashboard)/orders/error.tsx
  - src/app/(dashboard)/customers/loading.tsx
  - src/app/(dashboard)/customers/error.tsx
  - src/app/(dashboard)/inventory/loading.tsx
  - src/app/(dashboard)/inventory/error.tsx
  - src/app/(dashboard)/appointments/loading.tsx
  - src/app/(dashboard)/appointments/error.tsx
autonomous: true
requirements: [USAB-01, USAB-02, USAB-04]
must_haves:
  truths:
    - "Cada segmento de rota do dashboard mostra um skeleton durante o carregamento (não tela em branco)"
    - "Erros em segmentos do dashboard renderizam mensagem amigável com botão de retry (não stack/branco)"
    - "Listas/tabelas sem dados mostram empty state (emptyMessage no DataTable verificado em todas as listas)"
  artifacts:
    - path: "src/app/(dashboard)/loading.tsx"
      provides: "Skeleton global do dashboard"
      contains: "Skeleton"
    - path: "src/app/(dashboard)/error.tsx"
      provides: "Error boundary global do dashboard com unstable_retry"
      contains: "unstable_retry"
    - path: "src/app/(dashboard)/orders/loading.tsx"
      provides: "Skeleton de orders"
      contains: "Skeleton"
    - path: "src/app/(dashboard)/orders/error.tsx"
      provides: "Error boundary de orders"
      contains: "unstable_retry"
  key_links:
    - from: "src/app/(dashboard)/loading.tsx"
      to: "src/_components/ui/skeleton.tsx"
      via: "import do componente Skeleton"
      pattern: 'from "@/_components/ui/skeleton"'
---

<objective>
Adicionar os arquivos de convenção do Next.js 16 ausentes: `loading.tsx` (Server Component com Skeleton) e `error.tsx` (`"use client"` com `unstable_retry`) em cada segmento do dashboard (raiz, orders, customers, inventory, appointments). Verificar que todas as listas/tabelas têm empty state via `emptyMessage` do DataTable.

Purpose: USAB-01 (loading states), USAB-02 (error states), USAB-04 (empty states). Hoje NENHUM `loading.tsx`/`error.tsx` existe no projeto, então toda página de fetch mostra flash em branco e erros derrubam a tela.

Output: 10 arquivos novos (5 loading + 5 error) + verificação de empty states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-usabilidade-usability/03-RESEARCH.md
@.planning/phases/03-usabilidade-usability/03-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar loading.tsx em todos os segmentos do dashboard</name>
  <files>src/app/(dashboard)/loading.tsx, src/app/(dashboard)/orders/loading.tsx, src/app/(dashboard)/customers/loading.tsx, src/app/(dashboard)/inventory/loading.tsx, src/app/(dashboard)/appointments/loading.tsx</files>
  <read_first>
    - src/_components/ui/skeleton.tsx (componente Skeleton existente — export nomeado { Skeleton })
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/loading.md (convenção loading.tsx Next.js 16)
    - .planning/phases/03-usabilidade-usability/03-PATTERNS.md (seção "loading.tsx — todos os segmentos", padrão canônico)
  </read_first>
  <action>
    Criar 5 arquivos `loading.tsx` (raiz do dashboard + orders + customers + inventory + appointments), cada um Server Component (SEM `"use client"`), replicando o padrão canônico do PATTERNS.md:
    1. `import { Skeleton } from "@/_components/ui/skeleton";`.
    2. `export default function <Segmento>Loading() { return (<div className="space-y-4 p-6"><Skeleton className="h-8 w-48" /><Skeleton className="h-10 w-full" />{Array.from({ length: 5 }).map((_, i) => (<Skeleton key={i} className="h-16 w-full rounded-xl" />))}</div>); }`.
    3. Nome do export default descreve o segmento: `DashboardLoading`, `OrdersLoading`, `CustomersLoading`, `InventoryLoading`, `AppointmentsLoading`.
    4. Apenas markup de esqueleto — sem lógica, sem estado, sem hooks.
    Sem `any`/`as unknown`.
  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/Oficina-Mecanica" && npx tsc --noEmit && C=$(find "src/app/(dashboard)" -name "loading.tsx" | wc -l) && test "$C" -eq 5 && ! grep -rl '"use client"' "src/app/(dashboard)"/loading.tsx "src/app/(dashboard)"/*/loading.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Existem exatamente 5 arquivos `loading.tsx` nos segmentos: dashboard raiz, orders, customers, inventory, appointments.
    - Nenhum `loading.tsx` contém `"use client"`.
    - Cada um importa `Skeleton` de `@/_components/ui/skeleton`.
    - `npx tsc --noEmit` retorna zero erros.
  </acceptance_criteria>
  <done>Todos os segmentos do dashboard mostram skeleton durante o carregamento; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Criar error.tsx em todos os segmentos + verificar empty states</name>
  <files>src/app/(dashboard)/error.tsx, src/app/(dashboard)/orders/error.tsx, src/app/(dashboard)/customers/error.tsx, src/app/(dashboard)/inventory/error.tsx, src/app/(dashboard)/appointments/error.tsx</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md (convenção error.tsx Next.js 16 — prop é unstable_retry, NÃO reset)
    - .planning/phases/03-usabilidade-usability/03-PATTERNS.md (seção "error.tsx — todos os segmentos", padrão canônico)
    - src/app/(dashboard)/orders/_components/OrdersClient.tsx (já passa emptyMessage no DataTable)
    - src/app/(dashboard)/inventory/inventory-client.tsx (já passa emptyMessage no DataTable)
    - src/app/(dashboard)/customers/customers-client.tsx (já passa emptyMessage no DataTable)
  </read_first>
  <action>
    Criar 5 arquivos `error.tsx` (mesmos segmentos), cada um `"use client"` (OBRIGATÓRIO — React Error Boundary), replicando o padrão canônico do PATTERNS.md:
    1. Primeira linha `"use client";`.
    2. `export default function <Segmento>Error({ error, unstable_retry }: { error: Error & { digest?: string }; unstable_retry: () => void })`. Usar `unstable_retry` — NUNCA `reset` (Next.js 16 renomeou).
    3. Markup: container centralizado com mensagem "Ocorreu um erro inesperado." (tokens `text-on-surface-variant`, `font-mono`), bloco condicional `{process.env.NODE_ENV === "development" && <p className="text-error font-mono text-xs">{error.message}</p>}`, e botão `onClick={unstable_retry}` com texto "Tentar novamente" (`text-secondary font-mono text-sm underline`).
    4. Nome do export descreve o segmento: `DashboardError`, `OrdersError`, `CustomersError`, `InventoryError`, `AppointmentsError`.
    Verificação de empty states (USAB-04):
    5. Confirmar (grep) que `OrdersClient.tsx`, `inventory-client.tsx` e `customers-client.tsx` passam `emptyMessage` ao DataTable. Se alguma lista/tabela tocada nesta fase NÃO tiver `emptyMessage`, adicionar uma mensagem em pt-BR (ex: "Nenhum registro encontrado."). NÃO criar componente novo — usar o prop existente do DataTable.
    Sem `any`/`as unknown`.
  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/Oficina-Mecanica" && npx tsc --noEmit && npm run lint && C=$(find "src/app/(dashboard)" -name "error.tsx" | wc -l) && test "$C" -eq 5 && ! grep -rl "reset" "src/app/(dashboard)"/error.tsx "src/app/(dashboard)"/*/error.tsx && grep -rlq "unstable_retry" "src/app/(dashboard)"/error.tsx && grep -q "emptyMessage" "src/app/(dashboard)/orders/_components/OrdersClient.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Existem exatamente 5 arquivos `error.tsx` nos segmentos do dashboard.
    - Cada `error.tsx` começa com `"use client"` e usa a prop `unstable_retry` (nenhum contém a prop `reset`).
    - `error.message` só é exibido quando `NODE_ENV === "development"`.
    - As tabelas de orders/inventory/customers passam `emptyMessage` ao DataTable.
    - `npx tsc --noEmit` e `npm run lint` retornam zero erros.
  </acceptance_criteria>
  <done>Todos os segmentos têm error boundary amigável com retry e empty states verificados; tsc + lint verdes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` e `npm run lint` zero erros.
- Fluxo manual: navegar para cada segmento → skeleton aparece no load; forçar erro → mensagem amigável + retry; lista vazia → empty state.
</verification>

<success_criteria>

- USAB-01: loading.tsx em todos os segmentos do dashboard.
- USAB-02: error.tsx (use client, unstable_retry) em todos os segmentos.
- USAB-04: empty states verificados em todas as tabelas via emptyMessage.
  </success_criteria>

<output>
Create `.planning/phases/03-usabilidade-usability/03-05-SUMMARY.md` when done
</output>
