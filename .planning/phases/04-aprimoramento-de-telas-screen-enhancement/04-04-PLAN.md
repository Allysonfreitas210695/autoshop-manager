---
phase: 04-aprimoramento-de-telas-screen-enhancement
plan: 04
type: execute
wave: 2
depends_on: ["04-02", "04-03"]
files_modified:
  - src/app/(dashboard)/appointments/appointments-client.tsx
  - src/app/(dashboard)/inventory/purchase-orders/_components/PurchaseOrdersClient.tsx
  - src/app/(dashboard)/inventory/alerts/_components/AlertsClient.tsx
  - src/app/(dashboard)/analytics/_components/AnalyticsClient.tsx
autonomous: true

requirements: [SCRN-07]

must_haves:
  truths:
    - "Status indicators on these four screens use StatusChip where its status set maps cleanly, OR a documented justification exists in the SUMMARY where StatusChip's fixed status set does not fit"
    - "Data labels, codes, prices, and numbers on these screens use font-mono per the design system"
    - "No hardcoded hex color literal is used for UI chrome in className styling on these screens (system color tokens used instead); chart-data color palettes are the only allowed hex and carry an explanatory comment"
    - "Page titles use text-headline-md and section titles use text-title-md/text-title-sm consistently"
  artifacts:
    - path: "src/_components/ui/status-chip.tsx"
      provides: "Canonical StatusChip visual contract referenced by polished screens"
      contains: "statusChipVariants"
  key_links:
    - from: "src/app/(dashboard)/inventory/purchase-orders/_components/PurchaseOrdersClient.tsx"
      to: "StatusChip or design-system tokens"
      via: "status rendering uses StatusChip or the StatusChip visual contract with tokens, not ad-hoc hex"
      pattern: "font-mono"
---

<objective>
Run the SCRN-07 design-system polish pass (D-07, D-08) over the four status-indicator-heavy screens — appointments, purchase-orders, alerts, analytics: apply font-mono to labels/codes/numbers, use StatusChip where its status set fits (else normalize the inline badge to the StatusChip visual contract with tokens and justify it), replace hex UI chrome with system color tokens, and normalize heading hierarchy.

Purpose: First half of the SCRN-07 consistency pass — the screens with the densest status/badge usage. The remaining dashboard screens and the locked completion gate are handled in 04-05.
Output: These four screens consistent with the Industrial Precision design system; status badges canonicalized or justified; tsc clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-aprimoramento-de-telas-screen-enhancement/04-CONTEXT.md
@src/_components/ui/status-chip.tsx
@src/_components/dashboard/metric-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish status badges + tokens — appointments and purchase-orders</name>
  <files>src/app/(dashboard)/appointments/appointments-client.tsx, src/app/(dashboard)/inventory/purchase-orders/_components/PurchaseOrdersClient.tsx</files>
  <read_first>
    - src/_components/ui/status-chip.tsx (FULL — `StatusChip` accepts status: pending|in_progress|completed|delayed plus a `label` override and `className`; base style `rounded-full px-2.5 py-0.5 font-mono text-label-sm font-bold uppercase tracking-wider`. Its fixed status set does NOT include scheduled/confirmed/cancelled/draft/sent/received — see the action rule)
    - src/app/(dashboard)/appointments/appointments-client.tsx (inline status badge in AppointmentCard lines ~98-102 using `rounded-full border px-2 py-1 font-mono text-[10px] font-bold tracking-wider uppercase ${statusColor[...]}`; AppointmentBadge lines ~70-83; statusColor/statusDot maps lines ~50-64)
    - src/app/(dashboard)/inventory/purchase-orders/_components/PurchaseOrdersClient.tsx (FULL — statusLabels/statusColor maps lines ~12-26, inline status badge line ~170-176, table header/cells)
  </read_first>
  <action>
    Decision rule for status badges (apply consistently across this plan): where a domain status maps cleanly onto StatusChip's set (pending/in_progress/completed/delayed) use `<StatusChip status={...} label={...} />`. Where the domain set does NOT fit (appointment statuses scheduled/confirmed/cancelled/completed; PO statuses draft/sent/received/cancelled), DO NOT force a wrong mapping — instead normalize the inline badge to the StatusChip VISUAL contract (`rounded-full`, `font-mono`, `uppercase`, `tracking-wider`, `font-bold`) using system color tokens, and record in the SUMMARY why StatusChip was not used (its status union is too narrow for these domains). This is a justified deviation, not a skip. Do NOT widen the StatusChip status union in this plan (that would be a shared-component change affecting other screens; out of scope here).

    appointments-client.tsx: ensure both the AppointmentCard status pill and the AppointmentBadge already follow the visual contract (they largely do — `font-mono`, `uppercase`, `tracking-wider`). Verify the color classes are all system tokens (`status-completed`, `status-pending`, `error`, `secondary`, `on-surface-variant`) — they are; make no color regressions. Confirm the page `<h1>` uses `text-headline-md` (line ~189) and section titles use `text-title-md`/`text-title-sm`. Apply font-mono to any numeric/code label not already mono. If no change is warranted on a sub-element, leave it and note it in the SUMMARY.

    PurchaseOrdersClient.tsx: normalize the inline status badge (line ~170-176) to the StatusChip visual contract with tokens (it already uses `rounded-full ... font-mono ... uppercase tracking-wider`; ensure `statusColor` uses only token classes, no hex). Ensure OC codes, supplier names where appropriate, dates, and the "Valor Total" column carry font-mono. Normalize `<h1>` to `text-headline-md` and section headings to `text-title-md`/`text-title-sm`.

    No `any`/`as unknown`. Base UI components use the `render` prop, never `asChild`.

  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && npx tsc --noEmit 2>&1 | tail -5; echo "--- hex UI-chrome (expect 0) ---"; for f in "src/app/(dashboard)/appointments/appointments-client.tsx" "src/app/(dashboard)/inventory/purchase-orders/_components/PurchaseOrdersClient.tsx"; do grep -n 'className="[^"]*#[0-9a-fA-F]\{6\}' "$f" | grep -v '^[[:space:]]*//'; done | wc -l; echo "--- PO badge visual contract ---"; grep -c "tracking-wider" "src/app/(dashboard)/inventory/purchase-orders/_components/PurchaseOrdersClient.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors
    - Zero hex color literals inside `className` strings on both files (combined grep count == 0)
    - The PO status badge carries the StatusChip visual contract markers (grep `tracking-wider` >= 1)
    - SUMMARY records, per screen, whether StatusChip was adopted or a justified inline-badge normalization was applied
    - No `: any` or `as unknown` added
  </acceptance_criteria>
  <done>Appointments and purchase-orders status indicators are canonicalized (StatusChip or justified visual-contract normalization), font-mono and heading hierarchy applied, no hex UI chrome.</done>
</task>

<task type="auto">
  <name>Task 2: Polish status badges + tokens + typography — alerts and analytics</name>
  <files>src/app/(dashboard)/inventory/alerts/_components/AlertsClient.tsx, src/app/(dashboard)/analytics/_components/AnalyticsClient.tsx</files>
  <read_first>
    - src/_components/ui/status-chip.tsx (visual contract reference — see Task 1)
    - src/app/(dashboard)/inventory/alerts/_components/AlertsClient.tsx (FULL — CRÍTICO/ATENÇÃO summary cards lines ~37-80 and the per-row severity badge lines ~144-148; all colors are `error`/`tertiary`/`secondary`/`status-completed` tokens already)
    - src/app/(dashboard)/analytics/_components/AnalyticsClient.tsx (FULL — KPI cards lines ~104-128; the service-category `colors` array lines ~255-262 uses hex `#adc6ff` etc; the chart legend swatches use token classes already)
  </read_first>
  <action>
    AlertsClient.tsx: the CRÍTICO/ATENÇÃO severities do NOT map to StatusChip's status set (they are inventory-severity, not order-status). Per the Task 1 decision rule, keep the inline severity badges but ensure they follow the StatusChip visual contract (`rounded-full`, `font-mono`, `uppercase`, `tracking-wider`, `font-bold`) using only system color tokens (error/tertiary). The summary cards and AlertRow already use tokens — verify and tighten any label/number not yet `font-mono`. Normalize `<h1>` to `text-headline-md` and section `<h2>` to `text-title-md`/`text-title-sm`. Record the StatusChip-not-applicable justification in the SUMMARY.

    AnalyticsClient.tsx: the inline KPI cards (lines ~104-128) are bespoke metric tiles; do NOT force-migrate them to `MetricCard` (MetricCard's icon set and props do not match the analytics KPIs — that is a redesign, out of scope). Instead ensure KPI values/labels use font-mono where they are numeric/technical (the values already use `font-mono`), and that heading hierarchy is consistent (`text-headline-md` page title, `text-title-sm`/`text-title-md` section titles — already present; verify). Documented exception: the service-category `colors` array (lines ~255-262) and any Recharts series colors are DATA-visualization palettes, not UI chrome — leave them as hex BUT add a one-line code comment above the array noting they are chart-data colors. Do not break the charts.

    No `any`/`as unknown`. Do not redesign either screen; this is a consistency pass, not a rewrite.

  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && npx tsc --noEmit 2>&1 | tail -5; echo "--- hex UI-chrome in alerts (expect 0) ---"; grep -n 'className="[^"]*#[0-9a-fA-F]\{6\}' "src/app/(dashboard)/inventory/alerts/_components/AlertsClient.tsx" | grep -v '^[[:space:]]*//' | wc -l; echo "--- analytics chart-color comment present ---"; grep -ci "chart-data\|chart data\|visualization\|gráfico" "src/app/(dashboard)/analytics/_components/AnalyticsClient.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors
    - Zero hex color literals inside `className` strings in AlertsClient.tsx (grep count == 0)
    - The analytics `colors` data array has an explanatory comment marking it chart-data (grep >= 1)
    - AlertsClient severity badges retain CRÍTICO/ATENÇÃO with the StatusChip visual contract (font-mono, uppercase, tracking-wider, rounded-full)
    - SUMMARY records StatusChip-not-applicable justification for alert severities and the MetricCard-not-applicable note for analytics KPIs
    - No `: any` or `as unknown` added
  </acceptance_criteria>
  <done>Alerts and analytics carry consistent font-mono labels, token colors, and heading hierarchy; chart-data hex palettes are preserved and documented; no UI-chrome hex remains.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                                                                                        |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none new) | This plan is a presentation-only polish over already-fetched, already-authorized data. No new input crosses a trust boundary; no server action, schema, or data source is changed. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                       | Disposition | Mitigation Plan                                                                                                                                                |
| ---------- | ---------------------- | ------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-04-01 | Information Disclosure | analytics / alerts / PO screens | accept      | Renders only data already authorized for authenticated dashboard users; styling-only change introduces no new exposure.                                        |
| T-04-04-02 | Tampering              | shared StatusChip component     | accept      | This plan deliberately does NOT modify the shared StatusChip component; status-set widening is out of scope, so no cross-screen regression risk is introduced. |

</threat_model>

<verification>
- `npx tsc --noEmit` zero errors
- `npm run lint` zero errors on touched files
- The four screens render without crash; status badges, font-mono labels, and heading hierarchy are consistent
- No hex UI-chrome remains; chart-data palettes are documented exceptions
</verification>

<success_criteria>
The four status-heavy SCRN-07 screens (appointments, purchase-orders, alerts, analytics) are consistent with the Industrial Precision design system: StatusChip adopted where it fits or a justified visual-contract normalization applied, font-mono on technical labels/codes/numbers, system tokens instead of hex UI chrome, and a consistent heading hierarchy.
</success_criteria>

<output>
Create `.planning/phases/04-aprimoramento-de-telas-screen-enhancement/04-04-SUMMARY.md` when done
</output>
