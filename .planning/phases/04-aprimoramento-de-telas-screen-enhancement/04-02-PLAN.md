---
phase: 04-aprimoramento-de-telas-screen-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/appointments/appointments-client.tsx
autonomous: true
requirements: [SCRN-06]

must_haves:
  truths:
    - "The appointments view switcher offers three options: Mês (calendário), Semana, and Lista"
    - "Selecting Semana renders a 7-column grid (Dom..Sáb) for the current week with each appointment placed in its day column"
    - "The user can navigate to the previous/next week within the weekly view"
    - "Weekly view uses the same date-fns + ptBR locale and design-system tokens as the existing month view (no new calendar library)"
  artifacts:
    - path: "src/app/(dashboard)/appointments/appointments-client.tsx"
      provides: "Weekly grid view added to the existing calendar/list switcher"
      contains: "week"
  key_links:
    - from: "src/app/(dashboard)/appointments/appointments-client.tsx"
      to: "date-fns startOfWeek/endOfWeek/eachDayOfInterval"
      via: "week grid day computation"
      pattern: "eachDayOfInterval"
---

<objective>
Add a third "Semana" (weekly) view to the appointments calendar, alongside the existing month calendar and list views, implemented with the project's custom date-fns calendar — NOT react-big-calendar (D-10, D-11, D-12).

Purpose: SCRN-06 requires the appointments screen to render a working calendar. The month and list views already exist; the remaining gap is a weekly grid that shows the seven days of the current week with appointments allocated by day/time slot (D-11, D-12).
Output: `appointments-client.tsx` gains a `"week"` view in the `View` type, a "Semana" button in the switcher, week navigation (prev/next), and a 7-column weekly grid consistent with the Industrial Precision design system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-aprimoramento-de-telas-screen-enhancement/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the view switcher with a "Semana" option and week-range state</name>
  <files>src/app/(dashboard)/appointments/appointments-client.tsx</files>
  <read_first>
    - src/app/(dashboard)/appointments/appointments-client.tsx (FULL file — note: the `View` type on line 131 is `"calendar" | "list"`; the switcher buttons live in the header at lines ~197-220; existing date-fns imports at lines 3-16 already include startOfWeek/endOfWeek/eachDayOfInterval/addDays; `currentMonth`/`selectedDate`/`view` state at lines 144-148)
    - node_modules/next/dist/docs/01-app/03-api-reference/01-directives/use-client.md (this is already a "use client" component — confirm hooks usage stays client-side)
  </read_first>
  <action>
    Widen the `View` type from `"calendar" | "list"` to `"calendar" | "week" | "list"`. Add a new state value `currentWeek` initialized to `startOfWeek(today, { weekStartsOn: 0 })` (reuse the same `weekStartsOn: 0` convention already used for the month grid on lines 152-153). Add two handlers to move the week back/forward by 7 days using the already-imported `addDays` (e.g. `addDays(currentWeek, -7)` and `addDays(currentWeek, 7)`).

    In the header switcher (the `<div className="...flex rounded-lg border p-0.5">` block), insert a third `<button>` for "Semana" BETWEEN the existing "Calendário" and "Lista" buttons, mirroring their exact styling pattern: same `text-label-sm flex items-center gap-1.5 rounded-md px-3 py-1.5 font-medium transition-colors` base, the same active/inactive ternary on `view === "week"` (`bg-secondary text-on-secondary` active vs `text-on-surface-variant hover:text-on-surface` inactive). Use the `CalendarDays` icon already imported, or import `CalendarRange` from lucide-react if you prefer a distinct icon (lucide-react is already a dependency — add it to the existing import block on lines 18-28, do not create a second import statement). Label text inside `<span className="hidden sm:inline">Semana</span>` to match the existing responsive label pattern.

    Do not change the existing "calendar" or "list" rendering branches in this task. Keep the file a single default-styled component; no `any`, no `as unknown` (project rule).

  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && npx tsc --noEmit 2>&1 | tail -5; grep -c '"calendar" | "week" | "list"' "src/app/(dashboard)/appointments/appointments-client.tsx"; grep -c 'Semana' "src/app/(dashboard)/appointments/appointments-client.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - The `View` type union contains `"week"` (grep: `"calendar" | "week" | "list"` returns 1)
    - A `currentWeek` state is declared and initialized with `startOfWeek(today, { weekStartsOn: 0 })`
    - The switcher contains a button whose label span text is `Semana` (grep `Semana` >= 1)
    - Week prev/next handlers use `addDays(currentWeek, -7)` and `addDays(currentWeek, 7)` (or equivalent subWeeks/addWeeks if imported)
    - `npx tsc --noEmit` reports zero errors
    - No `any` or `as unknown` introduced (grep `as unknown` and `: any` return 0 new hits)
  </acceptance_criteria>
  <done>The appointments header shows three view buttons (Calendário, Semana, Lista) and week-range state plus prev/next handlers exist, with tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Render the weekly 7-column grid with per-day appointment slots</name>
  <files>src/app/(dashboard)/appointments/appointments-client.tsx</files>
  <read_first>
    - src/app/(dashboard)/appointments/appointments-client.tsx (the month-view branch lines ~257-370 for the visual pattern: card wrapper `bg-surface-container border-outline-variant/30 ... rounded-2xl border p-5`, the month-nav header with ChevronLeft/ChevronRight, the `weekDays` array on line 163, the `AppointmentCard` component lines 85-129, the `AppointmentBadge` component lines 70-83, the `byTime` sorter line 156-157, and `countByDate` line 165-166)
  </read_first>
  <action>
    Add a new render branch for `view === "week"`, placed BETWEEN the existing `view === "calendar"` branch and the `view === "list"` branch. Restructure the existing ternary into an explicit conditional chain (`view === "calendar" ? (...) : view === "week" ? (...) : (...)`) so all three branches render correctly. Do not delete or alter the month or list branches.

    Compute the seven days of the displayed week with `eachDayOfInterval({ start: currentWeek, end: endOfWeek(currentWeek, { weekStartsOn: 0 }) })` (both functions already imported). Render a week-nav header identical in structure to the month-nav header: a ChevronLeft button calling the week-prev handler from Task 1, a centered `<h2>` showing the week range using `format` + ptBR (e.g. `format(weekStart, "dd 'de' MMM", { locale: ptBR })` to `format(weekEnd, "dd 'de' MMM", { locale: ptBR })`), and a ChevronRight button calling the week-next handler.

    Render a 7-column grid (`grid grid-cols-7 gap-1` matching the month grid) where each column is one day. Each column header shows the `weekDays` label and the day number, reusing the design-system token classes from the month cells (font-mono, `text-label-xs`/`text-label-sm`, `text-on-surface-variant`, `isToday` highlight via `bg-secondary text-on-secondary` like line 307). Below each header, list that day's appointments sorted by `byTime` (use `countByDate(day).sort(byTime)`), rendering each as an `AppointmentBadge` (the compact badge already shows time + customer first name, which serves as the per-slot allocation D-12 requires — Claude's discretion on whether to show all or cap with a "+N mais" overflow like the month view). Use a vertical scroll wrapper per column or for the grid (`overflow-y-auto` with a max height) so dense days do not break layout, consistent with the day-detail panel pattern on line 363.

    Keep the KPI strip and the month/list branches unchanged. No new external libraries — date-fns only (D-10). No `any`/`as unknown`.

  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && npx tsc --noEmit 2>&1 | tail -5; grep -c 'view === "week"' "src/app/(dashboard)/appointments/appointments-client.tsx"; grep -c 'endOfWeek(currentWeek' "src/app/(dashboard)/appointments/appointments-client.tsx"; grep -c "react-big-calendar" "src/app/(dashboard)/appointments/appointments-client.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - A `view === "week"` render branch exists (grep returns >= 1)
    - The branch computes its days from `eachDayOfInterval` over `currentWeek`..`endOfWeek(currentWeek, ...)` (grep `endOfWeek(currentWeek` >= 1)
    - The branch renders a `grid grid-cols-7` weekly grid with prev/next week navigation wired to the Task 1 handlers
    - Appointments are placed per-day and sorted with `byTime`
    - `react-big-calendar` is NOT imported anywhere in the file (grep count == 0) per D-10
    - `npx tsc --noEmit` reports zero errors
  </acceptance_criteria>
  <done>Selecting "Semana" shows a navigable 7-column week grid with appointments allocated per day, built entirely on the existing date-fns calendar with design-system tokens.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                                                                           |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none new) | This plan is a pure client-side rendering change over already-fetched, already-authenticated appointment data passed as props. No new input crosses a trust boundary. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                              |
| ---------- | ---------------------- | --------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-02-01 | Information Disclosure | appointments-client.tsx week view | accept      | Renders only the same `initialAppointments` already authorized for the existing month/list views; no new data source or endpoint introduced. |

</threat_model>

<verification>
- `npx tsc --noEmit` zero errors
- `npm run lint` zero errors on the touched file
- Switching between Mês, Semana, and Lista renders each view without crash
- The Semana view shows the seven current-week days with appointments and supports prev/next week navigation
</verification>

<success_criteria>
The appointments screen offers a working weekly view as a third switcher option, built on the custom date-fns calendar (no react-big-calendar), consistent with the Industrial Precision design system, fully covering the remaining SCRN-06 calendar requirement.
</success_criteria>

<output>
Create `.planning/phases/04-aprimoramento-de-telas-screen-enhancement/04-02-SUMMARY.md` when done
</output>
