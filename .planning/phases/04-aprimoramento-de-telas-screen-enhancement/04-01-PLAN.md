---
phase: 04-aprimoramento-de-telas-screen-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/(public)/track/[id]/page.tsx
  - src/app/(public)/track/[id]/_components/TrackQrCode.tsx
autonomous: false
requirements: [SCRN-06]
user_setup:
  - service: vercel
    why: "Public tracking URL must resolve to the deployed domain so scanned QR codes open the live page"
    env_vars:
      - name: NEXT_PUBLIC_APP_URL
        source: "Vercel Project -> Settings -> Environment Variables (set to the production domain, e.g. https://precision-auto.vercel.app)"

must_haves:
  truths:
    - "Scanning the QR code on /track/[id] opens the full tracking URL for that order"
    - "The QR code renders a real scannable matrix, not a hardcoded CSS grid"
  artifacts:
    - path: "src/app/(public)/track/[id]/_components/TrackQrCode.tsx"
      provides: "Client QR code component encoding the tracking URL"
      contains: "use client"
    - path: "package.json"
      provides: "qrcode.react dependency"
      contains: "qrcode.react"
  key_links:
    - from: "src/app/(public)/track/[id]/page.tsx"
      to: "src/app/(public)/track/[id]/_components/TrackQrCode.tsx"
      via: "renders <TrackQrCode orderId={order.id} />"
      pattern: "TrackQrCode"
    - from: "src/app/(public)/track/[id]/_components/TrackQrCode.tsx"
      to: "qrcode.react"
      via: "import QRCodeSVG"
      pattern: 'from "qrcode.react"'
---

<objective>
Replace the hardcoded CSS-grid QR simulation on the public tracking page with a real, scannable QR code that encodes the full tracking URL for the order.

Purpose: A customer scans the QR on a printed O.S. and lands directly on the live status page on their phone (SCRN-06, D-01..D-04).
Output: `qrcode.react` installed; a `"use client"` `TrackQrCode` component; the track page wired to render it in place of the CSS grid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-aprimoramento-de-telas-screen-enhancement/04-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify qrcode.react package legitimacy before install</name>
  <what-built>No RESEARCH.md Package Legitimacy Audit exists for this phase, so qrcode.react is treated as [ASSUMED]. This checkpoint verifies the package is the genuine, widely-used React QR component before it is added to package.json.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/qrcode.react in a browser.
    2. Confirm: weekly downloads are in the millions, the repository links to github.com/zpao/qrcode.react, license is ISC/MIT, and the latest version is recent (4.x).
    3. Confirm the package exports `QRCodeSVG` (named export documented in the README).
  </how-to-verify>
  <resume-signal>Type "approved" to allow install, or describe concerns to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install qrcode.react and create the TrackQrCode client component</name>
  <files>package.json, src/app/(public)/track/[id]/_components/TrackQrCode.tsx</files>
  <read_first>
    - src/app/(public)/track/[id]/page.tsx (current CSS QR grid at the "QR code / info box" section, lines ~291-322; uses order.id)
    - src/_hooks/use-forgot-password-form.ts (line 28 shows the established `process.env.NEXT_PUBLIC_APP_URL ?? window.location.origin` fallback pattern)
    - .env.example (NEXT_PUBLIC_APP_URL declared)
    - node_modules/qrcode.react/README.md (confirm QRCodeSVG props: value, size, level, bgColor, fgColor)
  </read_first>
  <action>
    Run the package install for `qrcode.react` (use the project's package manager — npm, per package-lock.json/pnpm-lock as present) so it lands in package.json dependencies.

    Create `src/app/(public)/track/[id]/_components/TrackQrCode.tsx` as a `"use client"` component accepting prop `orderId: string`. Compute the encoded value as `${process.env.NEXT_PUBLIC_APP_URL ?? window.location.origin}/track/${orderId}` (D-02, D-03, D-04 fallback). Render `QRCodeSVG` imported from `qrcode.react` with `value` set to that URL, `size={160}` (Claude's discretion within the existing card), `level="M"`, and design-system colors: `bgColor` transparent and `fgColor` using the on-surface token value (read the resolved color from globals/Tailwind config rather than a raw hex if a token utility exists; otherwise use the same currentColor approach the old grid used). Wrap the SVG so it is centered, matching the existing card layout (mx-auto, mb-4). Because `window` is read client-side, guard the value computation so it does not throw during SSR (compute inside the component body — `"use client"` components still render once on the server; use `process.env.NEXT_PUBLIC_APP_URL` first which is available on both sides, falling back to `window.location.origin` only when `typeof window !== "undefined"`).

  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5; grep -q "qrcode.react" package.json && echo PKG_OK; grep -q "use client" "src/app/(public)/track/[id]/_components/TrackQrCode.tsx" && echo CLIENT_OK</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `qrcode.react` under dependencies
    - TrackQrCode.tsx first line is `"use client";`
    - TrackQrCode.tsx imports `QRCodeSVG` from `qrcode.react` (grep: `from "qrcode.react"`)
    - TrackQrCode.tsx computes value containing `/track/` and references both `process.env.NEXT_PUBLIC_APP_URL` and `window.location.origin`
    - `npx tsc --noEmit` reports zero errors
  </acceptance_criteria>
  <done>qrcode.react is installed and TrackQrCode renders a real QR encoding the tracking URL with SSR-safe origin fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Wire TrackQrCode into the track page, remove the CSS grid simulation</name>
  <files>src/app/(public)/track/[id]/page.tsx</files>
  <read_first>
    - src/app/(public)/track/[id]/page.tsx (the "QR code / info box" block, lines ~291-322 — the `Array.from({ length: 25 })` grid is the simulation to remove)
    - src/app/(public)/track/[id]/_components/TrackQrCode.tsx (component created in Task 2)
  </read_first>
  <action>
    In the "QR code / info box" card, delete the `<div className="... grid size-20 grid-cols-5 ...">` block and its `Array.from({ length: 25 })` mapping (the hardcoded simulation). Import `TrackQrCode` from `./_components/TrackQrCode` and render `<TrackQrCode orderId={order.id} />` in its place, keeping the surrounding card, the "Código da O.S.: {order.id}" caption, and the contact-hours footer intact. Do not change the page's Server Component nature — only this one child becomes a client component.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5; grep -q "TrackQrCode" "src/app/(public)/track/[id]/page.tsx" && echo WIRED; grep -vc '^\s*//' "src/app/(public)/track/[id]/page.tsx" >/dev/null; if grep -q "Array.from({ length: 25 })" "src/app/(public)/track/[id]/page.tsx"; then echo SIM_STILL_PRESENT; else echo SIM_REMOVED; fi
