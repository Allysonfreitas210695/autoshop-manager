---
phase: 04-aprimoramento-de-telas-screen-enhancement
plan: 05
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - src/app/(dashboard)/page.tsx
  - src/app/(dashboard)/inventory/inventory-client.tsx
  - src/app/(dashboard)/customers/customers-client.tsx
  - src/app/(dashboard)/orders/_components/OrdersClient.tsx
  - src/app/(dashboard)/finance/page.tsx
  - src/app/(dashboard)/finance/reports/page.tsx
  - src/app/(dashboard)/finance/finance-charts.tsx
autonomous: false

requirements: [SCRN-07]

must_haves:
  truths:
    - "The remaining dashboard screens (home dashboard, inventory, customers, orders, finance, finance/reports) use StatusChip where its status set fits, font-mono on labels/codes/numbers, and system color tokens for UI chrome"
    - "Status indicators that map to StatusChip's set use StatusChip; non-fitting status sets use the StatusChip visual contract with tokens and are justified in the SUMMARY"
    - "No hardcoded hex UI-chrome remains on these screens; Recharts/chart-data hex palettes are the only allowed hex and carry an explanatory comment"
    - "Page titles use text-headline-md and section titles use text-title-md/text-title-sm consistently"
    - "Locked completion gate passes: npx tsc --noEmit zero errors, npm run lint zero errors, npm run build succeeds"
  artifacts:
    - path: "src/app/(dashboard)/page.tsx"
      provides: "Home dashboard polished to the design system"
      contains: "StatusChip"
  key_links:
    - from: "src/app/(dashboard)/orders/_components/OrdersClient.tsx"
      to: "src/_components/ui/status-chip.tsx"
      via: "order status rendered via StatusChip"
      pattern: "StatusChip"
---

<objective>
Complete the SCRN-07 design-system polish pass (D-07, D-08) over the remaining dashboard screens — home dashboard, inventory, customers, orders, finance, finance/reports — then run the locked completion gate (tsc + lint + build) that closes Phase 4.

Purpose: Second half of the SCRN-07 consistency pass plus the phase's final verification gate. Together with 04-04, this makes the app visually complete and consistent across ALL dashboard screens (D-07).
Output: Remaining screens polished (StatusChip where it fits / justified normalization elsewhere, font-mono labels, token colors, heading hierarchy); locked completion gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-aprimoramento-de-telas-screen-enhancement/04-CONTEXT.md
@src/_components/ui/status-chip.tsx
@src/_components/dashboard/metric-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish home dashboard, inventory, customers, and orders screens</name>
  <files>src/app/(dashboard)/page.tsx, src/app/(dashboard)/inventory/inventory-client.tsx, src/app/(dashboard)/customers/customers-client.tsx, src/app/(dashboard)/orders/_components/OrdersClient.tsx</files>
  <read_first>
    - src/_components/ui/status-chip.tsx (FULL — status set pending|in_progress|completed|delayed; `label` + `className` overrides; visual contract `rounded-full ... font-mono ... uppercase tracking-wider font-bold`)
    - src/app/(dashboard)/page.tsx (FULL — already imports StatusChip per phase audit; confirm order-status rows use it and labels/numbers are font-mono)
    - src/app/(dashboard)/orders/_components/OrdersClient.tsx (FULL — already imports StatusChip; confirm the order status column uses StatusChip and codes/totals are font-mono)
    - src/app/(dashboard)/inventory/inventory-client.tsx (FULL — look for inline stock/status badges, SKU/price labels needing font-mono, any hex UI chrome)
    - src/app/(dashboard)/customers/customers-client.tsx (FULL — look for inline badges, plate/phone/code labels needing font-mono, heading hierarchy)
  </read_first>
  <action>
    Apply the SCRN-07 D-08 checklist using the same decision rule as 04-04: where a status maps to StatusChip's set (pending/in_progress/completed/delayed) render `<StatusChip status={...} label={...} />`; where a domain status set does NOT fit, normalize the inline badge to the StatusChip visual contract with system tokens and justify in the SUMMARY. Do NOT widen the StatusChip status union (shared-component change, out of scope).

    page.tsx and OrdersClient.tsx already import StatusChip — verify order statuses render through it; if any order-status pill is still hand-rolled, migrate it. Ensure dashboard metric labels, counts, currency, O.S. codes, and SKUs use font-mono.

    inventory-client.tsx: convert any inline stock-status badge to the StatusChip visual contract (or StatusChip if it maps), ensure SKU/price/quantity carry font-mono, replace any hex UI chrome with tokens (`text-on-surface`, `bg-surface-container`, `text-error`, `text-tertiary`, etc.).

    customers-client.tsx: ensure plates, phones, codes, and counts use font-mono; normalize any inline badge; replace hex UI chrome with tokens.

    Normalize `<h1>` to `text-headline-md` and section `<h2>`/`<h3>` to `text-title-md`/`text-title-sm` on each screen. No redesign, no feature removal. No `any`/`as unknown`. Base UI uses `render` prop, never `asChild`.

  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && npx tsc --noEmit 2>&1 | tail -5; echo "--- StatusChip on dashboard + orders ---"; grep -c "StatusChip" "src/app/(dashboard)/page.tsx" "src/app/(dashboard)/orders/_components/OrdersClient.tsx"; echo "--- hex UI-chrome (expect 0) ---"; for f in "src/app/(dashboard)/page.tsx" "src/app/(dashboard)/inventory/inventory-client.tsx" "src/app/(dashboard)/customers/customers-client.tsx" "src/app/(dashboard)/orders/_components/OrdersClient.tsx"; do grep -n 'className="[^"]*#[0-9a-fA-F]\{6\}' "$f" | grep -v '^[[:space:]]*//'; done | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors
    - page.tsx and OrdersClient.tsx render order status via StatusChip (grep `StatusChip` >= 1 each)
    - Zero hex color literals inside `className` strings across the four files (combined grep count == 0)
    - SUMMARY records, per screen, StatusChip adoption vs justified inline-badge normalization
    - No `: any` or `as unknown` added
  </acceptance_criteria>
  <done>Home dashboard, inventory, customers, and orders screens use StatusChip where it fits (or justified normalization), font-mono technical labels, token colors, and consistent heading hierarchy.</done>
</task>

<task type="auto">
  <name>Task 2: Polish finance + finance/reports screens (chart-color exception documented)</name>
  <files>src/app/(dashboard)/finance/page.tsx, src/app/(dashboard)/finance/reports/page.tsx, src/app/(dashboard)/finance/finance-charts.tsx</files>
  <read_first>
    - src/_components/ui/status-chip.tsx (visual-contract reference)
    - src/app/(dashboard)/finance/page.tsx (FULL — KPI cards, transaction rows, currency labels; check for inline badges and hex UI chrome)
    - src/app/(dashboard)/finance/reports/page.tsx (FULL — report tables/cards; check font-mono on currency/codes, heading hierarchy)
    - src/app/(dashboard)/finance/finance-charts.tsx (Recharts series colors at lines ~77-158 use hex `#adc6ff`/`#ffb690`/`#22c55e` — these are chart-data palettes, NOT UI chrome; see action)
  </read_first>
  <action>
    finance/page.tsx and finance/reports/page.tsx: apply the D-08 checklist — font-mono on currency values, transaction codes, dates, and counts; normalize any inline status/category badge to the StatusChip visual contract with system tokens (or StatusChip if a status maps); replace any hex UI chrome with tokens; normalize `<h1>` to `text-headline-md` and section titles to `text-title-md`/`text-title-sm`.

    finance-charts.tsx: the Recharts `fill`/`stroke`/`dot` hex values are DATA-visualization colors passed to chart primitives, NOT className UI chrome. Leave them as hex but add a one-line code comment near the top of the color usage noting they are Recharts chart-data colors and intentionally not tokenized (Recharts requires literal color strings). Do not break the charts.

    No redesign. No `any`/`as unknown`. Base UI uses `render` prop, never `asChild`.

  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && npx tsc --noEmit 2>&1 | tail -5; echo "--- hex UI-chrome in finance page/reports (expect 0) ---"; for f in "src/app/(dashboard)/finance/page.tsx" "src/app/(dashboard)/finance/reports/page.tsx"; do grep -n 'className="[^"]*#[0-9a-fA-F]\{6\}' "$f" | grep -v '^[[:space:]]*//'; done | wc -l; echo "--- chart-color comment present ---"; grep -ci "chart-data\|chart data\|Recharts\|gráfico\|visualization" "src/app/(dashboard)/finance/finance-charts.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors
    - Zero hex color literals inside `className` strings in finance/page.tsx and finance/reports/page.tsx (combined grep count == 0)
    - finance-charts.tsx carries a comment documenting the Recharts chart-data color exception (grep >= 1)
    - SUMMARY records per-screen findings and the documented chart-color exception
    - No `: any` or `as unknown` added
  </acceptance_criteria>
  <done>Finance and finance/reports are consistent with the design system; the Recharts color palette is preserved and documented as an intentional exception.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Locked completion gate — tsc + lint + build, then human verification of polished screens</name>
  <what-built>The full SCRN-07 polish pass is complete across all dashboard screens (04-04 + this plan). This task runs the locked completion gate and has a human confirm the visual result.</what-built>
  <how-to-verify>
    1. Run the locked gate and confirm each step succeeds:
       - `npx tsc --noEmit` → zero errors
       - `npm run lint` → zero errors
       - `npm run build` → succeeds (Next.js 16 + Turbopack)
    2. Run `npm run dev` and visit each polished route: `/` (home dashboard), `/inventory`, `/inventory/alerts`, `/inventory/purchase-orders`, `/customers`, `/orders`, `/appointments`, `/analytics`, `/finance`, `/finance/reports`.
    3. Confirm status indicators are consistent (rounded-full, font-mono, uppercase, tracking-wider), prices/codes/numbers use font-mono, colors look correct (no broken/black/transparent badges from a bad token swap), and charts still render with their colors intact.
    4. Confirm no screen crashes and the layout is unchanged structurally (polish only, no redesign).
  </how-to-verify>
  <resume-signal>Type "approved" if the gate is green and all screens look consistent, or describe the issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                                              |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| (none new) | Presentation-only polish over already-fetched, already-authorized data. No server action, schema, route gate, or data source is changed. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                   | Disposition | Mitigation Plan                                                                                                         |
| ---------- | ---------------------- | --------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------- |
| T-04-05-01 | Information Disclosure | finance / dashboard screens | accept      | Renders only data already authorized for authenticated dashboard users; styling-only change introduces no new exposure. |
| T-04-05-02 | Tampering              | shared StatusChip component | accept      | This plan does NOT modify the shared StatusChip component; no cross-screen regression risk introduced.                  |
| T-04-05-03 | Denial of Service      | npm run build               | accept      | Build is a local/CI verification step over existing dependencies; no new package is installed in this plan.             |

</threat_model>

<verification>
- `npx tsc --noEmit` zero errors
- `npm run lint` zero errors
- `npm run build` succeeds
- All dashboard routes render without crash; status badges, font-mono labels, token colors, and heading hierarchy are consistent; charts render intact
</verification>

<success_criteria>
SCRN-07 is fully satisfied: design-system consistency (font-mono labels, StatusChip / justified status badges, system color tokens, heading hierarchy) is applied across ALL dashboard screens, and the locked completion gate (tsc + lint + build) passes — closing Phase 4.
</success_criteria>

<output>
Create `.planning/phases/04-aprimoramento-de-telas-screen-enhancement/04-05-SUMMARY.md` when done
</output>
