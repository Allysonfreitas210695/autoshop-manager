---
phase: 04-aprimoramento-de-telas-screen-enhancement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/analytics/_components/AnalyticsClient.tsx
  - src/app/(dashboard)/inventory/alerts/_components/AlertsClient.tsx
  - src/app/(dashboard)/inventory/purchase-orders/_components/PurchaseOrdersClient.tsx
  - src/app/(dashboard)/inventory/purchase-orders/new/_components/NewPurchaseOrderClient.tsx
  - src/app/(dashboard)/orders/new/order-wizard.tsx
  - src/app/(dashboard)/orders/new/steps/step-checklist.tsx
autonomous: true
requirements: [SCRN-01, SCRN-02, SCRN-03, SCRN-04, SCRN-05]

must_haves:
  truths:
    - "SCRN-01: /analytics renders advanced strategic metrics (revenue 12m, total O.S., net margin, NPS, active customers) plus trend and breakdown charts"
    - "SCRN-02: /inventory/alerts shows low-stock items with CRÍTICO and ATENÇÃO highlighting"
    - "SCRN-03: /inventory/purchase-orders lists purchase orders and supports generating a new one"
    - "SCRN-04: the new purchase-order form captures a delivery forecast (Previsão de Entrega) and the list displays it"
    - "SCRN-05: the new-O.S. wizard includes the vehicle intake checklist step"
    - "Any gap found against a SCRN-01..05 success criterion is fixed; if no gap is found the screen is left unchanged"
  artifacts:
    - path: "src/app/(dashboard)/inventory/purchase-orders/new/_components/NewPurchaseOrderClient.tsx"
      provides: "Purchase-order delivery forecast input"
      contains: "deliveryDate"
    - path: "src/app/(dashboard)/orders/new/order-wizard.tsx"
      provides: "Checklist step wired into the order wizard"
      contains: "StepChecklist"
  key_links:
    - from: "src/app/(dashboard)/inventory/purchase-orders/new/_components/NewPurchaseOrderClient.tsx"
      to: "expectedDelivery payload field"
      via: "deliveryDate state serialized to ISO"
      pattern: "expectedDelivery"
    - from: "src/app/(dashboard)/orders/new/order-wizard.tsx"
      to: "src/app/(dashboard)/orders/new/steps/step-checklist.tsx"
      via: "wizard renders StepChecklist"
      pattern: "StepChecklist"
---

<objective>
Audit each existing SCRN-01..05 screen against its ROADMAP/REQUIREMENTS success criteria and fix any concrete gap found (D-05, D-06). The phase-planning audit found all five screens substantially implemented; this plan VERIFIES that against the live code and closes any specific gap that surfaces — it is explicitly NOT a rewrite or redesign (out of scope per REQUIREMENTS.md).

Purpose: Guarantee the strategic dashboard, low-stock alerts, purchase-order generation + delivery forecast, and the intake checklist all meet their requirement success criteria before the design-system polish pass runs.
Output: Each screen confirmed compliant; targeted edits applied only where a gap exists, with no scope reduction (no "v1"/"placeholder"/"static for now" — if a behavior is required, it is delivered in full).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-aprimoramento-de-telas-screen-enhancement/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit SCRN-01 (analytics) and SCRN-05 (intake checklist) and fix any gap</name>
  <files>src/app/(dashboard)/analytics/_components/AnalyticsClient.tsx, src/app/(dashboard)/orders/new/order-wizard.tsx, src/app/(dashboard)/orders/new/steps/step-checklist.tsx</files>
  <read_first>
    - .planning/REQUIREMENTS.md (SCRN-01 success: strategic dashboard with advanced metrics; SCRN-05 success: new O.S. with intake checklist as part of the order flow)
    - src/app/(dashboard)/analytics/_components/AnalyticsClient.tsx (FULL — KPIs array lines 38-79: Receita 12 meses, Total O.S., Margem Líquida, NPS, Clientes Ativos; revenue trend lines 130-192; mechanic + service breakdowns lines 194-301)
    - src/app/(dashboard)/orders/new/order-wizard.tsx (confirm StepChecklist is rendered as a wizard step and its values flow into the order submission)
    - src/app/(dashboard)/orders/new/steps/step-checklist.tsx (FULL — 12 default checklist items, ok/atencao/dano states, per-item notes, general notes)
  </read_first>
  <action>
    For SCRN-01: confirm `/analytics` renders the advanced strategic metrics (the five KPIs) AND at least the revenue trend chart plus the mechanic/service breakdowns. If all present and rendering, make NO change. If a documented advanced metric from the success criteria is absent, add it using the existing KPI card / chart pattern in the file (do not introduce a new charting library — Recharts is already used in AnalyticsCharts).

    For SCRN-05: confirm `StepChecklist` is wired into `order-wizard.tsx` as an actual step whose collected values are carried into the order submission payload (not rendered in isolation). If wired, make NO change. If the checklist is defined but not reachable in the wizard flow, wire it as a step following the same pattern the wizard uses for its other steps, threading `defaultValues` and the `onNext` handler.

    Do not redesign either screen. Do not remove working features. If you make zero edits because both are compliant, state that explicitly in the SUMMARY with the grep evidence used.

  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && npx tsc --noEmit 2>&1 | tail -5; grep -c "StepChecklist" "src/app/(dashboard)/orders/new/order-wizard.tsx"; grep -c "Margem Líquida\|NPS\|Clientes Ativos\|Receita 12 meses\|Total de O.S." "src/app/(dashboard)/analytics/_components/AnalyticsClient.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `order-wizard.tsx` references `StepChecklist` (grep >= 1) and the checklist values reach the wizard submission flow
    - `AnalyticsClient.tsx` still contains all five strategic KPI labels (grep of the five labels >= 5 combined matches)
    - `npx tsc --noEmit` reports zero errors
    - SUMMARY records, per screen, whether a gap was found and what (if anything) was changed
  </acceptance_criteria>
  <done>SCRN-01 and SCRN-05 are verified against their success criteria; any concrete gap is closed without redesign or scope reduction.</done>
</task>

<task type="auto">
  <name>Task 2: Audit SCRN-02 (alerts), SCRN-03 (PO list/generate), SCRN-04 (delivery forecast) and fix any gap</name>
  <files>src/app/(dashboard)/inventory/alerts/_components/AlertsClient.tsx, src/app/(dashboard)/inventory/purchase-orders/_components/PurchaseOrdersClient.tsx, src/app/(dashboard)/inventory/purchase-orders/new/_components/NewPurchaseOrderClient.tsx</files>
  <read_first>
    - .planning/REQUIREMENTS.md (SCRN-02 success: low-stock alerts with CRÍTICO/ATENÇÃO highlighting; SCRN-03 success: purchase-order generation; SCRN-04 success: purchase order with delivery forecast)
    - src/app/(dashboard)/inventory/alerts/_components/AlertsClient.tsx (FULL — CRÍTICO/ATENÇÃO summary cards lines 37-80, AlertRow severity badge lines 144-148)
    - src/app/(dashboard)/inventory/purchase-orders/_components/PurchaseOrdersClient.tsx (FULL — status KPIs, table with "Prev. Entrega" column line 117 + expectedDelivery cell lines 163-169, "Nova Ordem" link line 50)
    - src/app/(dashboard)/inventory/purchase-orders/new/_components/NewPurchaseOrderClient.tsx (lines 34, 90-91, 176-181 — deliveryDate state, expectedDelivery serialization, "Previsão de Entrega" date input)
  </read_first>
  <action>
    For SCRN-02: confirm the alerts screen highlights low-stock items with both CRÍTICO and ATENÇÃO treatments (summary cards + per-row severity badge). If present, make NO change.

    For SCRN-03: confirm the purchase-orders screen lists orders AND offers a path to generate a new one (the "Nova Ordem" link to `/inventory/purchase-orders/new`). If present, make NO change.

    For SCRN-04: confirm the new-PO form captures a delivery forecast (`deliveryDate` -> `expectedDelivery` ISO field, "Previsão de Entrega" input) and that the list surfaces it (the "Prev. Entrega" column). If both present, make NO change.

    Only if a criterion is unmet, add the minimal field/element using the existing patterns in the file (e.g. if the delivery-forecast input were missing, add a `type="date"` "Previsão de Entrega" field wired into the submission payload as `expectedDelivery`). Do not redesign. Record gap/no-gap findings per screen in the SUMMARY with grep evidence.

  </action>
  <verify>
    <automated>cd "/home/allyson/Área de trabalho/Projetos pessoal/precision-auto" && npx tsc --noEmit 2>&1 | tail -5; grep -c "CRÍTICO\|ATENÇÃO" "src/app/(dashboard)/inventory/alerts/_components/AlertsClient.tsx"; grep -c "purchase-orders/new" "src/app/(dashboard)/inventory/purchase-orders/_components/PurchaseOrdersClient.tsx"; grep -c "expectedDelivery\|Previsão de Entrega" "src/app/(dashboard)/inventory/purchase-orders/new/_components/NewPurchaseOrderClient.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - AlertsClient contains both `CRÍTICO` and `ATENÇÃO` severity treatments (grep >= 2 combined)
    - PurchaseOrdersClient links to the PO creation route (grep `purchase-orders/new` >= 1)
    - NewPurchaseOrderClient captures and serializes a delivery forecast (grep `expectedDelivery` or `Previsão de Entrega` >= 1)
    - `npx tsc --noEmit` reports zero errors
    - SUMMARY records per-screen gap/no-gap findings with evidence
  </acceptance_criteria>
  <done>SCRN-02, SCRN-03, SCRN-04 are verified against their success criteria; any concrete gap is closed without redesign or scope reduction.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                            | Description                                                                                                             |
| --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| client form → server action (purchase order create) | Only relevant if SCRN-04 requires adding the delivery-forecast field; the value crosses into a server action on submit. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                 | Disposition | Mitigation Plan                                                                                                                                                                                                                        |
| ---------- | ---------------------- | ----------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-03-01 | Tampering              | purchase-order create server action input | mitigate    | If a delivery-forecast field is added/modified, it must be validated by the existing Zod schema for the create-PO action (SEC-02 pattern) on the server, not just the client. If the field already exists and is validated, no change. |
| T-04-03-02 | Information Disclosure | analytics KPIs                            | accept      | Analytics renders aggregate data already authorized for authenticated dashboard users; this plan adds no new data source.                                                                                                              |

</threat_model>

<verification>
- `npx tsc --noEmit` zero errors
- `npm run lint` zero errors on touched files
- Each SCRN-01..05 screen renders without crash and meets its ROADMAP success criterion
- SUMMARY documents, per screen, gap-found vs already-compliant with grep evidence
</verification>

<success_criteria>
All five SCRN-01..05 screens verifiably meet their requirement success criteria. Where the audit found them already complete, they are left unchanged with evidence recorded; where a concrete gap existed, it is closed in full (no scope reduction, no redesign).
</success_criteria>

<output>
Create `.planning/phases/04-aprimoramento-de-telas-screen-enhancement/04-03-SUMMARY.md` when done
</output>
