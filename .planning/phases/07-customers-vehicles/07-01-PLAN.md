---
phase: 07-customers-vehicles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/_actions/customers.ts
  - src/_data-access/customers.ts
  - src/_actions/customers.test.ts
autonomous: true
requirements: [CLI-01, CLI-03]
must_haves:
  truths:
    - "Criar cliente com email já existente retorna mensagem amigável (sem crash PG)"
    - "Editar cliente para um email de outro cliente retorna mensagem amigável; manter o próprio email é permitido"
    - "Busca de clientes encontra registros por nome, email, CPF e placa em todo o banco"
  artifacts:
    - path: "src/_actions/customers.ts"
      provides: "Email guards em createCustomerAction e updateCustomerAction"
      contains: "ActionError"
    - path: "src/_data-access/customers.ts"
      provides: "searchCustomers ampliado para CPF e placa"
      contains: "vehicles"
    - path: "src/_actions/customers.test.ts"
      provides: "Static source-assertions para CLI-01 e CLI-03"
      contains: "exportBlocks"
  key_links:
    - from: "src/_actions/customers.ts createCustomerAction"
      to: "user.email SELECT pre-check"
      via: "eq(user.email, parsedInput.email) antes de db.insert"
      pattern: "eq\\(user\\.email"
    - from: "src/_data-access/customers.ts searchCustomers"
      to: "vehicles.plate"
      via: "leftJoin vehicles + ILIKE plate"
      pattern: "vehicles\\.plate"
---

<objective>
Fechar a camada de dados de clientes: guard de email duplicado em create/update (CLI-03) e busca server-side ampliada para CPF e placa (CLI-01). Criar o test scaffold static source-assertion (Wave 0) que cobre toda a phase.

Purpose: Eliminar crash de PG UNIQUE em email e garantir busca em todo o banco (não só na página carregada).
Output: `customers.ts` (action) com guards, `customers.ts` (data-access) com searchCustomers ampliado, `customers.test.ts` novo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-customers-vehicles/07-CONTEXT.md
@.planning/phases/07-customers-vehicles/07-PATTERNS.md
@.planning/phases/07-customers-vehicles/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Criar test scaffold static source-assertion (Wave 0)</name>
  <files>src/_actions/customers.test.ts</files>
  <read_first>
    - src/_actions/orders.test.ts (estrutura exata a replicar: readFileSync + exportBlocks + assertions string-based)
    - src/_actions/customers.ts (source que será assertado)
    - src/_data-access/customers.ts (source que será assertado)
  </read_first>
  <action>
    Criar `src/_actions/customers.test.ts` copiando a estrutura de `orders.test.ts`: helper `exportBlocks(source)` que faz match de `/export const (\w+)\s*=/g` e fatia o corpo de cada export. Usar `readFileSync(join(ACTIONS_DIR, "customers.ts"), "utf8")` e `ACTIONS_DIR = dirname(fileURLToPath(import.meta.url))`. Para searchCustomers, ler também `readFileSync(join(ACTIONS_DIR, "..", "_data-access", "customers.ts"), "utf8")`.
    Describe block: "Phase 7 customers actions wiring (D-01..D-07)". Incluir os seguintes `it`:
    - "D-01: createCustomerAction contém email guard antes de db.insert": bloco createCustomerAction inclui "ActionError" E inclui "E-mail já cadastrado".
    - "D-02: updateCustomerAction contém email guard com ne(): bloco updateCustomerAction inclui "ActionError", inclui "E-mail já cadastrado", e inclui "ne(".
    - "CLI-01: searchCustomers cobre CPF e placa": source do data-access inclui "user.cpf" (referência em where) e inclui "vehicles.plate".
    Os asserts usam a forma `expect(block?.body.includes("..."), "mensagem").toBe(true)`. Este task roda ANTES das implementações, então RED é esperado até os tasks 1-3.
  </action>
  <verify>
    <automated>npx vitest run src/_actions/customers.test.ts 2>&1 | grep -qE "(failing|FAIL|×|passed|passing)" && echo "test file executa"</automated>
  </verify>
  <acceptance_criteria>
    - `src/_actions/customers.test.ts` existe e contém `exportBlocks`
    - Arquivo contém os 3 `it` descritos (D-01, D-02, CLI-01)
    - `npx vitest run src/_actions/customers.test.ts` executa o arquivo (testes podem estar RED neste ponto — esperado)
  </acceptance_criteria>
  <done>Test file existe, executa via vitest, contém assertions para D-01, D-02 e CLI-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Email guards em createCustomerAction e updateCustomerAction (CLI-03)</name>
  <files>src/_actions/customers.ts</files>
  <behavior>
    - createCustomerAction com email já existente lança ActionError "E-mail já cadastrado. Use outro ou acesse o perfil do cliente existente." e NÃO chama db.insert
    - updateCustomerAction com email de OUTRO cliente lança a mesma ActionError
    - updateCustomerAction mantendo o próprio email (mesmo id) NÃO bloqueia (ne(user.id, parsedInput.id))
  </behavior>
  <read_first>
    - src/_actions/customers.ts (estado atual: createCustomerAction linhas 11-36, updateCustomerAction linhas 70-96)
    - src/_lib/safe-action.ts (ActionError + handleServerError que retorna error.message como error.serverError)
    - src/_actions/customers.test.ts (assertions D-01/D-02 que devem passar após este task)
  </read_first>
  <action>
    Adicionar imports: `import { and, eq, ne } from "drizzle-orm";` (substituir o import atual de `eq` only) e `import { ActionError } from "@/_lib/safe-action";`.
    Em createCustomerAction (D-01): antes de `db.insert(user)`, executar `db.select({ id: user.id }).from(user).where(eq(user.email, parsedInput.email)).limit(1)`. Se `existing.length > 0`, `throw new ActionError("E-mail já cadastrado. Use outro ou acesse o perfil do cliente existente.")`.
    Em updateCustomerAction (D-02): antes de `db.update(user)`, executar `db.select({ id: user.id }).from(user).where(and(eq(user.email, parsedInput.email), ne(user.id, parsedInput.id))).limit(1)`. Se `existing.length > 0`, lançar a MESMA ActionError com a mesma mensagem exata.
    Não alterar nada mais nos actions. Mensagem deve ser idêntica em ambos.
  </action>
  <verify>
    <automated>npx vitest run src/_actions/customers.test.ts -t "D-01" && npx vitest run src/_actions/customers.test.ts -t "D-02"</automated>
  </verify>
  <acceptance_criteria>
    - `src/_actions/customers.ts` contém `import { ActionError }` from `@/_lib/safe-action`
    - createCustomerAction contém `eq(user.email` e `ActionError` e a string `E-mail já cadastrado. Use outro ou acesse o perfil do cliente existente.`
    - updateCustomerAction contém `ne(user.id` e a mesma string de erro
    - `npx vitest run src/_actions/customers.test.ts -t "D-01"` passa; `-t "D-02"` passa
  </acceptance_criteria>
  <done>Ambos os guards de email presentes; mensagem exata; pre-check de update usa ne(id); testes D-01/D-02 verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Ampliar searchCustomers para CPF e placa (CLI-01)</name>
  <files>src/_data-access/customers.ts</files>
  <read_first>
    - src/_data-access/customers.ts (searchCustomers linhas 182-197; padrão de leftJoin com vehicles já usado em listCustomers linhas 86-96)
    - src/_db/schema (vehicles.ownerId, vehicles.plate, user.cpf)
    - src/_actions/customers.test.ts (assertion CLI-01 que deve passar após este task)
  </read_first>
  <action>
    Modificar `searchCustomers(query)`: adicionar `.leftJoin(vehicles, eq(vehicles.ownerId, user.id))` (vehicles e eq já importados no arquivo). Ampliar a cláusula WHERE para incluir CPF e placa: manter `lower(${user.name}) like ${lq} or lower(${user.email}) like ${lq}` e adicionar `or lower(${user.cpf}) like ${lq} or lower(${vehicles.plate}) like ${lq}`. Como o leftJoin pode duplicar linhas por cliente com múltiplos veículos, adicionar `.groupBy(user.id)` antes de `.limit(10)` para deduplicar (manter os mesmos campos selecionados: id, name, email, phone, cpf). Não alterar a assinatura nem o tipo de retorno.
  </action>
  <verify>
    <automated>npx vitest run src/_actions/customers.test.ts -t "CLI-01"</automated>
  </verify>
  <acceptance_criteria>
    - searchCustomers contém `leftJoin(vehicles`
    - WHERE contém `user.cpf` e `vehicles.plate`
    - searchCustomers contém `groupBy(user.id)`
    - `npx vitest run src/_actions/customers.test.ts -t "CLI-01"` passa
    - `npx tsc --noEmit` não introduz novos erros em customers.ts
  </acceptance_criteria>
  <done>searchCustomers busca por nome, email, CPF e placa em todo o banco com dedupe por cliente; teste CLI-01 verde.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                | Description                                                   |
| ------------------------------------------------------- | ------------------------------------------------------------- |
| client form → createCustomerAction/updateCustomerAction | Input não confiável (email, cpf) cruza para a camada de dados |
| search input → searchCustomers                          | Query string do usuário vira filtro ILIKE no DB               |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                 | Disposition | Mitigation Plan                                                                                                |
| --------- | ---------------------- | --------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------- |
| T-07-01   | Information Disclosure | Mensagem de email duplicado revela existência de cadastro | accept      | Mensagem informativa por design (D-01); oficina interna, sem self-signup público                               |
| T-07-02   | Tampering / Injection  | searchCustomers ILIKE com query do usuário                | mitigate    | Query parametrizada via Drizzle `sql` template (bind params), nunca string concat; `lq` passado como parâmetro |
| T-07-03   | Spoofing               | Acesso não autenticado aos actions                        | mitigate    | `authActionClient` exige sessão Better Auth em create/update                                                   |
| T-07-04   | Elevation of Privilege | Validação de input                                        | mitigate    | Zod schema (`z.string().email()`) valida formato antes de qualquer query (V5 ASVS L1)                          |
| T-07-SC   | Tampering              | npm/pip/cargo installs                                    | accept      | Nenhum pacote novo instalado nesta phase (RESEARCH: audit N/A)                                                 |

</threat_model>

<verification>
- `npx vitest run src/_actions/customers.test.ts` — D-01, D-02, CLI-01 verdes
- `npm test -- --run` — suite completa verde
- `npx tsc --noEmit` — sem novos erros de tipo
</verification>

<success_criteria>

- Email duplicado em create/update retorna mensagem amigável exata (sem exception PG genérica)
- updateCustomerAction permite manter o próprio email
- searchCustomers encontra por nome, email, CPF e placa em todo o banco
- Test scaffold cobre CLI-01 e CLI-03 e está verde
  </success_criteria>

<output>
Create `.planning/phases/07-customers-vehicles/07-01-SUMMARY.md` when done
</output>
