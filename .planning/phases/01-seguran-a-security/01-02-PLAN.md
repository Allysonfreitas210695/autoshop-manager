---
phase: 01-seguran-a-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/proxy.ts
  - src/proxy.test.ts
autonomous: true
requirements: [SEC-03]
must_haves:
  truths:
    - "An unauthenticated request to a dashboard route is redirected to /login with a redirect param"
    - "An authenticated request to an auth route (/login, /register, /forgot-password, /reset-password) is redirected to /"
    - "An unauthenticated request to /track/[id] is allowed through (public O.S. tracking)"
    - "An unauthenticated request to /forgot-password and /reset-password is allowed through"
    - "getSessionCookie remains a presence-only optimistic check (not the authz boundary)"
  artifacts:
    - path: "src/proxy.ts"
      provides: "Route gating with corrected authRoutes + public prefixes"
      contains: "/forgot-password"
    - path: "src/proxy.test.ts"
      provides: "Unit tests for redirect logic with mocked NextRequest + getSessionCookie"
  key_links:
    - from: "src/proxy.ts authRoutes"
      to: "/forgot-password and /reset-password"
      via: "authRoutes array membership"
      pattern: "/forgot-password"
    - from: "src/proxy.ts ALWAYS_PUBLIC_PREFIXES"
      to: "/track"
      via: "public prefix membership"
      pattern: "/track"
---

<objective>
Fix the SEC-03 route-gating bugs (D-03) with a surgical edit of two arrays in `src/proxy.ts`: add `/forgot-password` + `/reset-password` to `authRoutes` (reachable while logged out; redirect to `/` when already authenticated) and add `/track` to `ALWAYS_PUBLIC_PREFIXES` (public QR O.S. tracking `/track/[id]`). Keep `getSessionCookie` as a presence-only optimistic check (D-05) — the proxy is UX-only and NOT the security boundary. No role-based gating (D-04 deferred).

Purpose: Recovery routes and public tracking are currently mis-gated; this corrects redirect UX without changing the real (server-side) authorization boundary.
Output: Updated `src/proxy.ts`, new `src/proxy.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-seguran-a-security/01-CONTEXT.md
@.planning/phases/01-seguran-a-security/01-RESEARCH.md
@.planning/phases/01-seguran-a-security/01-PATTERNS.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing proxy route-gating tests</name>
  <files>src/proxy.test.ts</files>
  <read_first>
    - src/proxy.ts (full file: proxy(), authRoutes, ALWAYS_PUBLIC_PREFIXES, isStaticAsset, matcher)
    - node_modules/next/dist/docs/01-app/01-getting-started/16-proxy.md (proxy convention; "not an authz solution")
    - vitest.config.ts (globals: true; alias @ → ./src)
    - .planning/phases/01-seguran-a-security/01-PATTERNS.md (proxy.ts pattern assignment)
  </read_first>
  <behavior>
    - unauthenticated request to /dashboard (or /orders) → redirect to /login with searchParam redirect set
    - authenticated request to /login → redirect to /
    - authenticated request to /forgot-password → redirect to /
    - unauthenticated request to /forgot-password → NextResponse.next() (allowed)
    - unauthenticated request to /reset-password → allowed
    - unauthenticated request to /track/abc123 → allowed (public)
  </behavior>
  <action>Create `src/proxy.test.ts`. Mock `better-auth/cookies` so `getSessionCookie` returns a truthy token (authenticated) or `null`/undefined (unauthenticated) per case using `vi.mock`. Construct request inputs as objects shaped like `NextRequest` exposing `nextUrl.pathname` and a `url` string (build minimal stubs sufficient for `new URL(...)` calls inside `proxy`). Import `proxy` from `@/proxy`. Assert on the returned `NextResponse` — for redirects inspect the `Location` header / status (307), for pass-through assert it is not a redirect. This test MUST fail initially for the `/track` and `/forgot-password` cases because those routes are not yet whitelisted (RED). Rely on Vitest globals.</action>
  <verify>
    <automated>npx vitest run src/proxy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/proxy.test.ts` exists, mocks `better-auth/cookies`, imports `proxy` from `@/proxy`
    - Running `npx vitest run src/proxy.test.ts` FAILS (RED) on at least the `/track` and `/forgot-password` unauth cases
  </acceptance_criteria>
  <done>Failing test file exists covering all six redirect/pass-through behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add recovery routes + /track public prefix to proxy (D-03)</name>
  <files>src/proxy.ts</files>
  <read_first>
    - src/proxy.ts (line 4 authRoutes; lines 9-18 ALWAYS_PUBLIC_PREFIXES; line 35 getSessionCookie; lines 51-55 matcher)
    - .planning/phases/01-seguran-a-security/01-PATTERNS.md (proxy.ts Edit 1 + Edit 2 + "Do NOT change" list)
  </read_first>
  <action>Edit `authRoutes` (currently `["/login", "/register"]`) to `["/login", "/register", "/forgot-password", "/reset-password"]`. Edit `ALWAYS_PUBLIC_PREFIXES` to add `"/track"` (public O.S. tracking `/track/[id]` — D-03). Do NOT change `getSessionCookie` (line 35) — it is optimistic presence-only by design (D-05) and is NOT the authz boundary. Do NOT change the `matcher` config (already excludes api|_next/static|_next/image|favicon.ico|icon.png|sw.js|manifest.webmanifest). Do NOT add role-based gating (D-04 deferred). Keep ESLint import sort intact.</action>
  <verify>
    <automated>npx vitest run src/proxy.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/proxy.ts` `authRoutes` contains `/forgot-password` and `/reset-password`
    - `src/proxy.ts` `ALWAYS_PUBLIC_PREFIXES` contains `/track`
    - `getSessionCookie` line is unchanged (still presence-only)
    - `npx vitest run src/proxy.test.ts` exits 0 (GREEN)
    - `npx tsc --noEmit` zero errors; `npm run lint` zero errors
  </acceptance_criteria>
  <done>Recovery routes gate as auth routes; /track is public; proxy tests green; authz boundary unchanged.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                               |
| ------------------------------ | --------------------------------------------------------- |
| browser → proxy.ts             | Optimistic redirect UX only; presence-only cookie check   |
| browser → server action / page | The REAL authz boundary (auth.api.getSession server-side) |

## STRIDE Threat Register

| Threat ID | Category                     | Component                                              | Disposition         | Severity | Mitigation Plan                                                                                                                                  |
| --------- | ---------------------------- | ------------------------------------------------------ | ------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-1-RT1   | Elevation of Privilege       | proxy treated as authz boundary                        | accept (documented) | medium   | Per D-05, proxy stays optimistic UX-only; real authz is server-side `authActionClient` (verified in Plan 05). Do not strengthen proxy into authz |
| T-1-RT2   | Information Disclosure / DoS | Public /track/[id] mis-gated (blocked or over-exposed) | mitigate            | low      | `/track` added to public prefixes so QR tracking is reachable logged-out; no auth-only data on this route                                        |
| T-1-RT3   | Spoofing                     | Unsigned cookie trust                                  | accept              | low      | `getSessionCookie` presence-only by design; never used for authz decisions (D-05)                                                                |

</threat_model>

<verification>
- `npx vitest run src/proxy.test.ts` green (all six behaviors)
- `npx tsc --noEmit` zero errors; `npm run lint` zero errors
- Manual: logged-out visit to `/track/<id>` renders; logged-out visit to `/forgot-password` renders; logged-in visit to `/login` redirects to `/`
</verification>

<success_criteria>

- SEC-03: dashboard routes protected, auth + recovery routes gated, /track public; proxy remains optimistic (not the security boundary)
  </success_criteria>

<output>
Create `.planning/phases/01-seguran-a-security/01-02-SUMMARY.md` when done
</output>
