---
phase: 01-seguran-a-security
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/security/headers.test.ts
  - src/app/api/csp-report/route.ts
autonomous: true
requirements: [SEC-04]
must_haves:
  truths:
    - "Every response carries HSTS, X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy, Permissions-Policy"
    - "Responses carry Content-Security-Policy-Report-Only (NOT an enforcing CSP)"
    - "The CSP allows 'unsafe-eval' in development only (React Compiler) and not in production"
    - "A /api/csp-report endpoint accepts report POSTs without erroring"
  artifacts:
    - path: "next.config.ts"
      provides: "headers() returning baseline security headers + CSP report-only for /(.*)"
      contains: "Strict-Transport-Security"
    - path: "src/security/headers.test.ts"
      provides: "Unit test invoking nextConfig.headers() and asserting each header key/value"
    - path: "src/app/api/csp-report/route.ts"
      provides: "CSP report sink (POST → 204)"
  key_links:
    - from: "next.config.ts headers()"
      to: "source: /(.*) header rule"
      via: "async headers() return array"
      pattern: "Content-Security-Policy-Report-Only"
    - from: "next.config.ts CSP report-uri"
      to: "/api/csp-report"
      via: "report-uri directive"
      pattern: "/api/csp-report"
---

<objective>
Apply SEC-04 security headers (D-01) and a report-only CSP (D-02) via `next.config.ts` `headers()` for `source: "/(.*)"`. Baseline headers: `Strict-Transport-Security` (long max-age + includeSubDomains + preload), `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` (deny camera/mic/geolocation). CSP ships as `Content-Security-Policy-Report-Only` ONLY (never blocking this phase) with `'unsafe-eval'` gated to development (React Compiler uses eval in dev). Add a tiny `/api/csp-report` route as the `report-uri` sink.

Purpose: Clickjacking, MIME-sniffing, protocol-downgrade, and XSS hardening at the edge without breaking the Next.js 16 / React Compiler inline runtime. Nonce-based enforced CSP is deferred (D-02).
Output: Updated `next.config.ts`, new `src/security/headers.test.ts`, new `src/app/api/csp-report/route.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-seguran-a-security/01-CONTEXT.md
@.planning/phases/01-seguran-a-security/01-RESEARCH.md
@.planning/phases/01-seguran-a-security/01-PATTERNS.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing security-headers test</name>
  <files>src/security/headers.test.ts</files>
  <read_first>
    - next.config.ts (current empty stub)
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/headers.md (headers() async config shape)
    - node_modules/next/dist/docs/01-app/02-guides/content-security-policy.md (report-only / "Without Nonces" / dev 'unsafe-eval')
    - vitest.config.ts (globals: true; alias @ → ./src)
    - .planning/phases/01-seguran-a-security/01-RESEARCH.md Pattern 1 + Pitfall 1
  </read_first>
  <behavior>
    - nextConfig.headers() resolves to an array with one rule whose source is "/(.*)"
    - that rule's headers include key "Strict-Transport-Security" with includeSubDomains
    - include "X-Frame-Options" value "DENY"
    - include "X-Content-Type-Options" value "nosniff"
    - include "Referrer-Policy" value "strict-origin-when-cross-origin"
    - include "Permissions-Policy" denying camera/microphone/geolocation
    - include "Content-Security-Policy-Report-Only" (and NO key "Content-Security-Policy")
  </behavior>
  <action>Create `src/security/headers.test.ts`. Import the default export from `@/../next.config` (or relative path to the repo-root `next.config.ts` — resolve the correct import specifier for a root-level config from a `src/` test; use a relative path if the `@` alias does not reach repo root). `await` `nextConfig.headers()` and assert via array find on `source === "/(.*)"` then a helper that maps the headers array to key→value. Assert each required header key/value and assert the absence of an enforcing `Content-Security-Policy` key. This MUST fail initially because the stub `headers()` does not exist (RED). Rely on Vitest globals.</action>
  <verify>
    <automated>npx vitest run src/security/headers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/security/headers.test.ts` exists and invokes `nextConfig.headers()`
    - Running `npx vitest run src/security/headers.test.ts` FAILS (RED) against the current empty config
  </acceptance_criteria>
  <done>Failing test asserts all six baseline headers + report-only CSP + absence of enforcing CSP.</done>
</task>

<task type="auto">
  <name>Task 2: Add baseline headers + report-only CSP to next.config.ts (D-01, D-02)</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (current 7-line stub — preserve import type NextConfig + export default shape)
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/headers.md
    - node_modules/next/dist/docs/01-app/02-guides/content-security-policy.md
    - .planning/phases/01-seguran-a-security/01-RESEARCH.md Pattern 1 (verified template) + Pitfall 1
  </read_first>
  <action>Add an `async headers()` to `nextConfig` returning a single rule for `source: "/(.*)"` with these header entries: `Strict-Transport-Security` = `max-age=63072000; includeSubDomains; preload`; `X-Frame-Options` = `DENY`; `X-Content-Type-Options` = `nosniff`; `Referrer-Policy` = `strict-origin-when-cross-origin`; `Permissions-Policy` = `camera=(), microphone=(), geolocation=()`; `Content-Security-Policy-Report-Only` = the collapsed report-only directive string. Build the CSP string from directives: `default-src 'self'`, `script-src 'self' 'unsafe-inline'` plus `'unsafe-eval'` ONLY when `process.env.NODE_ENV === "development"`, `style-src 'self' 'unsafe-inline'`, `img-src 'self' blob: data:`, `font-src 'self'`, `connect-src 'self'`, `object-src 'none'`, `base-uri 'self'`, `form-action 'self'`, `frame-ancestors 'none'`, `upgrade-insecure-requests`, `report-uri /api/csp-report`. Collapse whitespace and trim. Do NOT emit an enforcing `Content-Security-Policy` header (D-02 — report-only only). Preserve `import type { NextConfig } from "next"` and `export default nextConfig`. Keep ESLint import sort + no `any`.</action>
  <verify>
    <automated>npx vitest run src/security/headers.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `next.config.ts` `headers()` returns a `/(.*)` rule containing all five baseline headers with the exact values above
    - The rule contains `Content-Security-Policy-Report-Only` and NOT `Content-Security-Policy`
    - `'unsafe-eval'` appears only under `NODE_ENV === "development"`
    - `npx vitest run src/security/headers.test.ts` exits 0 (GREEN)
    - `npx tsc --noEmit` zero errors; `npm run lint` zero errors
  </acceptance_criteria>
  <done>All baseline headers + report-only CSP applied app-wide; no blocking CSP.</done>
</task>

<task type="auto">
  <name>Task 3: Add /api/csp-report sink route (D-02)</name>
  <files>src/app/api/csp-report/route.ts</files>
  <read_first>
    - src/app/api/auth/[...all]/route.ts (route-handler shape: named exports / async function)
    - node_modules/next/dist/docs (route handler guide for the App Router — read before writing the handler)
    - .planning/phases/01-seguran-a-security/01-PATTERNS.md (csp-report route pattern)
  </read_first>
  <action>Create `src/app/api/csp-report/route.ts` exporting an async `POST` handler that reads the JSON body (tolerant of empty/invalid body — wrap in try/catch, do not throw), logs it only when `process.env.NODE_ENV !== "production"` (avoid noisy/abusable prod logging), and returns `new Response(null, { status: 204 })`. This is the `report-uri` sink referenced by the CSP. Keep it minimal; no auth (CSP reports are unauthenticated by spec). No `any` — type the body as `unknown`.</action>
  <verify>
    <automated>npx tsc --noEmit && test -f src/app/api/csp-report/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/csp-report/route.ts` exports an async `POST` returning HTTP 204
    - Handler does not throw on empty/invalid JSON body
    - `npx tsc --noEmit` zero errors; `npm run lint` zero errors
  </acceptance_criteria>
  <done>CSP report-uri has a working in-app sink returning 204.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                          |
| ------------------------- | ---------------------------------------------------- |
| internet → all responses  | Security headers applied at the edge for every route |
| browser → /api/csp-report | Unauthenticated report sink (untrusted body)         |

## STRIDE Threat Register

| Threat ID | Category                    | Component                                | Disposition        | Severity | Mitigation Plan                                                                                           |
| --------- | --------------------------- | ---------------------------------------- | ------------------ | -------- | --------------------------------------------------------------------------------------------------------- |
| T-1-HD1   | Tampering                   | Clickjacking via iframe embedding        | mitigate           | medium   | `X-Frame-Options: DENY` + CSP `frame-ancestors 'none'`                                                    |
| T-1-HD2   | Tampering / Info Disclosure | MITM / protocol downgrade                | mitigate           | high     | `Strict-Transport-Security` max-age=63072000; includeSubDomains; preload                                  |
| T-1-HD3   | Tampering                   | MIME sniffing                            | mitigate           | low      | `X-Content-Type-Options: nosniff`                                                                         |
| T-1-HD4   | Tampering                   | XSS                                      | mitigate (partial) | medium   | CSP report-only now (collect violations); enforced nonce CSP deferred (D-02); React auto-escaping remains |
| T-1-HD5   | DoS                         | Abuse of unauthenticated /api/csp-report | accept             | low      | Handler is side-effect-free (204), logs only in non-prod; no storage/amplification                        |

</threat_model>

<verification>
- `npx vitest run src/security/headers.test.ts` green
- `npx tsc --noEmit` zero errors; `npm run lint` zero errors
- Manual (SEC-04): `curl -I` against a running dev/prod server shows HSTS, X-Frame-Options=DENY, X-Content-Type-Options=nosniff, Referrer-Policy, Permissions-Policy, and Content-Security-Policy-Report-Only (no enforcing CSP)
</verification>

<success_criteria>

- SEC-04: baseline security headers + report-only CSP on all responses; no blocking CSP; React Compiler dev runtime not broken
  </success_criteria>

<output>
Create `.planning/phases/01-seguran-a-security/01-03-SUMMARY.md` when done
</output>
