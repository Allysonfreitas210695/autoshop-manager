---
phase: 01-seguran-a-security
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/_actions/_audit.test.ts
  - scripts/check-secret-boundary.mjs
autonomous: true
requirements: [SEC-02, SEC-06]
must_haves:
  truths:
    - "Every exported server action in src/_actions/* is built from authActionClient and declares a .schema()"
    - "A future action that skips authActionClient or .schema() fails the audit test"
    - "No server-only secret string appears in the client bundle after build"
    - 'No module reading a server secret declares "use client"'
    - "Only NEXT_PUBLIC_ env vars are exposed to the client"
  artifacts:
    - path: "src/_actions/_audit.test.ts"
      provides: "Static coverage assertion that every _actions export uses authActionClient + .schema"
      contains: "authActionClient"
    - path: "scripts/check-secret-boundary.mjs"
      provides: "Secret-boundary guard script (grep recipe → non-zero exit on leak)"
      contains: "BETTER_AUTH_SECRET"
  key_links:
    - from: "src/_actions/_audit.test.ts"
      to: "src/_actions/{appointments,customers,inventory,orders}.ts exports"
      via: "import + shape assertion"
      pattern: "_actions"
    - from: "scripts/check-secret-boundary.mjs"
      to: ".next/static + src use client modules"
      via: "build-output + source grep"
      pattern: "use client"
---

<objective>
Lock SEC-02 and SEC-06 as enforceable guards rather than one-time manual checks. SEC-02 (D-09): the audit already PASSES — all 9 exported actions across the 4 `src/_actions/*` files use `authActionClient.schema(...).action(...)`. Add a static `src/_actions/_audit.test.ts` that fails if ANY current or future export skips that shape (so coverage cannot regress). SEC-06 (D-10): secrets currently appear only in server modules and only `NEXT_PUBLIC_APP_URL` is client-facing. Add `scripts/check-secret-boundary.mjs` implementing the RESEARCH grep recipe so the boundary is verifiable in CI / pre-deploy (no source rewrite needed — both already pass).

Purpose: Make passing audits non-regressable. No edits to the action files themselves (D-09 verified PASS); no edits to secret modules (D-10 verified PASS) — this plan adds the guards.
Output: New `src/_actions/_audit.test.ts`, new `scripts/check-secret-boundary.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-seguran-a-security/01-CONTEXT.md
@.planning/phases/01-seguran-a-security/01-RESEARCH.md
@.planning/phases/01-seguran-a-security/01-PATTERNS.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static SEC-02 coverage audit test (D-09)</name>
  <files>src/_actions/_audit.test.ts</files>
  <read_first>
    - src/_actions/orders.ts (canonical action shape: "use server" + authActionClient.schema(z).action)
    - src/_actions/appointments.ts, src/_actions/customers.ts, src/_actions/inventory.ts (all exported actions)
    - src/_lib/safe-action.ts (authActionClient — the required base client)
    - .planning/phases/01-seguran-a-security/01-PATTERNS.md (SEC-02 AUDIT table: 9 actions, all PASS) + Test Patterns (Wave 0)
    - vitest.config.ts (globals: true; alias @ → ./src)
  </read_first>
  <action>Create `src/_actions/_audit.test.ts` that statically asserts every exported server action conforms to the `authActionClient` + `.schema()` shape. Two complementary checks: (1) SOURCE-LEVEL — read each `src/_actions/{appointments,customers,inventory,orders}.ts` file as text (via `node:fs`) and assert that for every `export const X = ...` the definition references `authActionClient` and `.schema(`; fail with the offending export name if not. (2) IMPORT-LEVEL — import each actions module and assert each exported action is a function/object produced by next-safe-action (e.g. has the callable shape returned by `.action(...)`). Prefer the source-level regex check as the primary guard (catches a future export that forgets `.schema`); keep it resilient to formatting. The test must currently PASS (all 9 actions comply per the audit). No edits to the action files. Rely on Vitest globals; resolve action file paths via the `@` alias or `node:path` from the test location.</action>
  <verify>
    <automated>npx vitest run src/_actions/_audit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/_actions/_audit.test.ts` exists and checks all four `_actions/*.ts` files
    - For every exported action it asserts presence of `authActionClient` and `.schema(`
    - `npx vitest run src/_actions/_audit.test.ts` exits 0 (GREEN) against current sources
    - A hypothetical export lacking `.schema(` would fail the test (assertion targets each export, not file-level imports)
    - `npx tsc --noEmit` zero errors; `npm run lint` zero errors
  </acceptance_criteria>
  <done>SEC-02 coverage is asserted statically and passes; regressions will fail the suite.</done>
</task>

<task type="auto">
  <name>Task 2: Secret-boundary guard script (SEC-06 / D-10)</name>
  <files>scripts/check-secret-boundary.mjs</files>
  <read_first>
    - src/_lib/auth.ts + src/_db/index.ts (server-only secret readers — must NOT be "use client")
    - src/_lib/auth-client.ts (only NEXT_PUBLIC_APP_URL client-facing)
    - .planning/phases/01-seguran-a-security/01-RESEARCH.md "SEC-06 secret-boundary verification recipe" (lines 440-450) + Pitfall 5
    - .planning/phases/01-seguran-a-security/01-PATTERNS.md (Secret/env boundary section)
  </read_first>
  <action>Create `scripts/check-secret-boundary.mjs` (Node ESM, no new deps) implementing the RESEARCH grep recipe as an executable guard with a non-zero exit on any violation: (1) find every `src/` module that references a server secret token (`BETTER_AUTH_SECRET`, `GOOGLE_CLIENT_SECRET`, `UPSTASH_`, `DATABASE_URL`) and FAIL if any such module's first line is `"use client"` (Pitfall 5); (2) assert no non-`NEXT_PUBLIC_` secret is referenced inside a `"use client"` module; (3) if a `.next/static` build output exists, grep it for the literal secret env NAMES `BETTER_AUTH_SECRET` / `GOOGLE_CLIENT_SECRET` and FAIL ("LEAK!") if found, else print "clean" (skip gracefully if `.next/static` absent so the script runs pre-build too). Print a clear PASS/FAIL summary. Use `node:fs`/`node:child_process` only. This documents the current PASS state and makes it CI-enforceable; do NOT edit any secret module (D-10 already passes).</action>
  <verify>
    <automated>node scripts/check-secret-boundary.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-secret-boundary.mjs` runs and exits 0 against the current (already-passing) tree
    - It exits non-zero if a server-secret-reading module declares `"use client"`
    - It greps `.next/static` for secret names when present and reports clean, skipping gracefully when absent
    - No secret module is edited
  </acceptance_criteria>
  <done>SEC-06 secret boundary is enforced by a runnable guard script that currently passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                       |
| ----------------------- | ----------------------------------------------------------------- |
| browser → server action | next-safe-action parses input server-side before the handler runs |
| build → client bundle   | Only NEXT*PUBLIC* env surface may cross into client JS            |

## STRIDE Threat Register

| Threat ID | Category                           | Component                                                 | Disposition | Severity | Mitigation Plan                                                                                                   |
| --------- | ---------------------------------- | --------------------------------------------------------- | ----------- | -------- | ----------------------------------------------------------------------------------------------------------------- |
| T-1-AU1   | Tampering / Elevation of Privilege | Server action accepting unvalidated/unauthenticated input | mitigate    | high     | `authActionClient.schema()` on every action; static `_audit.test.ts` fails on any export missing the shape (D-09) |
| T-1-AU2   | Information Disclosure             | Server secret leaking into client bundle                  | mitigate    | high     | `check-secret-boundary.mjs` greps build output + flags secret reads in `"use client"` modules (D-10 / Pitfall 5)  |
| T-1-AU3   | Information Disclosure             | Secret accidentally read from a client module             | mitigate    | medium   | Guard script asserts no non-NEXT*PUBLIC* secret referenced under `"use client"`                                   |

</threat_model>

<verification>
- `npx vitest run src/_actions/_audit.test.ts` green (all 9 actions covered)
- `node scripts/check-secret-boundary.mjs` exits 0 (clean) pre-build
- Phase gate (SEC-06 full): `npm run build && node scripts/check-secret-boundary.mjs` → "clean" against `.next/static`
- `npx tsc --noEmit` zero errors; `npm run lint` zero errors
</verification>

<success_criteria>

- SEC-02: every server-action export is statically asserted to use authActionClient + .schema (non-regressable)
- SEC-06: secret boundary is enforced by a runnable guard; no server secret reaches the client bundle
  </success_criteria>

<output>
Create `.planning/phases/01-seguran-a-security/01-05-SUMMARY.md` when done
</output>
