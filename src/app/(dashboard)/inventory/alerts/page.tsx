import { AlertsClient } from "./_components/AlertsClient";

export const metadata = {
  title: "Alertas de Estoque — Precision Auto",
};

export default function AlertsPage() {
  return <AlertsClient />;
}
