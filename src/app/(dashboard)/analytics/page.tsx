import { AnalyticsClient } from "./_components/AnalyticsClient";

export const metadata = {
  title: "Analytics — Precision Auto",
};

export default function AnalyticsPage() {
  return <AnalyticsClient />;
}
